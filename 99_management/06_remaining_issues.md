# 検出課題(GitHub Issue 化待ち)

> GitHub MCP 切断中のため一時記録。再接続後に `[設計再構成][区分]` で起票し、対応後 Close する。

## P1(要件分割)由来
| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| needs-review | NFR 全体 | 個別ファイルは NFR-001..079(79件)。P0 クロスウォーク想定(74)と差異。範囲行(NFR-029〜033 等)の展開で増加とみられる。整合確認 | NFR 採番の最終確定・crosswalk 更新 |
| migration | 要件全般 | 要件文に内在する実装寄り記述(具体値・技法)の基本設計移管要否を精査(P1 エージェント報告がセッション上限で欠落) | 要件×実装記述の棚卸し→移管/削除判断 |
| needs-review | RULE | 業務ルール抽出は 20 件(初版)。FR/BR 文中の定量・ポリシールールの網羅性未確認 | RULE 化候補の網羅監査 |
| traceability | 要件↔UC | 各要件の「対応業務UC」はプレースホルダ。P2 で UC 導出後に結線 | P2 完了時に要件→UC を充足 |

## P2(業務ユースケース導出)由来
| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| traceability | 要件→UC 逆引き | 仕様ファイル(`01_specifications/FR-*.md`)側の「対応業務UC」欄は未結線のまま。UC→要件(順引き)は各 UC-NNN の対応要件ID で確立済み。逆引き結線は P7 範囲 | P7 で FR-* に新 UC-NNN を結線 |
| needs-review | 代替フロー | 画面イベント由来 UC の「代替フロー」は原典が単一正常フロー(条件分岐は基本フローに内包)のため定型プレースホルダを置いた。条件分岐が明示的な UC は手動で代替フローへ分離する余地あり | 分岐の多い UC(ログイン・FAQ 編集等)で代替フロー精緻化を検討 |
| design-gap | 業務処理の UC 欠落 | 画面にもシステム処理にも紐づかない純業務目的(旧 UC-BIZ-013 等)は、システム起点 UC 群への束ねで表現し、新規の独立 UC は起こしていない | 要件側で画面/システムに無い業務処理が判明したら個別 UC を追加 |
| ~~migration~~ 解決済 | TBL アンカー | ~~各 UC の関連テーブルは現行 `TBL-*` ファイルへリンク(フラグメント無し)~~ → **P5 で解消**。`TBL-NNN` へ採番・`#TBL-NNN` アンカー付与し、UC `関連テーブルID` のリンクを `#TBL-NNN` 付きへ更新済み | 対応完了 |
| migration | EVT 紐づけ | 各 UC の「関連画面イベントID」は `(P3 で EVT 付与)/ 現行 SCR-xxx EV-nn` のプレースホルダ。P3 の EVT 個別化後に新 EVT-ID へ結線 | P3 完了時に UC の関連画面イベントID を EVT-NNN へ更新 |

## P2(業務UC導出)由来
| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| cleanup | UC-*.md 備考 | 「旧 UC-SCR-…/UC-SYSTEM-… から導出」の provenance 注記が残存(リンクは新UCで 0/0) | P8 最終クリーンアップで除去 or 移管記録へ |
| traceability | 要件↔UC | UC→要件は結線済。要件→UC 逆引きは P7 で付与 | P7 |
| needs-review | 代替フロー | 単一正常フロー由来はプレースホルダ。分岐多い UC は精緻化余地 | 後続精緻化 |

## P3(画面設計フラット化・EVT 個別化)由来
> SCR は `SCR-001..030`(フラット 30)へ採番。画面イベントは `EVT-001..229`(229)を個別ファイル化。EVT↔UC を 1:1 結線(229)。SCR §6 に EVT 列、§1 に「対応業務UC」行を付与。検証 0/0。再現スクリプト `_build/p3_scr_rename.py` / `_build/p3_evt_build.py` / `_build/p3_uc_link.py`。

| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| needs-review | EVT イベント名 | EVT の H1・イベント名は SCR §6「イベント」列をそのまま採用(例「『ログイン』を押下」)。一部は操作主語が省略されており、命名統一の余地あり | P8 で命名規約に沿って正規化を検討 |
| needs-review | EVT 遷移先 | EVT「遷移先」は §6 処理セル内に現れる `SCR-NNN` を機械抽出した候補。遷移以外の文脈で言及される画面が混じる可能性(例: 呼出元の明示) | 遷移先の手動精査(分岐の多いイベントで) |
| design-gap | システム UC に EVT 無し | UC-230..247(旧 UC-SYSTEM-*)は画面イベントを持たないため EVT 無し(設計どおり)。逆に全 229 EVT は UC と 1:1。UC 無し EVT・EVT 無し画面UC は 0 件 | 対応不要(記録のみ) |
| cleanup | API ID 未確定 | EVT の「呼出API」および §6 処理セルの API 参照は現行ID(`API-AUTH-002` 等)・現行ファイル名(`API-auth.md` 等)のまま(P4 でフラット化予定) | P4(API 採番)で EVT・SCR の API 参照を一括更新 |
| cleanup | EVT 処理セル | EVT `## 処理` は SCR §6 処理セルの HTML を複製(正本は SCR §6 と明記)。二重管理の懸念 | 表現重複の整理方針を P8 で確定(片方を正本リンクに寄せる等) |

## P3(画面/EVT)由来
| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| needs-review | EVT 遷移先 | 処理セルから SCR-NNN を機械抽出。遷移以外の言及混入の可能性 | 手動精査 |
| cleanup | EVT 処理 / SCR §6 | EVT `## 処理` は SCR §6 の複製(正本=§6)。二重管理 | P8 で正本一元化方針 |
| pending | EVT/SCR の API 参照 | 現行 API ID/ファイル名のまま | P4 で更新 |

## P4(API設計フラット化)由来
> API は旧グループ別 14 ファイル(`API-auth.md` ほか)を廃し、**1 エンドポイント = 1 ファイル**へ分割し `API-001`〜`API-059`(59)へフラット採番。各ファイルは §8-3 骨格(項目表 / 処理概要 / リクエスト / レスポンス / バリデーション / エラー / 利用テーブル / 備考)。項目表に EVT 経由の対応画面ID・対応画面イベントID、UC 逆引きの対応業務UC を結線(EVT→API 96 / UC→API 133 / API→TBL 117 / 対応 SCR 30)。全層の旧 API ID・旧グループファイル参照を一括張替。検証 0/0。再現スクリプト `_build/p4_reverse.py`(逆引き)/ `_build/p4_split.py`(分割)/ `_build/p4_index.py`(索引)/ `_build/p4_refs.py`(参照張替)。

| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| design-gap | 逆引き不可 API | `API-031`(FAQ 全文検索)・`API-032`(質問ログ検索)は EVT・UC のいずれからも逆引きできない。両者は FAQ / 未解決質問 画面内の検索機能だが、EVT 層(画面イベント)に独立した「呼出API」結線が無い | SCR-016(FAQ 管理)/ SCR-018 等の検索イベントに `API-031`/`API-032` の呼出API を追記し EVT 結線を補完(P3 EVT の見直し) |
| design-gap | EVT 無し(内部/IF系) | `API-003`(ログアウト)・`API-057`(AI 推論 IF)・`API-058`(メール配信 IF)・`API-059`(外部 Webhook)は画面イベントを直接持たない。`API-003` は UC のみ結線、`API-057/058/059` は内部 IF・Webhook で設計どおり EVT 無し | 対応不要(記録のみ)。`API-003` のログアウト操作 EVT が将来定義されれば結線 |
| ~~cleanup~~ 解決済 | TBL アンカー未確定 | ~~各 API の `## 利用テーブル` は TBL ファイルへフラグメント無しでリンク~~ → **P5 で解消**。TBL を `TBL-001`〜`TBL-031` へフラット採番・`#TBL-NNN` アンカー付与し、`## 利用テーブル` のリンクへ `#TBL-NNN` を補完済み(検証 0/0) | 対応完了 |
| cleanup | バリデーション節 | 多くの API は独立した「バリデーション」記述が原文に無く、`## バリデーション` は「`## リクエスト` の各パラメータ説明を参照」のプレースホルダ。原文に `バリデーション:` 箇条書きがある API(例 `API-008`)は実体を移送済み | 必要に応じて各 API の検証規則を精緻化(後続) |
| needs-review | 要約リード文 | 各 API の要約(引用ブロック)は原文「処理概要」先頭文を機械流用。一部に体言止め・受身が混在 | 文体統一(P8) |

## P5(DB設計フラット化・項目/逆引き整備)由来
> TBL を旧 `TBL-<分類>-<nnn>`(`TBL-M-001` 等 31 件)から **`TBL-001`〜`TBL-031`** へフラット採番。各 TBL に H1 アンカー `# <span id="TBL-NNN"></span><物理名>(<論理名>)` を付与し、リード文直後に「項目」セクション(テーブルID / 物理名 / 論理名 / 概要 / 主キー / 論理削除 / 監査項目 / **対応業務UC**(UC 逆引き) / **利用API**(API 逆引き))を挿入。`index.md` に「テーブル↔API/業務UC 対応表」「読み順」を追加。API の `## 利用テーブル`・UC の `関連テーブルID` のフラグメント無し TBL リンクへ `#TBL-NNN` を補完(P4 保留分の解消)。逆引き結線 API→TBL 117 / UC→TBL 178。全層の旧 TBL ID を一括張替。検証 0/0。再現スクリプト `_build/p5_rename.py`(採番・参照張替)/ `_build/p5_db_pages.py`(H1 アンカー・項目セクション)/ `_build/p5_fragments.py`(フラグメント補完)/ `_build/p5_index.py`(索引・対応表)。

| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| design-gap | 逆引き不可テーブル | `TBL-008 M_OWNER_QUOTA_OVR`・`TBL-011 M_ANNOUNCE_AUD`・`TBL-016 T_QLOG_FAQ_REFS`・`TBL-028 H_ERROR_LOGS`・`TBL-029 H_INQUIRY_FAQ`・`TBL-031 TP_AI_THRESH_CACHE` は API の `## 利用テーブル`・UC の `関連テーブルID` のいずれからも逆引きできない。中間テーブル(M:N)・内部キャッシュ・ログ・レート上書きで、画面/API イベントに直接結線が無いものが大半 | 中間/ログ/キャッシュ系は設計どおり(対応不要)。`M_OWNER_QUOTA_OVR`(レート上書き)は運用 API・`M_ANNOUNCE_AUD`(配信対象)はお知らせ作成 API の `## 利用テーブル` に追記して結線補完を検討 |
| design-gap | API 結線欠落(UC のみ) | `TBL-010 M_SERVICE_ANNOUNCE`(UC 10 / API 0)・`TBL-021 T_ANNOUNCE_RCPT`(UC 10 / API 0)・`TBL-012 M_TERMS_VER`(UC 3 / API 0)は業務UC からは参照されるが、どの API の `## 利用テーブル` にも現れない。お知らせ配信・規約取得の API 側 `## 利用テーブル` が未記載とみられる | お知らせ系 API・規約取得 API の `## 利用テーブル` に該当 TBL を追記(API 設計の補完) |
| needs-review | 論理名の出典 | 各 TBL の H1・項目セクションの論理名は `index.md` テーブル一覧(カタログ正本)から取得。一部は各ページ §概要 の論理名と微差あり(例 `M_PRJ_QUOTA_LIMITS`= index「プロジェクト別利用設定」/ ページ概要「プロジェクト別利用上限・無料枠」) | カタログ正本へ寄せる方針で問題ないが、ページ §概要 の論理名を index と一致させるか後続で統一 |
| needs-review | 論理削除/監査の自動判定 | 「論理削除」「監査項目」はカラム定義から機械判定(`valid`/`status='deleted'`/`deleted_at`、`created_at`/`updated_at`/`*_by_*`/`actor_id`)。追記専用ログや特殊な削除方式の取りこぼし可能性 | 履歴系(`H_*`)・FTS(`TP_FAQ_FTS`)等の削除/保持方針を手動確認 |
| cleanup | 既存セクションアンカー | 各 TBL ページの `### 概要/カラム定義/…` の `<span id>` は移行由来で不統一(`概要` と `3231-概要` が混在)。参照は解決するため検証 0/0 だが見た目が不揃い | 必要なら後続でセクションアンカーを統一(`#overview` 等) |

## P6b(権限・エラー・メッセージ設計)由来
> 権限を `PERM-001..011`(11)へ個別化(旧 `07_auth-design.md` を移管・削除)。API の `## エラー`(59 ファイル・77 行)を正規化し `ERR-001..035`(35)へ採番、各 API のエラーコードセルへ `ERR-NNN` をインライン併記。メールテンプレを `MSG-001..013`(13)へ個別化(旧 `06_mail-design.md` を移管・削除)。`06_permissions/` / `07_errors/` / `08_messages/` に各 index(ロール×操作可否・認可判定段・UC/SCR/EVT/API↔権限 対応表 / エラー一覧・EVT/API↔エラー 対応表 / メッセージ一覧・共通基準・配信運用)。削除 2 ファイルへの全リンクを新移管先へ張替(UC-241..244 / SCR-023 / SCR-024 / API-017 / 05_billing / index)。検証 0/0。再現スクリプト `_build/p6b_permissions.py` / `_build/p6b_errors.py` / `_build/p6b_messages.py` / `_build/p6b_relink.py`。

| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| design-gap | ERR 未採番の認可エラー | 認可判定段(PERM-002)が参照する `E-AUTH-SESSION-EXPIRED` / `E-AUTH-REAUTH-REQUIRED` / `E-AUTHZ-OWNER-BOUNDARY` / `E-AUTHZ-PROJECT-DENIED` / `E-AUTHZ-TERMS` / `E-BIZ-CONTRACT-DELETED` は、どの API の `## エラー` 表にも現れないため ERR-NNN 未採番(PERM/index ではインライン code で表示・壊れリンクなし)。401/403/404 偽装で API レスポンスに乗る想定だが API 側の `## エラー` 表が未記載 | 該当 API(認可ミドルウェア横断)の `## エラー` 表へ当該コードを追記 → `p6b_errors.py` 再実行で ERR 採番・PERM の判定段リンクを ERR-NNN 化 |
| needs-review | 契約停止エラーの命名差 | 契約停止エラーは旧 auth-design §7 が `E-BIZ-CONTRACT-SUSPENDED`、API(API-002 等)・ERR-006 は `E-BILL-CONTRACT-SUSPENDED`。同一概念で接頭辞 `E-BIZ`/`E-BILL` が混在 | どちらかへ命名統一(`E-BILL-*` が API 実体に一致)。統一後 PERM-009 / ERR-006 の記述を合わせる |
| needs-review | ERR 採番の粒度 | エラー表由来のため `(未サポート項目)`(ERR-031, 422)・`(冪等)`(ERR-035, 200)など厳密にはエラーでない応答も ERR 化。主コードがバッククォート無しの行は説明文をそのまま ID 名に採用 | 純粋なエラーのみに絞るか、応答コード(非エラー)を別系列にするか方針確定 |
| migration | 05_billing の業務ルール | 05_billing-design.md の定量ルール(無料枠 1,000 件・上限 100%/125% 停止・決済猶予 7 日・アラート閾値・受信履歴 30 日・退会猶予 90 日)は既存 `RULE-008/010/011/013..017` で網羅済み。**新規 RULE 起票は不要**と判断。05_billing は横断設計として存置(削除対象外)し、RULE 正本へリンクで送る現行方針を維持 | 対応不要(記録のみ)。FAQ 件数無料枠(100 件・5 円)= RULE-010/011 との突合のみ後続で再確認 |
| needs-review | PERM の UC 逆引き | PERM-NNN の「対応業務UC」は P6b では未結線(`—`)。SCR/EVT/API は結線済。UC↔権限の逆引きは要件↔UC 逆引き(P7)と同様に後続フェーズ範囲 | P7 で UC→PERM(または PERM→UC)を結線 |
| cleanup | MSG index の節番号 | `08_messages/index.md` の共通基準・配信運用は旧 mail-design の節番号(§2.1 / §5.1 / §6.1)を `<span id>` ごと転記。本文「§4」は MSG-NNN を指す。アンカーは解決(0/0)だが番号体系が新構成と不揃い | 後続で MSG index の節番号を新構成(§2..§5)に振り直し |
| needs-review | EVT↔ERR 結線 | ERR の「対応EVT」は API→EVT(index 対応表)経由の機械導出。1 API が複数 EVT を持つ場合は全 EVT を列挙しており、特定エラーが特定 EVT のみで起きる絞り込みは未実施 | 精緻な EVT 単位のエラー結線が必要なら手動精査 |

## P7(トレーサビリティ確立)由来
> 要件→業務UC→画面→画面イベント→API→テーブル の一気通貫トレーサビリティを確立。全層(FR/BR/UC/EVT/API/TBL)の構造化リンク・項目表を正規表現で解析しリンクグラフを構築。要件仕様 `FR-*/BR-*` の `## 関連`「対応業務UC」プレースホルダ(`(P2 で付与)` 全 340 件)を逆引き UC 群で充足(FR は UC の `対応要件ID` 直接逆引き、**BR は親子 FR→BR を経由した推移逆引き**)。該当 UC が無い要件は `(該当UCなし=ギャップ)` と明記。一気通貫マトリクスを `99_management/02_traceability_matrix.md`(UC 単位 247 行)へ生成。リンクグラフ `p7_linkgraph.json` / ギャップ `p7_gaps.json` を機械可読で出力。確定 ID は不変更(逆引き欄の充足のみ本文編集)。検証 0/0。再現スクリプト `_build/p7_traceability.py`(決定論的・冪等)。

| 区分 | 対象 | 内容 | 推奨対応 |
|---|---|---|---|
| design-gap | UC 無し要件(FR) | 全 194 FR のうち **132 FR** はどの UC の `対応要件ID` からも逆引きされず「対応業務UC」が `(該当UCなし=ギャップ)`。UC は 62 FR のみを参照(画面イベント由来 UC が参照する FR に偏り)。`FR-012/013/015/016/017/019/020/021/023/024/025/026/028/029/032/…/194`(全件は `p7_gaps.json` の `要件→UC 無し(逆引きギャップ)`) | 各 FR を実現する業務UC を `対応要件ID` に追記して結線、または当該 FR が UC 化不要(横断的非機能・将来対応)であれば要件側に分類注記。要件カバレッジの精査が必要 |
| design-gap | UC 無し要件(BR) | 全 146 BR のうち **91 BR** は親子 FR→BR 経由でも UC へ到達せず `(該当UCなし=ギャップ)`。`BR-006/009/011/015/017/019/020/022/023/…/146`(全件は `p7_gaps.json`)。原因は (a) 当該 BR を親に持つ FR が UC 未結線、または (b) BR を参照する FR が存在しない | 親 FR の UC 結線(上記 FR ギャップ解消)で連動して解消。FR を持たない BR は要件構造(BR↔FR)の見直し |
| design-gap | 要件無し UC | 全 247 UC のうち **128 UC** は `対応要件ID | —`(要件トレース欠落)。多くは画面イベント由来の操作系 UC(例 `UC-029`「+ 新規プロジェクトを作成 を押下」)で、対応する FR が未割当。マトリクス状態 = `要件定義未反映`(128 行) | 各操作系 UC が実現する FR を `対応要件ID` に追記。FR が未定義の操作は要件側に FR を起票して双方向トレースを確立 |
| design-gap | UC 逆引き不可 API | `API-031`(FAQ 全文検索)・`API-032`(質問ログ検索)は EVT・UC のいずれからも逆引きできず `対応業務UC` 無し(P4 既知。EVT 結線欠落に連動)。`API-003/057/058/059` は EVT 無し(内部/IF 系で設計どおり、UC は結線) | P4 ギャップと同一。検索イベントへの呼出API 結線(EVT 補完)で `API-031/032` の UC が連動結線 |
| design-gap | UC/API 逆引き不可テーブル | `TBL-008/011/016/028/029/031`(6 件)は UC 逆引き無し、`TBL-008/010/011/012/015/016/021/028/029/031`(10 件)は API 逆引き無し(P5 既知。中間/ログ/キャッシュ/レート上書き系) | P5 ギャップと同一。中間/ログ/キャッシュ系は設計どおり、お知らせ・規約・レート上書き系は API `## 利用テーブル` 追記で結線補完 |
| info | 欠番 | FR/BR/UC/EVT/API/TBL いずれも連番に欠番なし(`p7_gaps.json` に `* 欠番` キーなし) | 対応不要(記録のみ) |
