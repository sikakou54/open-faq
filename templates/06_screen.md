# 画面設計(SCR)テンプレート

> **本テンプレートは画面設計(SCR)の記載骨格(6 セクション固定)を定義します。**

運用ルールの正本は [../CLAUDE.md](../CLAUDE.md)。共通記載スタイルは [共通記載スタイル](00_common-style.md) を参照する。

- **配置先**: `02_basic_design/01_frontend/01_screens/`(画面モックは同階層配下の `mocks/`)
- **採番**: `SCR-001`〜(1 画面 = 1 ファイル(フラット。`-NNN` 従属は廃止)・ゼロ詰め 3 桁・欠番なし)

## 骨格(6 セクション固定)

**1 画面 = 1 ファイル(フラット `SCR-NNN`)**。6 セクション固定:

1. **画面概要**(画面 ID 表 + `トレーサビリティID` 行(`TR-NNN`。複数該当時は ` ・ ` 区切り))
2. **画面遷移図**(mermaid flowchart)
3. **画面レイアウト**(モック = **PNG 画像のみ**。HTML は `mocks/` に外出し)
4. **画面項目定義**(項目 ID `IT-01`…)
5. **入出力一覧**(CRUD マトリクス)
6. **画面イベント一覧**(列に **EVT 列**(`<span id="EVT-NNN"></span>EVT-NNN` の非リンクアンカー。EVT は独立ページ・フォルダを持たず、この §6 にアンカーとして保持する)/ イベント ID `EV-01`… / 項目 ID(§4 の `IT-` に対応、無ければ `—`)/ イベント / 処理)

## 記載ルール

- 各 `##`/`###` 直後に 1〜2 文のリード文を置いてから表へ入る(表をいきなり置かない)。
- §3 画面レイアウトは **PNG 画像で表示**する(MD は画像 1 行のみ)。HTML ソースは `mocks/<画面>-<n>.html` に外出しし、各 SCR からは `mocks/<画面>-<n>.png`(子階層相対・`../` なし)で参照する。画面モックの画像化手順は [../CLAUDE.md](../CLAUDE.md) の「画面モックの画像化」を参照。
- §1 の `トレーサビリティID` 行が本画面の TR 結線。UC ↔ 設計の対応はトレーサビリティ一覧表 [`00_traceability/index.md`](../00_traceability/index.md) の当該 TR 行で一元管理する(SCR 本文に `対応業務UC` / `関連画面` 等は持たない)。API 等へのリンクは [共通記載スタイル](00_common-style.md) の相互参照アンカー例に従う。

## 記載例(セクション骨格)

```markdown
# <span id="SCR-001"></span>SCR-001: ログイン

> **本画面は「…」を定義します。**

*種別 画面設計 ・ ステータス ドラフト*

## 1. 画面概要

…(リード文)

| 画面 ID | 画面名 | 機能概要 |
|----|----|----|
| SCR-001 | ログイン | … |

| 項目 | 内容 |
|----|----|
| トレーサビリティID | [TR-001](../../00_traceability/index.md#TR-001) |

## 2. 画面遷移図

\`\`\`mermaid
flowchart LR
    …
\`\`\`

## 3. 画面レイアウト

![ログイン](mocks/login-1.png)

## 4. 画面項目定義

| 項目ID | 名称 | … |
|----|----|----|
| IT-01 | … | … |

## 5. 入出力一覧

…(CRUD マトリクス)

## 6. 画面イベント一覧

| EVT | イベントID | 項目ID | イベント | 処理 |
|----|----|----|----|----|
| <span id="EVT-001"></span>EVT-001 | EV-01 | IT-01 | … | … |
```
