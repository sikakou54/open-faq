あなたは設計ドキュメントのレビューアです。提供ソース「00_rules」(= 運用ルール正本 CLAUDE.md と、各層テンプレート templates/ を連結したもの。各ファイルは `===== <相対パス> =====` で区切られる)だけを根拠に、最新版の整合性をレビューしてください。

ルール:
- 指摘は必ず該当する相対パス(例: `CLAUDE.md` / `templates/14_future.md`)を添える。
- ソースに書かれている事実のみに基づくこと。無いことは「ソースに記載なし」と述べる。
- 各観点について、問題があれば「指摘: …(該当パス)」、なければ「問題なし」と明示する。

今回の変更点(これらが矛盾なく反映されているかを重点的に確認):

1. ナビ自動生成の廃止: ページ内ナビ(パンくず・戻り導線)の自動付与と README 自動生成・`portal_nav.py` は廃止された。CLAUDE.md に `portal_nav.py` への参照や「マーカで囲って埋め込む」等の旧運用が**残っていないか**。README は手書きの最小トップページ、ナビ・索引は手動保守、という記述に統一されているか。

2. 相互参照アンカーの維持: ナビ廃止後も「相互参照アンカー(`<span id>` とリンクの書き方)」のルールは CLAUDE.md に残り、矛盾なく成立しているか。

3. 将来対応(FUT)の採番統一: FUT の ID 表記が `FUT-0N`(ハイフン + 2 桁)に統一され、`FUT-06` が `-req` / `-detail` の親子であることが、CLAUDE.md(採番ルール表・ID 体系)と `templates/14_future.md` の間で整合しているか。旧表記 `FUT*` や `FUT0N`(ハイフンなし)が残っていないか。

4. 業務ルール(RULE)の逆参照禁止: RULE が下流(業務UC)へのリンク(`### 適用UC` 等)を持たない strict ルールになっており、CLAUDE.md(粒度分離・トレーサビリティ)と `templates/04_business-rule.md` の間で矛盾していないか。

5. 全体整合: 上記変更によって CLAUDE.md 内・CLAUDE.md↔templates 間に新たな矛盾・抜け・重複・宙に浮いた参照(削除済み機構への言及)が生じていないか。

最後に、次の 2 つをまとめてください。
- 重大な指摘の有無(あり / なし)
- 総合所見(1 段落)
