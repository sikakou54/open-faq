# NotebookLM レビュー報告: EVT/SEV 独立ページ廃止・アンカー化

対象: 画面イベント設計書(EVT)・システムイベント設計書(SEV)の独立ページ/フォルダ/テンプレート削除と、ID の SCR §6 / SYS §6 非リンクアンカー化、SEQ のプレーンID参照化に伴う全層整合性レビュー。
判定者: Google NotebookLM(notebook: faqrev / 3caccb80-…)。7 ソース(00_rules / 01_requirements / 02_usecases / 03_screens_events / 04_backend / 05_sequences / 06_cross)を変更後コーパスで再同期して実施。

## レビュー回数
- EVT/SEV フォーカス: 4 回(初回 + 修正後 3 回)
- 整合性スイープ(全7ソース): 1 回

## EVT/SEV 変更に起因する指摘と対応(すべて修正・収束)

| # | NotebookLM 指摘 | 検証 | 対応 |
|---|---|---|---|
| E1 | `templates/13_permission-error-message.md` の PERM/ERR 骨格に旧 `EVT` 結線が残存 | 確定(Agent スコープ漏れ) | 見出しを `対応 UC / SCR / API`・`対応 API` へ修正、例表の `対応EVT` 行を全削除 |
| E2 | 同テンプレ MSG 骨格 + 実 MSG 13 ファイルが見出し「対応画面 / EVT / ERR」に EVT を残存 | 確定(実ファイルは EVT 行なし・SCR §6 参照) | 実 MSG 13 + テンプレの見出しから EVT を除去(§6 への誘導は日本語で保持) |
| E3 | CLAUDE.md 冒頭「1 つの識別子(… / イベント / …)= 1 ファイル」に EVT 残存 | 確定 | 「イベント」を除去 |
| E4 | CLAUDE.md 相互参照節に SEQ→EVT(非リンク広域ID)記法の説明不足 | 妥当(改善) | 「イベント ID の特例」注記を追加 |
| E5 | 採番表に EV-/SE-(ページ内局所ID)の位置づけ説明不足 | 妥当(改善) | 「ページ内局所IDは本表対象外」注記を追加(EVT-/SEV- は §6 アンカーの広域IDと明記) |
| E6 | 集約 index に EVT/SEV 列を設けない理由の注記不足 | 妥当(改善) | `apis/index.md §2` に EVT/SEV 内包の [!NOTE] を追加 |

最終確認レビューで NotebookLM が E3/E4 を再指摘したが、**引用文は修正後テキストそのもの**であり、再同期直後のインデックス遅延+文脈引きずりによるステイル指摘。リポジトリ実体(CLAUDE.md 行5・行139)を grep で確認し解消済みと判定。

## EVT/SEV と無関係の既存指摘(本レビュー対象外・要判断)

整合性スイープ等で surface したが、いずれも **EVT/SEV 削除に起因しない既存課題**(回帰ではないことを git 原本と照合済み)。

| # | 指摘 | 検証 | 区分 |
|---|---|---|---|
| P1 | 個別 API ページの「利用テーブル」が空 | **偽陽性**(NotebookLM の連結表誤読。実体は投入済) | 対応不要 |
| P2 | `apis/index.md` の API-031/032 対応UC が `—`(個別ページは UC-083) | 確定・既存(HEAD 時点で既に `—`。EVT列削除の回帰ではない) | 既存トレース欠落 |
| P3 | `project_user`(actorType/CHECK)と「メンバー」呼称統合の不一致 | 確定・既存 | 用語統一(要判断) |
| P4 | `02_backend/01_system/index.md` メタ「システム処理 15」(実 35) | 確定・既存 | 件数更新(軽微) |
| P5 | `01_frontend/01_screens/index.md` メタ「画面数 31」(実 32) | 確定・既存 | 件数更新(軽微) |
| P6 | `API-029` 認可が `API-022 と同一`(FAQ系であるべき) | 確定・既存 | 認可参照誤り(要判断) |
| P7 | `02_basic_design/index.md` FR表に旧体系サブID `009-001` 等 + 直書きエンドポイント + テーブル名重複 | 確定・既存(旧 SCR 子ID形式のレガシー表) | レガシー表の整理(要判断) |

## 壊れリンク/アンカー
- 実コンテンツ(471 md)で **0 / 0**。
- 残 43 件の未解決リンクは CLAUDE.md/templates の例示・雛形プレースホルダ(HEAD にも存在、本変更起因でない)。

## 既存課題 P2〜P7 の対応(ユーザー指示で実施)

| # | 対応 | 内容 |
|---|---|---|
| P1 | 対応不要 | 偽陽性(NotebookLM の連結表誤読)。 |
| P2 | **修正** | `apis/index.md §2` の API-031/032 対応UC `—` → `UC-083`(個別ページと一致。UC-083=FAQ・質問ログ検索)。 |
| P3 | **対応不要(設計判断)** | `project_user` は11テーブルの CHECK 制約値 + API-002 `actorType` 契約値として一貫使用の技術 enum。論理名/説明は既に「メンバー」、対応は TBL-029 で明文化。enum 値変更は破壊的かつ非欠陥のため据置。 |
| P4 | **修正** | `01_system/index.md` メタ「システム処理 15」→「35」(実数 SYS-001〜035)。 |
| P5 | **修正** | `01_screens/index.md` メタ「画面数 31(〜031)」→「32(〜032)」(実数 SCR-001〜032)。 |
| P6 | **修正** | `API-029` 認可の誤参照注記「(API-022 と同一)」を個別ページ + `apis/index.md §1.4` の両所から除去(兄弟 FAQ API-025〜033 と整合)。 |
| P7 | **修正** | `02_basic_design/index.md` FR表の壊れた旧サブID(`009-001`/`004-001`/`005-001`/`006-001`)を除去、素の番号を `SCR-###` へ正規化、`M_PRJ_USERS` の重複を解消。精密な画面対応は正本(画面設計 §4)に委譲。 |

## 後続対応(ユーザー指示): イベントID を API / ERR / PERM へ復元

EVT 独立ページ廃止後、ユーザー指示により画面イベント `EVT` を参照する結線を以下へ復元(`9bd3e26` の対応付けを復元し、**リンクではなくプレーンテキストの広域 ID** で記載。SCR §6 アンカーが定義元、SEQ と同方式)。

- API 個別ページ(61): 「対応画面イベントID」行を復元
- `apis/index.md §2`: 「対応EVT」列を復元(P2 の UC-083 / P6 の認可注記は保持。EVT 除外の `[!NOTE]` は撤去)
- ERR 個別ページ(36)+ `errors/index.md §2`: §3「対応 API / EVT」見出し・「対応 EVT」行/列を復元
- PERM 個別ページ(11)+ `permissions/index.md §3`: §2「対応 UC / SCR / EVT / API」見出し・「対応EVT」行/列を復元
- `templates/10_api.md` / `CLAUDE.md`(採番表・相互参照注記)を整合
- 検証: 壊れリンク/アンカー 0/0、EVT は全てプレーンテキスト、`02_screen_events` 参照 0。

## 判定
- **EVT/SEV 独立ページ廃止・アンカー化に関する整合性: 合格(収束)。** 変更起因の不整合・参照切れ・回帰はゼロ。
- 既存課題: P2/P4/P5/P6/P7 を修正、P3 は設計判断で据置、P1 は偽陽性。壊れリンク/アンカー 0/0(実コンテンツ)。
- 後続: イベントID を API/ERR/PERM へプレーンテキストで復元(上記)。
