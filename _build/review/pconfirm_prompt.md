あなたはソフトウェア設計ドキュメントの外部レビュアです。提供されたソースのみに基づき、事実に忠実に回答してください。実在する相対パス・ID・引用を根拠に挙げ、推測で補完しないでください。連結 Markdown 表のセルを空と誤読しやすい点に注意し、表が空に見えても実在を断定しないこと。

# 確認対象(以下の個別項目が解消されているか、ピンポイントで判定)
直前のレビューで指摘した既存課題について、現在のソースで解消されているかだけを確認してください。新規の網羅レビューは不要です。

1. **P2**: `02_basic_design/02_backend/03_apis/index.md` の「API ↔ UC / TBL 対応表」で、API-031(FAQ 全文検索)/ API-032(質問ログ検索)の対応UC が、個別ページと同じ `UC-083` になっているか(以前は `—`)。
2. **P4**: `02_basic_design/02_backend/01_system/index.md` のメタ件数が、実在する SYS の総数(SYS-001〜SYS-035)と一致しているか。
3. **P5**: `02_basic_design/01_frontend/01_screens/index.md` のメタ件数が、実在する SCR の総数(SCR-001〜SCR-032)と一致しているか。
4. **P6**: `02_basic_design/02_backend/03_apis/API-029.md`(および apis/index.md の API-029 行)の認可欄から、無関係な「(API-022 と同一)」という注記が除去され、兄弟 FAQ API(API-025〜033)と整合した記述になっているか。
5. **P7**: `02_basic_design/index.md` の「要件定義 → 基本設計 の落とし込み」FR 表の「画面」列に、`009-001` / `004-001` / `005-001` / `006-001` のような旧体系サブ ID が残っていないか。また `M_PRJ_USERS` の重複が解消されているか。
6. **P3(設計判断の妥当性確認)**: DB の種別カラム(例 `actor_type` / `created_by_type` 等)の CHECK 値および API-002 の `actorType` で用いる技術 enum 値 `project_user` は、論理名・説明としては「メンバー」を用い、TBL-029 に値→論理名の対応が明記されている。この「物理/契約 enum 値は `project_user`、論理表記は メンバー で統一」という方針は整合的か(enum 値自体を変える必要はないとの判断で問題ないか)。

# 出力(厳守)
各項目 P2〜P7 について 1 行ずつ、次の形式で判定。
| 項目 | 判定(解消/未解消/妥当/要再検討) | 根拠(相対パス + ID/引用) |
最後に「上記以外に、これらの修正に起因する新たな不整合があれば挙げる。無ければ『新たな不整合なし』」を 1〜2 文で。
