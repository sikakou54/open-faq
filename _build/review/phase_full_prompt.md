あなたはソフトウェア設計ドキュメントの外部レビュアです。提供された全ソース(最新)のみに基づき、事実に忠実に最終総合レビューを実施してください。推測で補完せず、実在 ID を根拠に挙げてください。

# 判定基準
00_rules(CLAUDE.md=設計ルール正本)。

# これまでの修正(確認のため)
逆参照(要件→下流委譲文)除去・旧ID(E-/MSG-SCR)統一・粒度(SQL/メソッド名/物理名)除去・システム設計 SYS-016〜035 と SEV-029〜068 の新設(システム起点 UC を網羅)・用語統一(アカウント利用者/メンバー)・SEQ のトレース欄(業務要件 BR・メッセージ MSG)の結線完了、を実施済み。

# 最終確認観点(各観点で残存指摘の有無を判定)
1. 構成・記載粒度: 要件定義と基本設計の責務分離、BR/FR/UC/基本設計の粒度混在、要件への細かすぎる識別子混入、基本設計への実装レベル(SQL/メソッド/物理カラム/算出式)混入が残っていないか。
2. 逆参照: 要件定義が基本設計・画面・API・テーブル等の下流や下流識別子を参照/委譲していないか。トレース方向が要件→基本設計か。
3. 抜け漏れ: 要件→基本設計(画面/API/テーブル/権限/エラー/メッセージ/システム処理 SYS/外部連携/バッチ)の落とし込み漏れ、異常系・外部連携失敗・非機能の考慮漏れが残っていないか。特にシステム起点 UC への SYS 整備、SEQ の BR/MSG 結線が妥当か。
4. 整合性: 用語/ID/ステータス/ロール/エラーコードの統一、旧体系 ID・廃止・重複・矛盾の残存、層間(画面↔API↔テーブル↔権限↔エラー↔メッセージ)の整合。

# 出力形式
最初に「総合判定: 指摘なし / 指摘あり」を明記。指摘がある場合のみ次の表で列挙。

| No | 重要度 | 観点 | 対象ドキュメント | 指摘内容 | 根拠(実在ID) | 修正方針 |

重要度=高/中/低。実在 ID を伴わない推測指摘は挙げないこと。残存指摘が無ければ「指摘なし」と明記。
