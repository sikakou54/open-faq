あなたはソフトウェア設計ドキュメントの外部レビュアです。提供されたソースのみに基づき、事実に忠実に回答してください。推測で補完しないでください。

# レビュー対象
業務ユースケース(UC-001..UC-088)= ソース「02_usecases.md」。判定基準は「00_rules.md」(設計ルール正本)。

# レビュー観点(UC の逆参照・粒度)
CLAUDE.md の規約では、UC 本文に基本設計レベルの情報を書いてはならない。次を確認してください。
1. 画面ID `SCR-NNN` / 画面イベント `EVT-NNN` / API `API-NNN` / テーブル `TBL-NNN` / シーケンス `SEQ-NNN` / エラー `ERR-NNN` / メッセージ `MSG-NNN` が本文に混入していないか。
2. 画面名・ボタン名・フォーム名・入力項目名、API 名・エンドポイント・HTTPメソッド・ステータスコード、テーブル名・物理名・カラム名・SQL、クラス/関数/メソッド名が混入していないか。
3. mermaid 図(```mermaid```)が UC 本文に含まれていないか。
4. UC が「業務処理粒度(誰が・何の目的で・どの業務を・どう実行するか)」になっているか。1 画面イベント = 1 UC のような操作粒度、または umbrella 過ぎる粒度になっていないか。
5. UC の `対応する業務要件ID(BR)` `対応する機能要件ID(FR)` `関連する業務ルールID(RULE)` のみが本文トレースとして保持され、基本設計層への順方向リンク(関連画面ID 等)を持っていないか(規約準拠か)。

# 出力形式(厳守)
最初に総括を1〜2文。続けて、指摘を次の Markdown 表で列挙してください。対象箇所は UC-ID と相対パスで必ず特定してください。

| No | 重要度 | 対象ドキュメント | 対象箇所 | 逆参照の内容 | 問題点 | 修正方針 |

重要度は 高(基本設計情報の混入・重大な粒度逸脱)/ 中(粒度・参照方向の問題)/ 低(軽微な表現)。
**指摘が一件も無い場合は、表の代わりに「逆参照なし」とだけ明記してください。** 根拠の無い指摘は挙げないでください。
