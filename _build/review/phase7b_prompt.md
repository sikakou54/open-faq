あなたはソフトウェア設計ドキュメントの外部レビュアです。提供された全ソース(更新後)のみに基づき、事実に忠実に回答してください。実在 ID を根拠に挙げ、推測で補完しないでください。

# 背景
前回指摘「システム起点ユースケースに対応する システム設計(SYS)の欠落」等に対応し、SYS-016〜035 と SEV-029〜068 を新設、用語(管理者ユーザー→アカウント利用者/メンバー)を統一しました。解消確認と新規問題の有無を再レビューしてください。

# 確認項目(各項目を「解消/未解消」で判定し、根拠 ID を示す)
1. システム起点の業務ユースケース(UC-049〜088 のうち「システムが〜する」もの)それぞれに、対応する システム設計(SYS-001〜035)が存在するか。SEQ はあるが SYS が無い、という欠落が残っていないか。
2. 新設 SYS-016〜035 は、各々の対応業務UC・SEQ と整合し、関連表(FR/BR/RULE/対応業務UC)とトレースが妥当か。特に SYS-034(保持期間超過データの自動論理削除)が NFR-045/NFR-049 を根拠とし、SYS-035(課金通知 取込失敗の再処理)が UC-061/FR-099 と SYS-006 に整合しているか。
3. 新設 SYS/SEV に、基本設計を超える実装詳細(SQL・クラス/メソッド名・ORM・テーブルCRUDの本文記載・物理カラム名)が混入していないか。図は業務語の mermaid flowchart か。
4. 用語: 「管理者ユーザー」が要件本文から除かれ「アカウント利用者」(アカウント保有者の総称)/「メンバー」へ適切に統一されているか(billing-design の変更履歴の記録は除く)。新たな用語不統一が生じていないか。
5. これらの追加・変更により、新たな矛盾・リンク不整合・逆参照(要件が下流を参照する記述)が生じていないか。

# 出力形式
最初に「総合判定: 全解消 / 一部未解消」を明記。続けて次の表。

| 確認項目 | 判定 | 根拠(実在 ID。未解消なら該当箇所) |

未解消・新規問題があれば重要度付きで列挙。無ければ「新規問題なし」と明記。
