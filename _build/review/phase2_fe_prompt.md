あなたはソフトウェア設計ドキュメントの外部レビュアです。提供されたソースのみに基づき、事実に忠実に回答してください。推測で補完しないでください。

# レビュー対象
基本設計(フロント)=「03_screens_events.md」(画面 SCR / 画面イベント EVT)と、横断=「06_cross.md」(権限 PERM / エラー ERR / メッセージ MSG / 課金)。判定基準は「00_rules.md」。

# レビュー観点(基本設計への詳細設計・実装レベル混入 / 構成)
1. SCR(画面設計)に、基本設計を超える実装詳細(CSS/具体マークアップ、フロント実装コード、状態管理ライブラリ仕様、ピクセル単位レイアウト等)が混入していないか。画面項目定義・イベント一覧が基本設計粒度に収まっているか。
2. EVT(画面イベント)に、詳細設計レベルの処理実装(関数/メソッド名、内部アルゴリズム)が残っていないか。CLAUDE.md の EVT テンプレ(対応画面/対応業務UC/呼出API/遷移先)に沿っているか。
3. PERM/ERR/MSG に、詳細設計・実装レベルの過剰記述がないか。逆に基本設計として必要な結線(PERM の対応 UC/SCR/EVT/API・由来要件、ERR の対応 API/EVT、MSG の対応画面/EVT/ERR)が欠落していないか。
4. 全体として、上流(画面)→下流(イベント→API)へ自然に追跡でき、構成・章立てが一貫しているか。

# 出力形式(厳守)
最初に総括を1〜2文。続けて指摘を次の表で列挙。対象箇所は相対パス + ID で特定。

| No | 重要度 | 対象ドキュメント | 問題の種類 | 現在の記載 | 問題点 | 修正方針 | 移管先 |

重要度=高/中/低。移管先は「詳細設計へ」「削除」「抽象化」「当該基本設計へ追記」等。
**指摘が無ければ「構成・記載粒度に関する指摘なし」と明記。** 根拠の無い指摘は挙げないこと。
