あなたはソフトウェア設計ドキュメントの外部レビュアです。提供されたソースのみに基づき、事実に忠実に回答してください。推測で補完しないでください。

# レビュー対象
基本設計(バックエンド)=「04_backend.md」(システム SYS / システムイベント SEV / API / テーブル TBL)と、シーケンス=「05_sequences.md」(SEQ)。判定基準は「00_rules.md」。

# レビュー観点(基本設計への詳細設計・実装レベル混入)
CLAUDE.md は、基本設計には詳細設計・実装レベルを書かない方針。特にシーケンス(SEQ)では「SQL・クラス/メソッド名・ORM・テーブル CRUD・コンポーネント内部 ID は書かない」。次を確認してください。
1. SEQ 図に SQL・クラス/関数/メソッド名・ORM 呼び出し・テーブル別 CRUD 表記・内部 ID(旧 `API-XXX-NNN`/`IT-`/`EV-`/`E-`)が混入していないか。SEQ のアクターが規約どおり(画面起点=ユーザー/画面/サーバー、システム起点=外部実体/サーバー)になっているか。
2. API 設計に、基本設計を超える実装詳細(具体的なアルゴリズム実装、SQL 文、ORM/クラス設計、コード断片)が混入していないか。
3. TBL 設計に、物理 DDL を超える実装・チューニング詳細(インデックス内部実装、パーティション物理設計、トリガ実装コード等)が過剰に書かれていないか。
4. SYS/SEV(システム処理・イベント)に、詳細設計へ移管すべきアルゴリズム/実装手順が残っていないか。
5. 逆に、基本設計として必要な項目(対応業務UC・利用テーブル・エラー結線・I/O 定義など)が欠落している箇所がないか。

# 出力形式(厳守)
最初に総括を1〜2文。続けて指摘を次の表で列挙。対象箇所は相対パス + ID で特定。

| No | 重要度 | 対象ドキュメント | 問題の種類 | 現在の記載 | 問題点 | 修正方針 | 移管先 |

重要度=高/中/低。移管先は「詳細設計へ」「削除」「抽象化」「(欠落の場合)当該基本設計へ追記」等。
**指摘が無ければ「構成・記載粒度に関する指摘なし」と明記。** 根拠の無い指摘は挙げないこと。
