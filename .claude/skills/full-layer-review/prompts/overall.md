<!-- TEMPLATE: 総合レビュー / 修正後の再レビュー. Scope to ALL sources. -->
あなたはソフトウェア設計ドキュメントの外部レビュアです。提供された全ソース(最新)のみに基づき、最終総合レビューを実施してください。推測で補完せず、実在 ID を根拠に挙げてください。

<!-- 再レビュー時はここに「今回の修正概要」を1段落で書き、解消確認を促す:
これまでに <逆参照除去 / 旧ID統一 / 粒度是正 / 設計層新設 / 用語統一 / トレース結線> を実施済み。解消確認と新規問題の有無を再レビューしてください。 -->

# 最終確認観点(残存指摘の有無)
1. 構成・記載粒度: 要件と基本設計の責務分離、粒度混在、要件への識別子混入、基本設計への実装レベル混入が残っていないか。
2. 逆参照: 要件が下流/下流識別子を参照・委譲していないか。トレース方向が要件→基本設計か。
3. 抜け漏れ: 要件→基本設計の落とし込み漏れ、異常系・外部連携・非機能の考慮漏れが残っていないか。
4. 整合性: 用語/ID/コードの統一、旧体系・廃止・重複・矛盾の残存、層間整合。

# 出力(厳守)
最初に「総合判定: 指摘なし / 指摘あり」を明記。指摘がある場合のみ次の表。
| No | 重要度 | 観点 | 対象ドキュメント | 指摘内容 | 根拠(実在ID) | 修正方針 |
重要度=高/中/低。実在 ID を伴わない推測指摘は挙げない。残存指摘が無ければ「指摘なし」と明記。

<!-- 受領後は必ず各指摘を実ファイルで groundedness 検証してから対応すること(NotebookLM は pipe 表の誤読・要件スコープの過剰一般化を起こす)。 -->
