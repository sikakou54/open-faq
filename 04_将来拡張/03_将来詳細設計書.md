# FAQ AIウィジェット SaaS Future 詳細設計

| 項目 | 内容 |
|---|---|
| 文書種別 | Future / Post-MVP 詳細設計 |
| 版 | v1.0 |
| 対象システム | FAQ AIウィジェット SaaS(open-faq) |
| 対応要件 | [01_将来要件定義書.md](01_将来要件定義書.md) |
| 対応基本設計 | [02_将来基本設計書.md](02_将来基本設計書.md) |
| 作成日 | 2026-05-14 |
| 最終改訂日 | 2026-05-14 |

---

## 0. 位置づけ

本書は、Post-MVP 要件を実装へ落とす際の DDL / API / Worker / Queue / 移行 / テスト / Runbook 候補を管理する。MVP 詳細設計本文には、ここに記載する候補を戻さない。

---

## 1. 実装設計バックログ

| ID | 対象 | 実装設計で具体化すること | 関連 Future 要件 |
|---|---|---|---|
| FUT-DD-SEC-001 | 個別チャット列単位暗号化 | 暗号化カラム、鍵派生、ローテーション、バックフィル、復元手順、検索制約、監査ログ | FUT-REQ-SEC-001 |
| FUT-DD-SEC-002 | 利用者側 MFA | `accounts.mfa_enforcement`、MFA 登録 API、回復コード、未設定時ログイン制御、監査ログ | FUT-REQ-SEC-002 |
| FUT-DD-SEC-003 | 4-eyes 完全強制 | `operator_approvals` の対象操作拡張、状態遷移、承認者重複防止、バイパス削除、E2E | FUT-REQ-SEC-003 |
| FUT-DD-SEC-004 | 運営者サブロール | `operator_subroles` 相当テーブル、認可ミドルウェア、メニュー制御、監査ログ role snapshot | FUT-REQ-SEC-004 |
| FUT-DD-AI-001 | Vectorize 連携 | 埋め込み生成 Queue、インデックス同期、再生成、検索 API フォールバック、コストメトリクス | FUT-REQ-AI-001 |
| FUT-DD-AI-002 | SSE / ストリーミング応答 | SSE endpoint、chunk 保存方針、途中失敗、メータリング確定、クライアント再接続 | FUT-REQ-AI-002 |
| FUT-DD-AI-003 | PII NER 第 2 層 | model adapter、推論タイムアウト、判定統合、誤検出報告反映、評価データセット | FUT-REQ-AI-003 |
| FUT-DD-AI-004 | 矛盾検知意味類似度化 | embedding table、類似度計算 Worker、しきい値、false positive 学習、再実行ジョブ | FUT-REQ-AI-004 |
| FUT-DD-I18N-001 | 多言語 UI | i18n key 管理、メールテンプレート多言語化、fallback、翻訳差分 CI | FUT-REQ-I18N-001 |
| FUT-DD-I18N-002 | マルチリージョン | `accounts.region`、routing、D1 / R2 配置、region guard、越境拒否 API | FUT-REQ-I18N-002 |
| FUT-DD-I18N-003 | 管理者単位 TZ | profile setting、日付フォーマット、集計境界、監査ログ UTC 正本との変換 | FUT-REQ-I18N-003 |
| FUT-DD-PLAN-001 | プラン別機能 | `plan_entitlements`、feature gate、請求確定、プラン変更履歴、回帰テスト | FUT-REQ-PLAN-001 |
| FUT-DD-PLAN-002 | ホワイトラベル | brand assets、domain verification、CDN purge、テーマ CSS、権利表示監査 | FUT-REQ-PLAN-002 |
| FUT-DD-UX-001 | お知らせリアルタイム化 | Durable Object / SSE endpoint、subscription 管理、再接続、fallback polling | FUT-REQ-UX-001 |
| FUT-DD-OPS-001 | SBOM / OSS ライセンス | CI 生成、CycloneDX / SPDX 出力、ライセンス allowlist、PR ブロック条件 | FUT-REQ-OPS-004 |
| FUT-DD-OPS-002 | Runbook 追加 | RB-009〜RB-018 の起動条件、コマンド、通知、証跡、ポストモーテムテンプレート | FUT-REQ-OPS-005 |

---

## 2. DDL 候補

### 2.1 契約 MFA 強制

```sql
ALTER TABLE accounts
  ADD COLUMN mfa_enforcement TEXT NOT NULL DEFAULT 'off'
  CHECK (mfa_enforcement IN ('off', 'admin_only', 'all_members'));

CREATE TABLE owner_mfa_policy_audit_logs (
  id             TEXT PRIMARY KEY,
  owner_account_id      TEXT NOT NULL,
  changed_by     TEXT NOT NULL,
  before_policy  TEXT NOT NULL,
  after_policy   TEXT NOT NULL,
  reason         TEXT,
  created_at     TEXT NOT NULL
);
```

### 2.2 運営者サブロール

```sql
CREATE TABLE operator_subroles (
  id              TEXT PRIMARY KEY,
  operator_id     TEXT NOT NULL,
  subrole         TEXT NOT NULL,
  granted_at      TEXT NOT NULL,
  granted_by      TEXT NOT NULL,
  revoked_at      TEXT,
  FOREIGN KEY (operator_id) REFERENCES service_operators(id),
  FOREIGN KEY (granted_by) REFERENCES service_operators(id),
  CHECK (subrole IN ('billing_only', 'audit_read_only', 'support_operator', 'security_admin'))
);

CREATE INDEX idx_operator_subroles_operator
  ON operator_subroles(operator_id, revoked_at);
```

### 2.3 マルチリージョン

```sql
ALTER TABLE accounts
  ADD COLUMN region TEXT NOT NULL DEFAULT 'apac'
  CHECK (region IN ('apac', 'wnam', 'eeur'));

CREATE TABLE owner_region_audit_logs (
  id             TEXT PRIMARY KEY,
  owner_account_id      TEXT NOT NULL,
  before_region  TEXT NOT NULL,
  after_region   TEXT NOT NULL,
  approved_by    TEXT NOT NULL,
  reason         TEXT NOT NULL,
  created_at     TEXT NOT NULL
);
```

### 2.4 個別チャット本文の列単位暗号化

```sql
ALTER TABLE chat_messages
  ADD COLUMN body_ciphertext BLOB,
  ADD COLUMN body_nonce TEXT,
  ADD COLUMN body_key_version INTEGER;

CREATE TABLE owner_key_versions (
  id             TEXT PRIMARY KEY,
  owner_account_id      TEXT NOT NULL,
  key_version    INTEGER NOT NULL,
  status         TEXT NOT NULL,
  created_at     TEXT NOT NULL,
  rotated_at     TEXT,
  UNIQUE (owner_account_id, key_version),
  CHECK (status IN ('active', 'retired'))
);
```

### 2.5 プラン別機能

```sql
CREATE TABLE plan_entitlements (
  id              TEXT PRIMARY KEY,
  plan_code       TEXT NOT NULL,
  feature_code    TEXT NOT NULL,
  limit_value     INTEGER,
  enabled         INTEGER NOT NULL DEFAULT 1,
  created_at      TEXT NOT NULL,
  updated_at      TEXT NOT NULL,
  UNIQUE (plan_code, feature_code)
);
```

---

## 3. API / Worker 候補

| 領域 | 候補 |
|---|---|
| 契約 MFA | `POST /api/v1/settings/mfa-policy`、`POST /api/v1/auth/mfa/setup`、`POST /api/v1/auth/mfa/recovery-code` |
| 4-eyes | `POST /admin/v1/operator-approvals`、`POST /admin/v1/operator-approvals/{id}/approve`、`POST /admin/v1/operator-approvals/{id}/execute` |
| Vectorize | `FaqEmbeddingSyncWorker`、`POST /internal/v1/faqs/{id}/embedding-refresh`、検索 API の `mode=semantic` |
| SSE / リアルタイム | `GET /widget/v1/questions/{id}/stream`、`GET /api/v1/inbox/stream`、fallback polling |
| PII NER | `PiiNerWorker`、`POST /internal/v1/pii/evaluate`、誤検出報告反映ジョブ |
| マルチリージョン | `TenantRegionGuard`、`POST /admin/v1/owners/{id}/region-approval` |
| ホワイトラベル | `POST /api/v1/settings/brand-assets`、domain verification Worker、CDN purge job |
| SBOM | CI job `generate-sbom`、license policy check、release artifact upload |

---

## 4. 移行・リリース設計候補

Future の移行・リリース設計候補は [運用手順書.md](../03_運用/運用手順書.md) の「Future 詳細設計 移行・リリース候補」へ移管した。

## 5. テスト・検証候補

| 領域 | 検証項目 |
|---|---|
| MFA | 未登録ユーザーのログイン遮断、回復コード、強制 ON 変更時の既存セッション扱い、監査ログ |
| 暗号化 | key rotation、dual read、復元、検索不可項目のエラー、バックフィル再実行性 |
| 4-eyes | 申請者 = 承認者禁止、承認期限切れ、二重実行防止、緊急例外、監査ログ |
| Vectorize | embedding 欠落時 fallback、同期遅延、ランキング品質、コスト上限 |
| SSE | 切断・再接続、途中失敗、二重課金防止、モバイルブラウザ互換 |
| 多言語 | 翻訳キー欠落、メールテンプレート、日付・数値フォーマット、a11y |
| マルチリージョン | region guard、越境拒否、D1 / R2 配置、監査ログの region 記録 |
| SBOM | CI 出力、ライセンス allowlist、PR ブロック、リリース成果物への添付 |

---

## 6. Runbook 候補

Future の Runbook 候補は [運用手順書.md](../03_運用/運用手順書.md) の「Future 詳細設計 Runbook 候補」へ移管した。
