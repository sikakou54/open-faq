# テーブル定義書(メイン)

## 1. 文書概要

### 1.1 目的

メインシステムのデータベース(Cloudflare D1)に存在するすべてのテーブルについて、概要 / カラム定義 / 主キー / 外部キー / インデックス / 制約 / コード値・区分値の 7 サブセクションを統一フォーマットで定義する。SQLite/D1 を前提とし、DDL ブロックは持たず、本書の 7 サブセクション表が正本。

### 1.2 対象範囲

- 対象: 利用者向けMVPデータ(認証 / プロジェクト / FAQ / 未解決質問 / 通知 / 課金 / お知らせ / 監査 / 規約 等)
- 対象外: 運営者側専用テーブル(`operator_*`, ハッシュチェーン正本側 `audit_logs`, `webhook_events`, `operator_approvals`, `accounts_retired` 等) ── [../../02_運営者システム/02_基本設計/03_テーブル設計.md](../../02_運営者システム/02_基本設計/03_テーブル設計.md) 参照

### 1.3 版数

| 項目 | 値 |
|---|---|
| 版数 | 2.2(MVP構成) |
| 更新日 | 2026-06-12 |

### 1.4 関連ドキュメント

| ドキュメント名 | 役割 | 参照先 |
|---|---|---|
| 索引 | 11 ドキュメント体系の俯瞰 | [index.md](index.md) |
| 共有概念対応表 | `contract_owners.contract_status` / オーナーユーザーモデルなど | [../../共有/共有概念.md](../../共有/共有概念.md) |
| API 設計 | テーブルを操作する API | [02_API設計.md](02_API設計.md) |
| 認証・認可設計 | オーナー境界判定 | [08_認証・認可設計.md](08_認証・認可設計.md) |
| 権限設計 | `project_users.role`(3 ロール定義) | [04_権限設計.md](04_権限設計.md) |
| セキュリティ設計 | PII 暗号化 / 監査 / 削除モード | [09_セキュリティ設計.md](09_セキュリティ設計.md) |
| 課金・請求設計 | `contract_owners.contract_status` 正本 / 契約上書きテーブル | [10_課金・請求設計.md](10_課金・請求設計.md) |
| 詳細設計 | 実装関連の詳細(モジュール構成 / バッチ / マイグレーション)| [../03_詳細設計/index.md](../03_詳細設計/index.md) |

### 1.5 フォーマット規約

各テーブル(§3.x)は以下の 7 サブセクション構成。

| サブセクション | 内容 |
|---|---|
| N.1 概要 | テーブル名 / 論理名 / 説明 / 主管 FR / 保持期間 / `contract_owner_user_id` / `retention_class` / 設計判断 |
| N.2 カラム定義 | No / 論理名 / 物理名 / データ型 / 桁数 / NULL / PK / FK / UNIQUE / DEFAULT / 説明(11 列)|
| N.3 主キー | 主キー名 / 対象カラム |
| N.4 外部キー | 外部キー名 / 対象カラム / 参照先テーブル / 参照先カラム / 更新時 / 削除時 |
| N.5 インデックス | インデックス名 / 対象カラム / UNIQUE / 用途 |
| N.6 制約 | 制約名 / 種別(CHECK / UNIQUE / 複合CHECK) / 対象カラム / 条件・内容 |
| N.7 コード値・区分値 | カラム名 / 値 / 意味 |

- **桁数**: TEXT / INTEGER は空欄(`-`)、長さ制約は §N.6 制約の CHECK に集約
- **PK / FK / UNIQUE 列**: 単一は `○`、複合は `①` / `②` の番号付き
- **NULL 列**: `YES` / `NO`
- **外部キー名**: SQLite は名前なしのため `(暗黙)` 固定
- **更新時 / 削除時**: `NO ACTION` / `CASCADE` / `SET NULL` / `RESTRICT`
- **部分インデックス**: 対象カラム欄末尾に `WHERE …` 併記
- **コード値の値表記**: バッククォート(SC-007 通過のキー)。状態遷移詳細は §4.5.x へリンク

## 2. テーブル一覧

### 2.1 一覧

| # | テーブル名 | 論理名 | 概要 | 主キー | `contract_owner_user_id` | 主管 FR | 保持期間 | 保持起点 | retention_class |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `users` | ユーザーマスタ(管理者専用) | 管理者(オーナー / プロジェクト管理者 / メンバー)の認証情報を一元保持(`contract_owners` 行存在でオーナー判定)。エンドユーザーは含まない | `id` | (`users` 単独では持たず、`contract_owners.user_id` / `project_users.contract_owner_user_id` で表現)| FR-001, FR-015 | 永久(契約存続中)/ 退会後は §4.6 削除ポリシー | 作成日時 | `general` |
| 2 | `contract_owners` | 契約者ユーザー | オーナー専有属性(`contract_status` / `data_deletion_mode`)。行存在 = オーナー判定 | `user_id` | (本テーブル自身が契約境界、`user_id` = `contract_owner_user_id`)| FR-015a, FR-333 | 永久(契約存続中)/ 退会後は §4.6 削除ポリシー | 作成日時 | `billing` |
| 3 | `project_users` | プロジェクトユーザー割当 + プロジェクト別ロール | オーナー / メンバーのプロジェクトアクセスとロール(`admin` / `member`)| `(user_id, project_id)` | あり(`contract_owner_user_id` で契約境界冗長保持)| FR-015, FR-030-035 | 紐づくユーザーに従属 | - | `general` |
| 4 | `sessions` | セッション | セッション管理(複数デバイス対応) | `id` | (`users` 経由) | FR-022, FR-332 | 期限切れ後 90 日 | 最終アクティビティ | `general` |
| 5 | `access_tokens` | アクセストークン | 招待 / パスワード再設定等のトークン | `id` | (`users` 経由) | FR-004, FR-008 | 使用後 30 日 | 発行日時 | `general` |
| 6 | `projects` | プロジェクト | FAQ プロジェクト / ウィジェット設定 | `id` | あり | FR-030-035 | 永久(プロジェクト存続中) | 作成日時 | `general` |
| 7 | `project_legacy_keys` | レガシー API キー | ウィジェット鍵ローテーション 24 時間猶予 | `id` | (`projects` 経由) | - | 24 時間 | ローテーション日時 | `general` |
| 8 | `allowed_domains` | 許可ドメイン | ウィジェット埋め込み許可ドメイン | `id` | (`projects` 経由) | FR-150-156 | プロジェクトに従属 | - | `general` |
| 9 | `faqs` | FAQ | FAQ 本体 | `id` | あり | FR-040-048, FR-322 | 永久(FAQ 存続中) | 作成日時 | `general` |
| 10 | `faq_revisions` | FAQ 改訂履歴 | 全文スナップショット 50 件保持 | `id` | (`faqs` 経由) | FR-322 | 永久(版ごと) | 改訂日時 | `general` |
| 11 | `faq_search_fts` | FAQ 全文検索 | FTS5 仮想テーブル(trigram) | (rowid) | (`faqs` 連動) | FR-300 | `faqs` に従属 | - | - |
| 12 | `question_logs` | 質問ログ | エンドユーザーからの質問 / AI 推論 | `id` | あり | FR-003, NFR-601 | 730 日(自動匿名化)| 投稿日時 | `general` |
| 13 | `question_log_faq_refs` | 参照 FAQ | 質問と FAQ の M:N 関係 | `id` | (経由) | - | `question_logs` に従属 | - | `general` |
| 14 | `inquiries` | 未解決質問 | FAQ 登録前の未解決質問 | `id` | あり | FR-070〜077 | 730 日 | 作成日時 | `general` |
| 15 | `notification_logs` | 通知ログ | メール通知の送信履歴 | `id` | あり | NFR-506, NFR-319 | 1 年 | 送信日時 | `general` |
| 16 | `ai_threshold_persistent_cache` | AI しきい値キャッシュ | 3 階層しきい値の永続キャッシュ | `id` | あり(scope=`owner` 時)| FR-341 | 30 日 | 更新日時 | `general` |
| 17 | `email_suppression_list` | メールサプレスリスト | バウンス・苦情の全契約横断管理 | `id` | (全契約横断)| FR-149b | 永久 | サプレス日時 | `general` |
| 18 | `audit_logs` | 監査ログ(メイン側、API 操作ログ専用)| メイン管理者向けの API 操作ログ。**ハッシュチェーン正本は運営者側 `audit_logs`**(IF #8 で片方向同期)| `id` | あり(`contract_owner_user_id` カラム)| NFR-602a/b/d | retention_class 別(1 / 7 / 5 年)| 操作日時 | `general` / `billing` / `operator_high_priv` |
| 19 | `error_logs` | エラーログ | サーバーエラー記録 | `id` | あり(可)| NFR-704 | 180 日 | 発生日時 | `general` |
| 20 | `billing_subscriptions` | 課金サブスクリプション | Stripe Subscription 連動 | `id` | あり | FR-148 | 永久(契約存続中)| 作成日時 | `billing` |
| 21 | `billing_invoices` | 請求書 | 月次請求書(電子帳簿保存法)| `id` | あり | FR-148, NFR-602b | 7 年 | 発行日時 | `billing` |
| 22 | `usage_metering` | 利用量計測(プロジェクト単位)| 質問数 / FAQ 件数を **プロジェクト単位** で計測(請求は owner で SUM 集計)| `id` | あり | FR-148 | 7 年 | 計測日時 | `billing` |
| 23 | `owner_quota_overrides` | 契約別レート上書き | 運営者によるレート制限上書き(SCR-093)。**月次上限件数は `project_quota_limits`(#24)へ分離**。本テーブルはレート種別(`*_per_*`)専用 | `id` | あり | IF #5 | 5 年 | 設定日時 | `operator_high_priv` |
| 24 | `project_quota_limits` | プロジェクト別利用設定 | 質問数の月次上限件数・無料枠・アラートと、運営者が管理するFAQ件数の無料枠をプロジェクト単位で保持 | `id` | あり | FR-121 | 永久(プロジェクト存続中)| 設定日時 | `general` |
| 25 | `service_announcements` | お知らせ(Control Plane) | 運営者発信のお知らせ本体 | `id` | (全契約横断 or 限定)| - | 5 年 | 配信日時 | `operator_high_priv` |
| 26 | `announcement_audiences` | お知らせ配信対象 M:N | お知らせの配信先 owner 限定指定(行 0 件 = 全契約配信)| `(announcement_id, contract_owner_user_id)` | (限定)| - | 親お知らせに従属 | - | `operator_high_priv` |
| 27 | `announcement_recipients` | お知らせ受信者 | お知らせ配信先・配信集計・監査 | `id` | あり | - | 5 年 | 配信日時 | `general` |
| 28 | `inbox_messages` | 受信箱(Tenant Plane)| 利用者のお知らせ・通知の既読管理 | `id` | あり | FR-182-192 | 1 年 | 既読日時 | `general` |
| 29 | `withdrawal_requests` | 退会申請 | 退会申請レコード(30 日猶予)| `id` | あり | FR-189 | 7 年 | 申請日時 | `billing` |
| 30 | `terms_versions` | 規約版数 | 規約 / プライバシーポリシーの版 | `version` | (全契約横断)| - | 永久 | 制定日時 | `general` |
| 31 | `terms_agreements` | 規約同意 | 利用者ごとの同意履歴 | `id` | (`users` 経由)| - | 7 年 | 同意日時 | `billing` |

### 2.2 retention_class 値の正本定義(`audit_logs.retention_class`)

| 値 | 保持期間 | 用途 | 例 |
|---|---|---|---|
| `general` | **1 年**(NFR-602a)| 業務監査 | 通常の操作 / FAQ 編集 / ログイン |
| `billing` | **7 年**(NFR-602b、電子帳簿保存法)| 課金・法定保管 | 請求 / 支払 / サブスク / 上限件数 / `owner.suspend` prefix |
| `operator_high_priv` | **5 年**(NFR-602d、SOX 類似)| 運営者高権限操作 | 復元 / 契約停止 / 物理削除 / AI 変更 / レート上書き / 鍵ローテーション |

詳細は [09_セキュリティ設計.md §7](09_セキュリティ設計.md)(運営者側 §7 が正本)を参照。

### 2.3 主要関係性

| 関係 | 型 | 説明 |
|---|---|---|
| ユーザー ↔ 契約者 | 1:0..1 | `users` 1 行に対し `contract_owners` は 0 行(メンバー)or 1 行(オーナー)。`contract_owners` 行存在 = オーナー判定 |
| 契約者 ↔ プロジェクト | 1:N | 契約削除時にプロジェクトも削除対象(`projects.contract_owner_user_id` REFERENCES `contract_owners(user_id)`) |
| プロジェクト ↔ FAQ | 1:N | FAQ はプロジェクトに帰属 |
| プロジェクト ↔ 未解決質問 | 1:N | - |
| 未解決質問 ↔ FAQ | 0..1:N | `faqs.source_unresolved_question_id` で参照可 |
| お知らせ ↔ 受信箱 | 1:N | `service_announcements` → `announcement_recipients` → `inbox_messages` の fan-out |

### 2.4 ユーザー権限保持方式(3 表モデル)

採用方式は **3 表分離型**(`users` + `contract_owners` + `project_users`):

- `users`: 全ロール共通の認証主体(認証情報 + PII)。「オーナーかどうか」は `users` 単独では判定せず、`contract_owners` 行存在で判定する
- `contract_owners`: オーナー専有属性(`contract_status` / `data_deletion_mode`)を分離。`user_id PRIMARY KEY` により 1 契約 = 1 オーナー固定が DB 制約で担保される
- `project_users`: プロジェクト単位ロール(`admin` / `member`)。`(user_id, project_id)` 複合 PK + `contract_owner_user_id` 冗長保持(認可境界クエリ高速化と意図明示) + `granted_at` / `granted_by` 保持で行レベル監査

**オーナーも `project_users` に行を持つ**(プロジェクト作成時に `project_users(user_id=オーナー, project_id=新PJ, contract_owner_user_id=オーナー, role='admin', valid=1)` を自動 INSERT。FR-030a)。認可フロー先頭の `isOwner=true` bypass(`contract_owners` 行存在で判定)は維持し、`project_users` 行は critical 通知宛先解決と画面表示(プロジェクト管理者一覧)の事実情報として参照する。個別権限フラグ方式は採用しない。

**最後の管理者保護判定**(`E-AUTHZ-LAST-ADMIN-PROTECTED`): オーナー由来の `project_users` 行を判定対象から除外する。具体的には `pu.user_id NOT IN (SELECT user_id FROM contract_owners WHERE valid=1)` で「メンバー由来 admin 行」のみを抽出して件数判定する(オーナーは常に admin 行を持つため判定対象外)。

**`valid` カラムによる論理削除を全削除対象テーブルに適用する**(対象テーブルは §4.6 削除モード表を正本)。`valid INTEGER NOT NULL DEFAULT 1 CHECK (valid IN (0,1))` を持つテーブルでは、論理削除は `UPDATE … SET valid=0` で表現する。論理削除データは `updated_at` 基準で 90 日経過後に物理削除バッチで物理削除する([DD13_バッチ・非同期処理.md](../03_詳細設計/DD13_バッチ・非同期処理.md) を正本)。`deleted_at` カラムは持たず、論理削除日時は既存 `updated_at`(`valid` が 0 に更新された時刻)で代用する。例外テーブル: `audit_logs`(API 操作ログ専用、`created_at + retention 期間` で物理 DELETE)、`sessions`(`revoked_at` で代用)、法定保管 7 年テーブル(`billing_invoices` / `usage_metering` / `withdrawal_requests` / `terms_versions` / `terms_agreements`)、全契約横断テーブル(`email_suppression_list`)、ログ系(`notification_logs` / `error_logs` / `inbox_messages`)、トークン / 履歴系(`access_tokens` / `faq_revisions` / `faq_search_fts` / `question_log_faq_refs`)、M:N 関係系(`announcement_audiences`)。

## 3. テーブル詳細

### 3.1 `users`

#### 3.1.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `users` |
| 論理名 | ユーザーマスタ(管理者専用) |
| 説明 | **管理者(オーナー / プロジェクト管理者 / メンバー)** の認証情報(メール / パスワード / 氏名 / セッション状態など)を一元保持。MVPのウィジェット利用者はアカウント行を作成しない |
| 主管 FR | FR-001, FR-015 |
| 保持期間 / 保持起点 | 永久(契約存続中)/ 作成日時 |
| `contract_owner_user_id` | なし(`contract_owners.user_id` / `project_users.contract_owner_user_id` で表現) |
| `retention_class` | `general` |
| 設計判断 | 本テーブルの行はすべて管理コンソール利用者。MVPのウィジェット利用者は本テーブルにも別テーブルにも永続アカウントを作成しない。`role` カラムは v1.13 で撤廃。「オーナーかどうか」は本テーブル単独では判定せず、**`contract_owners` 行存在で判定** する(`is_owner` カラムも `owner_account_id` 自己参照も持たない)。「契約所属」は `contract_owners.user_id = users.id`(本人がオーナー)または `project_users.user_id = users.id` 経由で取得する `contract_owner_user_id` で表現する。論理削除は `valid` カラムで表現(`valid=0` = 論理削除済、`valid=1` = 有効)。`deleted_at` 列は持たず、論理削除日時は `updated_at` で代用する。`email_key_version` は鍵ローテーション dual-decrypt 期間中の鍵バージョン参照用。**契約内メール一意 UNIQUE** は `users` 単独では表現不可のため、アプリ層トランザクション(同一 `contract_owner_user_id` 配下の `project_users.user_id` の `users.email_hmac` セット + `contract_owners` 行の `users.email_hmac` での重複検査)で担保する |

#### 3.1.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID 主キー |
| 2 | メール(暗号化) | `email_encrypted` | TEXT | - | NO |  |  |  |  | AES-256-GCM、オーナー派生鍵 |
| 3 | メール HMAC | `email_hmac` | TEXT | - | NO |  |  |  |  | 検索用 HMAC-SHA256 |
| 4 | 鍵バージョン | `email_key_version` | INTEGER | - | NO |  |  |  | `1` | dual-decrypt 期間中の鍵バージョン |
| 5 | パスワードハッシュ | `password_hash` | TEXT | - | YES |  |  |  |  | bcrypt / Argon2id |
| 6 | 氏名 | `name` | TEXT | - | YES |  |  |  |  | 表示名。**招待時は `NULL`** で作成され、招待された本人が SCR-018 メンバーアカウント有効化ページで入力した時点で設定される(FR-016d / FR-016e、個人情報原則により管理者は他人の氏名を入力できない)。本人による変更は SCR-018 / SCR-017 個人設定で可能 |
| 7 | 状態 | `status` | TEXT | - | NO |  |  |  | `'pending_verification'` | `pending_verification` / `pending_activation` / `active` |
| 8 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ(0=削除済 / 1=有効) |
| 9 | 最終ログイン日時 | `last_login_at` | TEXT | - | YES |  |  |  |  | ISO 8601 UTC |
| 10 | メール確認日時 | `email_verified_at` | TEXT | - | YES |  |  |  |  | ISO 8601 UTC |
| 11 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 12 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC(論理削除日時を代用) |

#### 3.1.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.1.4 外部キー

(なし)

#### 3.1.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_users_email_hmac` | `email_hmac` |  | メール検索 |
| 2 | `idx_users_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出(物理削除バッチ) |

#### 3.1.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `status` | `status IN ('pending_verification','pending_activation','active')` |
| 2 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.1.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `status` | `pending_verification` | メール未確認 |
| `status` | `pending_activation` | 招待中(SCR-018 メンバーアカウント有効化ページで氏名 + 初回パスワード + 規約同意 未完了)。`users.name=NULL` で作成され、SCR-018 完了時に `users.name` 設定 + `status='active'` に遷移する(FR-016e)|
| `status` | `active` | 有効 |
| `valid` | `1` | 有効 |
| `valid` | `0` | 論理削除済み(論理削除日時は `updated_at` で判別。`updated_at < now() - 90d AND valid=0` の行は物理削除バッチで物理削除される)|

> ウィジェット利用者は本テーブルに含まれず、永続アカウントを作成しない。

---

### 3.2 `contract_owners`

#### 3.2.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `contract_owners` |
| 論理名 | 契約者ユーザー(オーナー専有属性)|
| 説明 | オーナー専有属性(`contract_status` / `data_deletion_mode`)を分離。**行存在 = オーナー判定**(`is_owner` カラム不要)|
| 主管 FR | FR-015a, FR-333 |
| 保持期間 / 保持起点 | 永久(契約存続中)/ 作成日時 |
| `contract_owner_user_id` | 本テーブル自身が契約境界(`user_id` = `contract_owner_user_id`) |
| `retention_class` | `billing` |
| 設計判断 | `user_id PRIMARY KEY` により 1 契約 = 1 オーナー固定が DB 制約で担保される。「契約を持たないオーナー」のような不整合状態は表現不能。`contract_status='deleted'` ⇔ `valid=0` の同期は複合 CHECK で強制 |

#### 3.2.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ユーザー ID | `user_id` | TEXT | - | NO | ○ | ○ |  |  | `users(id)` への 1:1 参照 |
| 2 | 契約状態 | `contract_status` | TEXT | - | NO |  |  |  | `'active'` | `active` / `suspended` / `deleted_pending` / `deleted`(共有概念正本)|
| 3 | データ削除モード | `data_deletion_mode` | TEXT | - | NO |  |  |  | `'physical'` | `physical` / `anonymize` |
| 4 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 5 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.2.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `user_id` |

#### 3.2.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `user_id` | `users` | `id` | NO ACTION | CASCADE |

#### 3.2.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_contract_owners_status` | `contract_status` |  | 状態別オーナー抽出 |
| 2 | `idx_contract_owners_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.2.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `contract_status` | `contract_status IN ('active','suspended','deleted_pending','deleted')` |
| 2 | (暗黙) | CHECK | `data_deletion_mode` | `data_deletion_mode IN ('physical','anonymize')` |
| 3 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |
| 4 | (暗黙) | 複合CHECK | `contract_status × valid` | `contract_status='deleted'` のとき必ず `valid=0`(両者は同時セット)。`(contract_status <> 'deleted' AND valid = 1) OR (contract_status = 'deleted' AND valid = 0)` |

#### 3.2.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `contract_status` | `active` | 有効(**共有概念正本**)|
| `contract_status` | `suspended` | サスペンション中 |
| `contract_status` | `deleted_pending` | 退会申請中(30 日猶予)|
| `contract_status` | `deleted` | 削除済み(オーナー退会完了。`valid=0` も同時セット = CHECK 制約で強制)|
| `data_deletion_mode` | `physical` | 物理削除(初期値)|
| `data_deletion_mode` | `anonymize` | 匿名化 |

詳細状態遷移は [10_課金・請求設計.md §4](10_課金・請求設計.md) を正本とする。`contract_status × valid` の整合は §4.5.5 参照。

---

### 3.3 `project_users`

#### 3.3.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `project_users` |
| 論理名 | プロジェクトユーザー割当 + プロジェクト別ロール |
| 説明 | オーナー / プロジェクト管理者 / メンバーの全員に対し、プロジェクトごとに `admin`(プロジェクト管理者) / `member`(メンバー)のロールを付与する。**オーナーも本テーブルに行を持つ**(プロジェクト作成時に `(user_id=オーナー, project_id=新PJ, contract_owner_user_id=オーナー, role='admin', valid=1)` 行を自動 INSERT)|
| 主管 FR | FR-015, FR-030-035 |
| 保持期間 / 保持起点 | 紐づくユーザーに従属 / - |
| `contract_owner_user_id` | あり(契約境界クエリ高速化と意図明示) |
| `retention_class` | `general` |
| 設計判断 | 認可フロー先頭の `isOwner=true` bypass(`contract_owners` 行存在で判定)は維持し、本テーブルは critical 通知宛先解決と画面表示に使う。**割当ゼロ状態の発生条件**: メンバー削除はユーザー論理削除(`UPDATE users SET valid=0`)で行い、`project_users` 行のみの単独削除(離脱)操作は提供しない。論理削除に伴い `project_users` 行も `valid=0` に同時 UPDATE する。**PK と再招待**: `(user_id, project_id)` の物理 PK 維持。同じユーザー × 同じプロジェクトの再招待は既存行の `valid` を `0 → 1` に切り替える UPSERT(`INSERT … ON CONFLICT(user_id, project_id) DO UPDATE SET valid=1, role=?, granted_at=now(), granted_by=?, updated_at=now()`)で表現。これにより同一ペアで `valid=1` 行は最大 1 件、部分 UNIQUE インデックスは不要 |

#### 3.3.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ユーザー ID | `user_id` | TEXT | - | NO | ① | ○ |  |  | 複合 PK / `users(id)` 参照 |
| 2 | プロジェクト ID | `project_id` | TEXT | - | NO | ② | ○ |  |  | 複合 PK / `projects(id)` 参照 |
| 3 | 契約境界 owner | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | 認可境界クエリ高速化(`projects.contract_owner_user_id` と必ず一致、アプリ層担保) |
| 4 | ロール | `role` | TEXT | - | NO |  |  |  |  | `admin` / `member` |
| 5 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 6 | 付与日時 | `granted_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 7 | 付与者 | `granted_by` | TEXT | - | YES |  | ○ |  |  | `users(id)` 参照(招待者削除でも整合維持) |
| 8 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.3.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `(user_id, project_id)`(複合) |

#### 3.3.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `user_id` | `users` | `id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |
| 3 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 4 | (暗黙) | `granted_by` | `users` | `id` | NO ACTION | SET NULL |

#### 3.3.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_project_users_project` | `project_id` |  | プロジェクト割当一覧 |
| 2 | `idx_project_users_owner` | `contract_owner_user_id` |  | 契約境界クエリ |
| 3 | `idx_project_users_role_admin` | `user_id` WHERE `role = 'admin' AND valid = 1` |  | プロジェクト管理者の高速検索(critical 通知宛先解決) |
| 4 | `idx_project_users_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.3.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `role` | `role IN ('admin','member')` |
| 2 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.3.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `role` | `admin` | プロジェクト管理者(該当プロジェクトの FAQ 管理 / 要対応質問の状況管理 / ログ参照 / メンバー管理 = 招待・プロジェクト割当解除・ロール変更)|
| `role` | `member` | メンバー(該当プロジェクトの FAQ 管理 / 要対応質問の状況管理 / ログ参照)|

詳細は [04_権限設計.md §3](04_権限設計.md) を正本とする。

---

### 3.4 `sessions`

#### 3.4.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `sessions` |
| 論理名 | セッション |
| 説明 | セッション管理(複数デバイス対応)。`revoked_at` で論理失効を表現する |
| 主管 FR | FR-022, FR-332 |
| 保持期間 / 保持起点 | 期限切れ後 90 日 / 最終アクティビティ |
| `contract_owner_user_id` | なし(`users` 経由) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(`revoked_at IS NOT NULL` で論理失効を表現)。物理削除は `expires_at + 90 日` 経過後にバッチで実施 |

#### 3.4.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | ユーザー ID | `user_id` | TEXT | - | NO |  | ○ |  |  | `users(id)` 参照 |
| 3 | IP アドレス | `ip_address` | TEXT | - | NO |  |  |  |  | クライアント IP |
| 4 | ユーザーエージェント | `user_agent` | TEXT | - | YES |  |  |  |  | ブラウザ UA |
| 5 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 最終アクセス日時 | `last_accessed_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 7 | 有効期限 | `expires_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 8 | 失効日時 | `revoked_at` | TEXT | - | YES |  |  |  |  | 失効時のみセット |

#### 3.4.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.4.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `user_id` | `users` | `id` | NO ACTION | CASCADE |

#### 3.4.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_sessions_user` | `user_id` WHERE `revoked_at IS NULL` |  | ユーザーの有効セッション抽出 |
| 2 | `idx_sessions_expires` | `expires_at` WHERE `revoked_at IS NULL` |  | 期限切れ抽出 |

#### 3.4.6 制約

(なし。NOT NULL は §3.4.2 で表現済)

#### 3.4.7 コード値・区分値

(なし)

---

### 3.5 `access_tokens`

#### 3.5.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `access_tokens` |
| 論理名 | アクセストークン |
| 説明 | 招待 / パスワード再設定 / メール確認 / プロジェクト連絡先確認のトークン |
| 主管 FR | FR-004, FR-008 |
| 保持期間 / 保持起点 | 使用後 30 日 / 発行日時 |
| `contract_owner_user_id` | なし(`users` 経由) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(`used_at` で代用)。トークン本体は HMAC ハッシュで保存(平文非保持)。常に物理削除(匿名化対象外)。主体は管理コンソール利用者(`user_id`)のみ |

#### 3.5.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | ユーザー ID | `user_id` | TEXT | - | NO |  | ○ |  |  | `users(id)` 参照 |
| 3 | トークンハッシュ | `token_hash` | TEXT | - | NO |  |  | ○ |  | HMAC-SHA256 |
| 4 | 用途 | `purpose` | TEXT | - | NO |  |  |  |  | `email_verify` / `password_reset` / `activation` / `contact_verify` |
| 5 | メタ情報 | `meta` | TEXT | - | YES |  |  |  |  | JSON 補助情報。`purpose='activation'` の場合は `{"invitedProjectId": "...", "invitedRole": "admin\|member"}` を保持(SCR-018 有効化時に当該プロジェクトの予約行を特定、FR-016e)。`purpose='contact_verify'` の場合は `{"projectId": "..."}` を保持(SCR-019 着地時に `projects.contact_email_verified_at` を更新する対象プロジェクトを特定、FR-033a)|
| 6 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 7 | 有効期限 | `expires_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 8 | 使用日時 | `used_at` | TEXT | - | YES |  |  |  |  | 使用済みのみセット |

#### 3.5.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.5.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `user_id` | `users` | `id` | NO ACTION | CASCADE |

#### 3.5.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_access_tokens_hash` | `token_hash` | ○ | トークン検索 |
| 2 | `idx_access_tokens_expires` | `expires_at` WHERE `used_at IS NULL` |  | 未使用トークン期限切れ抽出 |

#### 3.5.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `purpose` | `purpose IN ('email_verify','password_reset','activation','contact_verify')` |

#### 3.5.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `purpose` | `email_verify` | メール確認 |
| `purpose` | `password_reset` | パスワード再設定 |
| `purpose` | `activation` | メンバーアカウント有効化(招待受諾。SCR-018 で氏名 + 初回パスワード + 規約同意。`meta` に `invitedProjectId` / `invitedRole` を JSON 保持)|
| `purpose` | `contact_verify` | プロジェクト連絡先メール確認(SCR-019 着地ページで `projects.contact_email_verified_at` セット。TTL 24 時間、`meta` に `projectId` を JSON 保持)|

---

### 3.6 `projects`

#### 3.6.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `projects` |
| 論理名 | プロジェクト |
| 説明 | FAQ プロジェクト / ウィジェット設定 |
| 主管 FR | FR-030-035 |
| 保持期間 / 保持起点 | 永久(プロジェクト存続中) / 作成日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | `status='deleted'` ⇔ `valid=0` の同期は複合 CHECK で強制。`widget_public_key` はウィジェット埋め込み時の鍵で UNIQUE。ローテーション時は `project_legacy_keys` に旧鍵を 24 時間保持 |

#### 3.6.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | プロジェクト名 | `name` | TEXT | - | NO |  |  |  |  | 1〜100 文字 |
| 4 | 説明 | `description` | TEXT | - | YES |  |  |  |  | 任意 |
| 5 | 状態 | `status` | TEXT | - | NO |  |  |  | `'active'` | `active` / `deleted` |
| 6 | ウィジェット公開鍵 | `widget_public_key` | TEXT | - | NO |  |  | ○ |  | 埋め込み用。無期限(失効は再発行でのみ。FR-194)|
| 7 | 連絡先メール | `contact_email` | TEXT | - | YES |  |  |  |  | プロジェクト連絡先 |
| 8 | 連絡先確認日時 | `contact_email_verified_at` | TEXT | - | YES |  |  |  |  | 確認済みのみセット |
| 9 | 設定 JSON | `settings` | TEXT | - | NO |  |  |  | `'{}'` | ウィジェット設定 JSON |
| 10 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 11 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 12 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.6.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.6.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |

#### 3.6.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_projects_owner_status` | `(contract_owner_user_id, status)` |  | オーナー別プロジェクト一覧 |
| 2 | `idx_projects_widget_key` | `widget_public_key` |  | ウィジェット鍵検索 |
| 3 | `idx_projects_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.6.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `name` | `length(name) BETWEEN 1 AND 100` |
| 2 | (暗黙) | CHECK | `status` | `status IN ('active','deleted')` |
| 3 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |
| 4 | (暗黙) | 複合CHECK | `contact_email × contact_email_verified_at` | `contact_email_verified_at IS NULL OR contact_email IS NOT NULL` |
| 5 | (暗黙) | 複合CHECK | `status × valid` | `(status = 'active' AND valid = 1) OR (status = 'deleted' AND valid = 0)` |

#### 3.6.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `status` | `active` | 有効 |
| `status` | `deleted` | 論理削除(`valid=0` と同時セット)|

---

### 3.7 `project_legacy_keys`

#### 3.7.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `project_legacy_keys` |
| 論理名 | レガシー API キー |
| 説明 | ウィジェット公開鍵ローテーション時の旧鍵を 24 時間猶予で保持 |
| 主管 FR | - |
| 保持期間 / 保持起点 | 24 時間 / ローテーション日時 |
| `contract_owner_user_id` | なし(`projects` 経由) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(`grace_until` 経過で物理削除対象)|

#### 3.7.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ |  |  | `projects(id)` 参照 |
| 3 | 公開鍵 | `public_key` | TEXT | - | NO |  |  |  |  | 旧 widget_public_key |
| 4 | 猶予期限 | `grace_until` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 5 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  | `datetime('now')` | ISO 8601 UTC |

#### 3.7.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.7.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |

#### 3.7.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_legacy_keys_key` | `public_key` |  | 旧鍵検索 |

#### 3.7.6 制約

(なし)

#### 3.7.7 コード値・区分値

(なし)

---

### 3.8 `allowed_domains`

#### 3.8.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `allowed_domains` |
| 論理名 | 許可ドメイン |
| 説明 | ウィジェット埋め込み許可ドメイン |
| 主管 FR | FR-150-156 |
| 保持期間 / 保持起点 | プロジェクトに従属 / - |
| `contract_owner_user_id` | なし(`projects` 経由) |
| `retention_class` | `general` |
| 設計判断 | 同一プロジェクト内 `valid=1` 行のみ UNIQUE(再追加可能にするため部分 UNIQUE)|

#### 3.8.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ |  |  | `projects(id)` 参照 |
| 3 | ドメイン | `domain` | TEXT | - | NO |  |  |  |  | 許可ドメイン |
| 4 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 5 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  | `datetime('now')` | ISO 8601 UTC |

#### 3.8.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.8.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |

#### 3.8.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_allowed_domains_project_domain` | `(project_id, domain)` WHERE `valid = 1` | ○ | プロジェクト内ドメイン一意(部分 UNIQUE) |
| 2 | `idx_allowed_domains_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.8.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.8.7 コード値・区分値

(なし)

---

### 3.9 `faqs`

#### 3.9.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `faqs` |
| 論理名 | FAQ |
| 説明 | FAQ 本体 |
| 主管 FR | FR-040-048, FR-322 |
| 保持期間 / 保持起点 | 永久(FAQ 存続中) / 作成日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | `status='deleted'` ⇔ `valid=0`(プロジェクト削除に伴うカスケード論理削除では `status` を変えずに `valid=0` のみセットすることがある。UI / API では `valid=0` 行を常に除外)。復元は **運営者のみ**(IF #4)|

#### 3.10.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ |  |  | `projects(id)` 参照 |
| 4 | タイトル | `title` | TEXT | - | NO |  |  |  |  | 1〜200 文字 |
| 5 | 本文 | `body` | TEXT | - | NO |  |  |  |  | 1〜10000 文字 |
| 6 | カテゴリ | `category` | TEXT | - | YES |  |  |  |  | 任意 |
| 7 | 状態 | `status` | TEXT | - | NO |  |  |  | `'draft'` | `draft` / `published` / `hidden` / `deleted` |
| 8 | バージョン | `version` | INTEGER | - | NO |  |  |  | `1` | 改訂版数 |
| 9 | タグ | `tags` | TEXT | - | YES |  |  |  |  | JSON 配列(最大 10 要素、各 30 文字以下、API 層でバリデーション)|
| 10 | 元未解決質問 ID | `source_unresolved_question_id` | TEXT | - | YES |  | ○ |  |  | `inquiries(id)` 参照 |
| 11 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 12 | 作成者 | `created_by` | TEXT | - | YES |  | ○ |  |  | `users(id)` 参照(削除でも整合維持) |
| 13 | 更新者 | `updated_by` | TEXT | - | YES |  | ○ |  |  | `users(id)` 参照(同上) |
| 14 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 15 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.10.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.10.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |
| 3 | (暗黙) | `source_unresolved_question_id` | `inquiries` | `id` | NO ACTION | SET NULL |
| 4 | (暗黙) | `created_by` | `users` | `id` | NO ACTION | SET NULL |
| 5 | (暗黙) | `updated_by` | `users` | `id` | NO ACTION | SET NULL |

#### 3.10.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_faqs_owner_project` | `(contract_owner_user_id, project_id)` |  | 契約境界 + プロジェクト |
| 2 | `idx_faqs_project_status` | `(project_id, status)` |  | プロジェクト × 状態 |
| 3 | `idx_faqs_source_inquiry` | `source_unresolved_question_id` |  | 元未解決質問逆引き |
| 4 | `idx_faqs_category` | `(project_id, category)` WHERE `category IS NOT NULL` |  | カテゴリ別 |
| 5 | `idx_faqs_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.10.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `title` | `length(title) BETWEEN 1 AND 200` |
| 2 | (暗黙) | CHECK | `body` | `length(body) BETWEEN 1 AND 10000` |
| 3 | (暗黙) | CHECK | `status` | `status IN ('draft','published','hidden','deleted')` |
| 4 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.10.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `status` | `draft` | 下書き |
| `status` | `published` | 公開中 |
| `status` | `hidden` | 非公開 |
| `status` | `deleted` | 論理削除(`valid=0` と同時セット。復元は **運営者のみ** = IF #4)|

状態遷移詳細は §4.5.1 参照。

---

### 3.10 `faq_revisions`

#### 3.10.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `faq_revisions` |
| 論理名 | FAQ 改訂履歴 |
| 説明 | 全文スナップショット 50 件保持 |
| 主管 FR | FR-322 |
| 保持期間 / 保持起点 | 永久(版ごと)/ 改訂日時 |
| `contract_owner_user_id` | なし(`faqs` 経由) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(履歴系、論理削除概念外)。`faqs` 物理削除時に ON DELETE CASCADE で連鎖物理削除 |

#### 3.10.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | FAQ ID | `faq_id` | TEXT | - | NO |  | ○ |  |  | `faqs(id)` 参照 |
| 3 | バージョン | `version` | INTEGER | - | NO |  |  |  |  | 版数 |
| 4 | タイトル | `title` | TEXT | - | NO |  |  |  |  | スナップショット |
| 5 | 本文 | `body` | TEXT | - | NO |  |  |  |  | スナップショット |
| 6 | カテゴリ | `category` | TEXT | - | YES |  |  |  |  | スナップショット |
| 7 | タグ | `tags` | TEXT | - | YES |  |  |  |  | スナップショット JSON |
| 8 | 状態スナップショット | `status_snapshot` | TEXT | - | NO |  |  |  |  | `draft` / `published` / `hidden` / `deleted` |
| 9 | 由来 | `source` | TEXT | - | NO |  |  |  | `'manual'` | `manual` / `import` |
| 10 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 11 | 作成者 | `created_by` | TEXT | - | NO |  | ○ |  |  | `users(id)` 参照 |

#### 3.10.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.10.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `faq_id` | `faqs` | `id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `created_by` | `users` | `id` | NO ACTION | NO ACTION |

#### 3.10.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_faq_revisions_faq` | `(faq_id, version DESC)` |  | 版数降順抽出 |
| 2 | `idx_faq_revisions_source` | `source` |  | 由来別集計 |

#### 3.10.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `status_snapshot` | `status_snapshot IN ('draft','published','hidden','deleted')` |
| 2 | (暗黙) | CHECK | `source` | `source IN ('manual','import')` |

#### 3.10.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `source` | `manual` | 手動編集 |
| `source` | `import` | インポートによる作成 |

---

### 3.11 `faq_search_fts`

#### 3.11.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `faq_search_fts` |
| 論理名 | FAQ 全文検索(FTS5 仮想テーブル) |
| 説明 | FTS5 contentless 仮想テーブル(trigram トークナイザ)|
| 主管 FR | FR-300 |
| 保持期間 / 保持起点 | `faqs` に従属 / - |
| `contract_owner_user_id` | なし(`faqs` 連動) |
| `retention_class` | - |
| 設計判断 | 通常テーブルと異なる FTS5 仮想テーブル。`content='faqs'` / `content_rowid='rowid'` / `tokenize='trigram'` で構成し、`faqs` への INSERT / UPDATE / DELETE をトリガで同期する。`(project_id, status='published')` 前段フィルタは §4.9 インデックス方針参照 |

#### 3.11.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | タイトル | `title` | TEXT | - | - |  |  |  |  | FTS5 仮想列(`faqs.title` と同期)|
| 2 | 本文 | `body` | TEXT | - | - |  |  |  |  | FTS5 仮想列(`faqs.body` と同期)|
| 3 | カテゴリ | `category` | TEXT | - | - |  |  |  |  | FTS5 仮想列 |
| 4 | タグ | `tags` | TEXT | - | - |  |  |  |  | FTS5 仮想列 |

#### 3.11.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (FTS5 仮想テーブル、rowid 暗黙) |
| 対象カラム | rowid(`faqs.rowid` と一致) |

#### 3.11.4 外部キー

(なし、`content='faqs'` で論理連動)

#### 3.11.5 インデックス

(FTS5 内部インデックス、明示作成なし)

#### 3.11.6 制約・トリガ

FTS5 contentless 同期トリガ 3 個(基本設計が正本):

| No | トリガ名 | 種別 | 対象 | 条件・内容 |
|---:|---|---|---|---|
| 1 | `faq_search_fts_ai` | AFTER INSERT トリガ | `faqs` | 新規行を `faq_search_fts` に INSERT |
| 2 | `faq_search_fts_ad` | AFTER DELETE トリガ | `faqs` | `'delete'` コマンドで削除行を FTS から除去 |
| 3 | `faq_search_fts_au` | AFTER UPDATE トリガ | `faqs` | 旧行を `'delete'` コマンドで除去 → 新行を INSERT |

トリガ実装手順 / 動作確認は [DD02_プロジェクト・FAQ管理.md](../03_詳細設計/DD02_プロジェクト・FAQ管理.md) を参照。

#### 3.11.7 コード値・区分値

(なし)

---

### 3.12 `question_logs`

#### 3.12.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `question_logs` |
| 論理名 | 質問ログ |
| 説明 | エンドユーザーからの質問 / AI 推論結果を記録 |
| 主管 FR | FR-003, NFR-601 |
| 保持期間 / 保持起点 | 730 日(自動匿名化) / 投稿日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | `metering_billable` は課金確定時点のイミュータブルなスナップショット値(派生値ではない。AI モデル / しきい値の事後変更で再評価しても値が変わらないことを保証)|

#### 3.12.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ |  |  | `projects(id)` 参照 |
| 4 | 質問本文 | `user_question` | TEXT | - | NO |  |  |  |  | 1〜2000 文字 |
| 5 | AI 応答 | `ai_response` | TEXT | - | YES |  |  |  |  | AI 生成本文 |
| 6 | 解決フラグ | `is_resolved` | INTEGER | - | NO |  |  |  | `0` | 0 / 1 |
| 7 | 課金確定フラグ | `metering_billable` | INTEGER | - | NO |  |  |  | `0` | スナップショット値 |
| 8 | 信頼度スコア | `confidence_score` | REAL | - | YES |  |  |  |  | 0.0〜1.0 |
| 9 | 関連度スコア | `relevance_score` | REAL | - | YES |  |  |  |  | 0.0〜1.0 |
| 10 | AI モデル | `ai_model` | TEXT | - | YES |  |  |  |  | モデル名 |
| 11 | 入力トークン数 | `ai_token_count_input` | INTEGER | - | YES |  |  |  |  | - |
| 12 | 出力トークン数 | `ai_token_count_output` | INTEGER | - | YES |  |  |  |  | - |
| 13 | 結果種別 | `result_type` | TEXT | - | YES |  |  |  |  | `answered` / `unanswered` / `error` |
| 14 | 結果理由コード | `result_reason_code` | TEXT | - | YES |  |  |  |  | - |
| 15 | PII マスク済 | `pii_masked` | INTEGER | - | NO |  |  |  | `0` | 0 / 1 |
| 16 | セッション ID | `session_id` | TEXT | - | YES |  |  |  |  | - |
| 17 | IP アドレス | `ip_address` | TEXT | - | YES |  |  |  |  | - |
| 18 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 19 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 20 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  | `datetime('now')` | ISO 8601 UTC |

#### 3.12.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.12.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |

#### 3.12.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_qlog_owner_created` | `(contract_owner_user_id, created_at DESC)` |  | オーナー別新着 |
| 2 | `idx_qlog_project_billable_created` | `(project_id, metering_billable, created_at DESC)` |  | プロジェクト × 課金確定 |
| 3 | `idx_qlog_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.12.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `user_question` | `length(user_question) BETWEEN 1 AND 2000` |
| 2 | (暗黙) | CHECK | `result_type` | `result_type IN ('answered','unanswered','error')`(NULL 許容)|
| 3 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.12.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `result_type` | `answered` | 解答あり |
| `result_type` | `unanswered` | 該当なし |
| `result_type` | `error` | AI エラー |

---

### 3.13 `question_log_faq_refs`

#### 3.13.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `question_log_faq_refs` |
| 論理名 | 参照 FAQ(M:N) |
| 説明 | 質問と FAQ の M:N 関係(AI が参照した FAQ を記録)|
| 主管 FR | - |
| 保持期間 / 保持起点 | `question_logs` に従属 / - |
| `contract_owner_user_id` | なし(`question_logs` 経由) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(M:N 関係系)。`question_logs` / `faqs` 物理削除時に ON DELETE CASCADE で連鎖物理削除 |

#### 3.13.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 質問ログ ID | `question_log_id` | TEXT | - | NO |  | ○ | ① |  | `question_logs(id)` 参照 |
| 3 | FAQ ID | `faq_id` | TEXT | - | NO |  | ○ | ② |  | `faqs(id)` 参照 |
| 4 | 関連度スコア | `relevance_score` | REAL | - | YES |  |  |  |  | 0.0〜1.0(参照時の判定スコア)|
| 5 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.13.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.13.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `question_log_id` | `question_logs` | `id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `faq_id` | `faqs` | `id` | NO ACTION | CASCADE |

#### 3.13.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_question_log_faq_refs_pair` | `(question_log_id, faq_id)` | ○ | M:N ペア一意 |
| 2 | `idx_question_log_faq_refs_faq` | `faq_id` |  | FAQ 別逆引き |

#### 3.13.6 制約

(なし)

#### 3.13.7 コード値・区分値

(なし)

---

### 3.14 `inquiries`

#### 3.14.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `inquiries` |
| 論理名 | 未解決質問 |
| 説明 | FAQ 登録前の未解決質問 |
| 主管 FR | FR-070〜077 |
| 保持期間 / 保持起点 | 730 日 / 作成日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | 対応状況は `status` の単一カラムで保持する(2 値: `open` / `closed`)。状態変更は SCR-005 詳細画面からの手動操作のみで行い、FAQ 下書き保存・FAQ 公開は本カラムを変更しない |

#### 3.14.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ |  |  | `projects(id)` 参照 |
| 4 | 問い合わせコード | `inquiry_code` | TEXT | - | NO |  |  | ○ |  | 管理画面表示コード |
| 5 | 元質問ログ ID | `question_log_id` | TEXT | - | YES |  | ○ |  |  | `question_logs(id)` 参照 |
| 6 | 質問本文 | `user_question` | TEXT | - | NO |  |  |  |  | 質問内容 |
| 7 | 状況 | `status` | TEXT | - | NO |  |  |  | `'open'` | `open` / `closed` |
| 8 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 9 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 10 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.14.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.14.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |
| 3 | (暗黙) | `question_log_id` | `question_logs` | `id` | NO ACTION | NO ACTION |

#### 3.14.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_inquiries_contract_status_created` | `(contract_owner_user_id, status, created_at DESC)` |  | 契約境界 + 状況別新着 |
| 2 | `idx_inquiries_project_status` | `(project_id, status)` |  | プロジェクト × 状況 |
| 3 | `uq_inquiries_code` | `inquiry_code` | ○ | 問い合わせコード一意 |
| 4 | `idx_inquiries_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.14.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `status` | `status IN ('open','closed')` |
| 2 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.14.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `status` | `open` | 対応必要(初期値、または `closed` から再オープン)|
| `status` | `closed` | 終了(SCR-005 詳細画面での手動操作のみ)|

状態遷移詳細は §4.5.2 参照(共有概念正本)。

---

### 3.15 `notification_logs`

#### 3.15.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `notification_logs` |
| 論理名 | 通知ログ |
| 説明 | メール通知の送信履歴 |
| 主管 FR | NFR-506, NFR-319 |
| 保持期間 / 保持起点 | 1 年 / 送信日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(ログ系、論理削除概念外)。匿名化時は `recipient_email_hmac` → SHA-256、vars → 空 JSON。案件削除でも履歴は残す(`inquiry_id` SET NULL)|

#### 3.15.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | 案件 ID | `inquiry_id` | TEXT | - | YES |  | ○ |  |  | `inquiries(id)` 参照(削除でも履歴維持) |
| 4 | 受信者メール HMAC | `recipient_email_hmac` | TEXT | - | YES |  |  |  |  | HMAC-SHA256 |
| 5 | 通知種別 | `notification_type` | TEXT | - | NO |  |  |  |  | TPL-* テンプレ ID |
| 6 | 配信状態 | `delivery_state` | TEXT | - | NO |  |  |  |  | §3.15.7 |
| 7 | メッセージ ID | `message_id` | TEXT | - | YES |  |  |  |  | Resend Message-ID |
| 8 | 試行回数 | `attempt_count` | INTEGER | - | NO |  |  |  | `0` | 当方再送回数 |
| 9 | 送信日時 | `sent_at` | TEXT | - | YES |  |  |  |  | - |
| 10 | 配信日時 | `delivered_at` | TEXT | - | YES |  |  |  |  | Webhook 経由 |
| 11 | 失敗日時 | `failed_at` | TEXT | - | YES |  |  |  |  | - |
| 12 | 失敗理由 | `fail_reason` | TEXT | - | YES |  |  |  |  | - |
| 13 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.15.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.15.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `inquiry_id` | `inquiries` | `id` | NO ACTION | SET NULL |

#### 3.15.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_nlog_owner_created` | `(contract_owner_user_id, created_at DESC)` |  | オーナー別新着 |
| 2 | `idx_nlog_message_id` | `message_id` |  | Webhook 紐付け |
| 3 | `idx_nlog_state` | `delivery_state` |  | 状態別集計 |

#### 3.15.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `delivery_state` | `delivery_state IN ('queued','sending','sent','delivered','failed','bounced','complained','suppressed')` |

#### 3.15.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `delivery_state` | `queued` | Queue 投入済み |
| `delivery_state` | `sending` | 送信中 |
| `delivery_state` | `sent` | 送信成功 |
| `delivery_state` | `delivered` | 配信成功(Resend Webhook 経由)|
| `delivery_state` | `failed` | 失敗(当方再送対象、最大 3 回)|
| `delivery_state` | `bounced` | バウンス(ハードまたはソフト 5 連続)|
| `delivery_state` | `complained` | スパム報告 |
| `delivery_state` | `suppressed` | サプレスリスト追加済み(終了状態)|

状態遷移は §4.5.4 参照。

---

### 3.16 `ai_threshold_persistent_cache`

#### 3.16.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `ai_threshold_persistent_cache` |
| 論理名 | AI しきい値キャッシュ |
| 説明 | 3 階層しきい値(`global` / `owner` / `project`)の永続キャッシュ |
| 主管 FR | FR-341 |
| 保持期間 / 保持起点 | 30 日 / 更新日時 |
| `contract_owner_user_id` | あり(scope=`owner` または `project` 時のみ) |
| `retention_class` | `general` |
| 設計判断 | scope と owner / project の整合は複合 CHECK で強制。詳細は [02_API設計.md](02_API設計.md)(IF #6)参照 |

#### 3.16.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | YES |  | ○ |  |  | scope=`global` 時 NULL |
| 3 | プロジェクト ID | `project_id` | TEXT | - | YES |  | ○ |  |  | scope=`project` 時のみ NOT NULL |
| 4 | スコープ | `scope` | TEXT | - | NO |  |  |  |  | `global` / `owner` / `project` |
| 5 | 信頼度しきい値 | `confidence_threshold` | REAL | - | NO |  |  |  |  | 0.0〜1.0 |
| 6 | 関連度しきい値 | `relevance_threshold` | REAL | - | NO |  |  |  |  | 0.0〜1.0 |
| 7 | バージョン | `version` | INTEGER | - | NO |  |  |  |  | 配信バージョン |
| 8 | 受信日時 | `received_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 9 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.16.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.16.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |

#### 3.16.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_ai_threshold_scope` | `(scope, COALESCE(contract_owner_user_id, ''), COALESCE(project_id, ''))` | ○ | スコープ + キーで一意 |

#### 3.16.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `scope` | `scope IN ('global','owner','project')` |
| 2 | (暗黙) | CHECK | `confidence_threshold` | `confidence_threshold BETWEEN 0.0 AND 1.0` |
| 3 | (暗黙) | CHECK | `relevance_threshold` | `relevance_threshold BETWEEN 0.0 AND 1.0` |
| 4 | (暗黙) | 複合CHECK | `scope × contract_owner_user_id × project_id` | `(scope = 'global' AND contract_owner_user_id IS NULL AND project_id IS NULL) OR (scope = 'owner' AND contract_owner_user_id IS NOT NULL AND project_id IS NULL) OR (scope = 'project' AND contract_owner_user_id IS NOT NULL AND project_id IS NOT NULL)` |

#### 3.16.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `scope` | `global` | 全契約共通しきい値 |
| `scope` | `owner` | 契約単位上書き |
| `scope` | `project` | プロジェクト単位上書き |

---

### 3.17 `email_suppression_list`

#### 3.17.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `email_suppression_list` |
| 論理名 | メールサプレスリスト(全契約横断) |
| 説明 | バウンス / 苦情の全契約横断管理(R-003 共通ドメインレピュテーション保護対策) |
| 主管 FR | FR-149b |
| 保持期間 / 保持起点 | 永久 / サプレス日時 |
| `contract_owner_user_id` | なし(**全契約横断**) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(全契約横断、`released_at` で解除を表現)。[09_セキュリティ設計.md §11](09_セキュリティ設計.md) 参照 |

#### 3.17.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | メール HMAC | `email_hmac` | TEXT | - | NO |  |  | ○ |  | HMAC-SHA256 |
| 3 | 理由 | `reason` | TEXT | - | NO |  |  |  |  | §3.17.7 |
| 4 | 永久フラグ | `is_permanent` | INTEGER | - | NO |  |  |  | `1` | 0 / 1 |
| 5 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 解除日時 | `released_at` | TEXT | - | YES |  |  |  |  | 解除済みのみセット |

#### 3.17.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.17.4 外部キー

(なし、全契約横断)

#### 3.17.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_suppression_permanent` | `(is_permanent, email_hmac)` |  | 永久 / 一時別検索 |

#### 3.17.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `reason` | `reason IN ('bounced_hard','bounced_soft_5x','complained','manual')` |

#### 3.17.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `reason` | `bounced_hard` | ハードバウンス |
| `reason` | `bounced_soft_5x` | ソフトバウンス 5 連続 |
| `reason` | `complained` | スパム苦情(永久)|
| `reason` | `manual` | 手動追加 |

---

### 3.18 `audit_logs`

#### 3.18.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `audit_logs` |
| 論理名 | 監査ログ(メイン側、API 操作ログ専用) |
| 説明 | メイン管理者向けの API 操作ログ。**ハッシュチェーン正本は運営者側 `audit_logs`**(IF #8 で片方向同期) |
| 主管 FR | NFR-602a / NFR-602b / NFR-602d |
| 保持期間 / 保持起点 | retention_class 別(`general` 1y / `billing` 7y / `operator_high_priv` 5y)/ 操作日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` / `billing` / `operator_high_priv`(本テーブルのカラム値で表現)|
| 設計判断 | `valid` 持たず(API 操作ログ専用、`created_at + retention 期間` で物理 DELETE)。tombstone 方式は採らない(チェーン非対象のため)。`actor_role` の `service_operator` は運営者によるメイン API 呼出(IF 経由)を記録する用途で許容値として残す(`users` のロール概念とは独立)。`metadata` の JSON 構造例: `{"before":{...},"after":{...},"ticket_id":"OPS-1234"}`。SCR-019 操作ログ画面など、メイン管理者向けの即時参照クエリに低レイテンシで応える。MVPの操作者参照は `actor_user_id`(`users(id)`)のみとし、システム・運営者操作はNULLとする |

#### 3.18.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | YES |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | 操作者(管理者)| `actor_user_id` | TEXT | - | YES |  | ○ |  |  | `users(id)` 参照(`actor_role='admin'` 時) |
| 4 | 操作者ロール | `actor_role` | TEXT | - | YES |  |  |  |  | §3.18.7 |
| 5 | アクション | `action` | TEXT | - | NO |  |  |  |  | アクションコード |
| 6 | 対象種別 | `target_type` | TEXT | - | YES |  |  |  |  | - |
| 7 | 対象 ID | `target_id` | TEXT | - | YES |  |  |  |  | - |
| 8 | IP(マスク済) | `ip_address_masked` | TEXT | - | YES |  |  |  |  | 末尾オクテットマスク |
| 9 | ユーザーエージェント | `user_agent` | TEXT | - | YES |  |  |  |  | - |
| 10 | メタデータ | `metadata` | TEXT | - | YES |  |  |  |  | JSON 文字列(差分・補助情報)|
| 11 | 保持クラス | `retention_class` | TEXT | - | NO |  |  |  | `'general'` | `general` / `billing` / `operator_high_priv` |
| 12 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.18.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.18.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | SET NULL |
| 2 | (暗黙) | `actor_user_id` | `users` | `id` | NO ACTION | SET NULL |

#### 3.18.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_audit_owner_action_created` | `(contract_owner_user_id, action, created_at DESC)` |  | オーナー × アクション × 新着 |
| 2 | `idx_audit_retention_created` | `(retention_class, created_at)` |  | 保持期間バッチ用 |
| 3 | `idx_audit_actor_created` | `(actor_user_id, created_at DESC)` |  | 管理者操作の操作者別新着 |

#### 3.18.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `actor_role` | `actor_role IS NULL OR actor_role IN ('admin','system','service_operator')` |
| 2 | (暗黙) | CHECK | `retention_class` | `retention_class IN ('general','billing','operator_high_priv')` |

#### 3.18.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `actor_role` | `admin` | 利用者管理者 |
| `actor_role` | `system` | システム(Cron / バッチ)|
| `actor_role` | `service_operator` | 運営者(IF 経由のメイン API 呼出)|
| `retention_class` | `general` | 業務監査 1 年(NFR-602a)|
| `retention_class` | `billing` | 課金・法定保管 7 年(NFR-602b)|
| `retention_class` | `operator_high_priv` | 運営者高権限 5 年(NFR-602d)|

ハッシュチェーン列(`prev_hash` / `current_hash` / `tombstone`)は持たない。チェーン正本は運営者側。詳細は §2.2 / [09_セキュリティ設計.md §7](09_セキュリティ設計.md) 参照。

---

### 3.19 `error_logs`

#### 3.19.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `error_logs` |
| 論理名 | エラーログ |
| 説明 | サーバーエラー記録 |
| 主管 FR | NFR-704 |
| 保持期間 / 保持起点 | 180 日 / 発生日時 |
| `contract_owner_user_id` | あり(可、契約コンテキストで発生したエラー) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(ログ系)。PII / トークン / カード情報を記録禁止(FR-114、エンドユーザー入力は先頭 100 文字 + ハッシュのみ)|

#### 3.19.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | YES |  |  |  |  | (FK 制約なし、削除でも記録維持) |
| 3 | ユーザー ID | `user_id` | TEXT | - | YES |  |  |  |  | (FK 制約なし、同上) |
| 4 | URL | `url` | TEXT | - | YES |  |  |  |  | リクエスト URL |
| 5 | エラー種別 | `error_type` | TEXT | - | YES |  |  |  |  | 例外クラス名 |
| 6 | スタック | `stack` | TEXT | - | YES |  |  |  |  | スタックトレース |
| 7 | 発生日時 | `occurred_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.19.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.19.4 外部キー

(なし。`contract_owner_user_id` / `user_id` は FK 制約を持たず、参照先削除でも記録を維持)

#### 3.19.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_error_logs_owner_occurred` | `(contract_owner_user_id, occurred_at DESC)` |  | オーナー別新着 |

#### 3.19.6 制約

(なし)

#### 3.19.7 コード値・区分値

(なし)

---

### 3.20 `billing_subscriptions`

#### 3.20.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `billing_subscriptions` |
| 論理名 | 課金サブスクリプション |
| 説明 | Stripe Subscription 連動 |
| 主管 FR | FR-148 |
| 保持期間 / 保持起点 | 永久(契約存続中) / 作成日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `billing` |
| 設計判断 | **物理削除バッチ除外**: `valid` カラムを持つが、電子帳簿保存法 / 法定保管要件との整合のため、90 日後の物理削除バッチ([DD13](../03_詳細設計/DD13_バッチ・非同期処理.md))の対象外。`valid=0` 行は永久に保持し、Stripe 連動の取消履歴として参照可能にする |

#### 3.20.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | Stripe サブスク ID | `stripe_subscription_id` | TEXT | - | YES |  |  | ○ |  | Stripe ID |
| 4 | 状態 | `status` | TEXT | - | NO |  |  |  |  | §3.20.7 |
| 5 | プラン ID | `plan_id` | TEXT | - | NO |  |  |  |  | - |
| 6 | 当期開始 | `current_period_start` | TEXT | - | YES |  |  |  |  | - |
| 7 | 当期終了 | `current_period_end` | TEXT | - | YES |  |  |  |  | - |
| 8 | 解約予定日 | `cancel_at` | TEXT | - | YES |  |  |  |  | - |
| 9 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ(物理削除対象外)|
| 10 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 11 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.20.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.20.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |

#### 3.20.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_billing_subs_owner` | `contract_owner_user_id` |  | オーナー検索 |
| 2 | `idx_billing_subs_status` | `status` |  | 状態別集計 |
| 3 | `idx_billing_subs_valid` | `valid` WHERE `valid = 0` |  | 論理削除済み参照(物理削除対象外)|

#### 3.20.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `status` | `status IN ('active','past_due','canceled','unpaid','incomplete')` |
| 2 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |

#### 3.20.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `status` | `active` | 有効 |
| `status` | `past_due` | 支払遅延 |
| `status` | `canceled` | 解約済み |
| `status` | `unpaid` | 未払い |
| `status` | `incomplete` | 初回支払未完了 |

---

### 3.21 `billing_invoices`

#### 3.21.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `billing_invoices` |
| 論理名 | 請求書(電子帳簿保存法対応、7 年保持) |
| 説明 | 月次請求書 |
| 主管 FR | FR-148, NFR-602b |
| 保持期間 / 保持起点 | 7 年 / 発行日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `billing` |
| 設計判断 | `valid` 持たず(法定保管 7 年テーブル)。`(contract_owner_user_id, billing_year_month)` で月次一意 |

#### 3.21.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ | ① |  | `contract_owners(user_id)` 参照 |
| 3 | 請求年月 | `billing_year_month` | TEXT | - | NO |  |  | ② |  | YYYY-MM |
| 4 | 状態 | `status` | TEXT | - | NO |  |  |  |  | §3.21.7 |
| 5 | 合計金額 | `amount_total` | INTEGER | - | NO |  |  |  |  | 円(税込)|
| 6 | 税額 | `amount_tax` | INTEGER | - | NO |  |  |  | `0` | 円 |
| 7 | 通貨 | `currency` | TEXT | - | NO |  |  |  | `'JPY'` | ISO 4217 |
| 8 | Stripe 請求書 ID | `stripe_invoice_id` | TEXT | - | YES |  |  | ○ |  | Stripe ID |
| 9 | PDF R2 キー | `pdf_r2_key` | TEXT | - | YES |  |  |  |  | R2 オブジェクトキー |
| 10 | 発行日時 | `issued_at` | TEXT | - | YES |  |  |  |  | - |
| 11 | 支払日時 | `paid_at` | TEXT | - | YES |  |  |  |  | - |
| 12 | 返金日時 | `refunded_at` | TEXT | - | YES |  |  |  |  | - |
| 13 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.21.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.21.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | NO ACTION |

#### 3.21.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_billing_invoices_owner_month` | `(contract_owner_user_id, billing_year_month)` | ○ | 月次一意 |
| 2 | `idx_billing_invoices_status` | `status` |  | 状態別集計 |

#### 3.21.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `status` | `status IN ('draft','issued','paid','past_due','refunded','void')` |

#### 3.21.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `status` | `draft` | 下書き |
| `status` | `issued` | 発行済み |
| `status` | `paid` | 支払完了 |
| `status` | `past_due` | 支払遅延 |
| `status` | `refunded` | 返金済み |
| `status` | `void` | 無効化 |

---

### 3.22 `usage_metering`

#### 3.22.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `usage_metering` |
| 論理名 | 利用量計測(プロジェクト単位)|
| 説明 | 質問数 / FAQ 件数 / AI トークン / コスト集計を **プロジェクト単位** で計測 |
| 主管 FR | FR-148 |
| 保持期間 / 保持起点 | 7 年 / 計測日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `billing` |
| 設計判断 | `valid` 持たず(法定保管 7 年テーブル)。**プロジェクト単位で計測**し、`(contract_owner_user_id, project_id, billing_year_month)` で月次一意。上限件数・無料枠判定はプロジェクト単位の本テーブル値を用い、**Stripe 請求(`billing_invoices`)は `contract_owner_user_id` で全プロジェクトを SUM 集計**して確定する(契約集計)。`ai_token_*` / `ai_cost_yen` は契約単位の社内原価指標だがプロジェクト粒度でも保持する |

#### 3.22.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ | ① |  | `contract_owners(user_id)` 参照 |
| 3 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ | ② |  | `projects(id)` 参照(プロジェクト単位計測)|
| 4 | 請求年月 | `billing_year_month` | TEXT | - | NO |  |  | ② |  | YYYY-MM |
| 5 | 質問数 | `question_count` | INTEGER | - | NO |  |  |  | `0` | 月次累計 |
| 6 | FAQ 数(スナップショット)| `faq_count_snapshot` | INTEGER | - | NO |  |  |  | `0` | 月末時点 |
| 7 | AI 入力トークン | `ai_token_input` | INTEGER | - | NO |  |  |  | `0` | - |
| 8 | AI 出力トークン | `ai_token_output` | INTEGER | - | NO |  |  |  | `0` | - |
| 9 | AI コスト(円)| `ai_cost_yen` | INTEGER | - | NO |  |  |  | `0` | - |
| 10 | 確定日時 | `finalized_at` | TEXT | - | YES |  |  |  |  | - |
| 11 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.22.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.22.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |

#### 3.22.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_usage_metering_owner_project_month` | `(contract_owner_user_id, project_id, billing_year_month)` | ○ | プロジェクト × 月次一意 |
| 2 | `idx_usage_metering_owner_month` | `(contract_owner_user_id, billing_year_month)` |  | 契約集計(請求 SUM)用 |

#### 3.22.6 制約

(なし)

#### 3.22.7 コード値・区分値

(なし)

---

### 3.23 `owner_quota_overrides`

#### 3.23.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `owner_quota_overrides` |
| 論理名 | 契約別レート制限上書き |
| 説明 | 運営者による契約単位のレート制限上書き(SCR-093、DDoS / Bot / 暴走対策のセキュリティ目的)。レート種別(`*_per_*`)のみを `resource_kind` で扱う。**月次上限件数・無料枠はプロジェクト単位の `project_quota_limits`(§3.24)へ分離**(本テーブルでは扱わない)|
| 主管 FR | IF #5 / FR-128 |
| 保持期間 / 保持起点 | 5 年 / 設定日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `operator_high_priv` |
| 設計判断 | レート制限は契約単位のセキュリティ施策のため本テーブル(契約キー)に残置する。`valid` 持たず(`valid_until` 経過で無効化)。`window_sec` に計測窓秒、`threshold` に上限値を格納する。月次上限件数(`*_monthly_limit`)は v2.x でプロジェクト単位化に伴い本テーブルの `resource_kind` から撤去し、`project_quota_limits`(§3.24)へ移管した |

#### 3.23.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | リソース種別 | `resource_kind` | TEXT | - | NO |  |  |  |  | §3.23.7 |
| 4 | しきい値 | `threshold` | INTEGER | - | NO |  |  |  |  | レート系=上限値、月次上限件数系=上限件数 |
| 5 | 窓秒 | `window_sec` | INTEGER | - | YES |  |  |  |  | 計測窓秒(レート系)。月次上限件数系は `NULL`(暦月固定) |
| 6 | 有効期限 | `valid_until` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 7 | 理由 | `reason` | TEXT | - | YES |  |  |  |  | - |
| 8 | 作成者 | `created_by` | TEXT | - | YES |  |  |  |  | 運営者 ID(別 D1) |
| 9 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.23.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.23.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |

#### 3.23.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_tqo_owner_kind` | `(contract_owner_user_id, resource_kind, valid_until)` |  | オーナー × リソース × 有効期限 |

#### 3.23.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `resource_kind` | `resource_kind IN ('widget_ask_per_min','widget_inquiry_per_min','email_per_hour','admin_api_per_min')` |

#### 3.23.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `resource_kind` | `widget_ask_per_min` | ウィジェット質問レート |
| `resource_kind` | `widget_inquiry_per_min` | ウィジェット未解決質問送信レート |
| `resource_kind` | `email_per_hour` | メール送信レート |
| `resource_kind` | `admin_api_per_min` | 管理 API レート |

> `*_monthly_limit` 系コードは `project_quota_limits`(§3.24)へ移管したため、本テーブルには含めない。質問数だけが月次上限を持ち、FAQは無料枠行の識別子として使用する。

詳細は [10_課金・請求設計.md §3](10_課金・請求設計.md) / [04_権限設計.md §7](04_権限設計.md) 参照。

---

### 3.24 `project_quota_limits`

> 本テーブルは表番号 #24(§2 全テーブル一覧)。掲載位置は他テーブルとの差分を最小化するため §3.23 直後に置く。

#### 3.24.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `project_quota_limits` |
| 論理名 | プロジェクト別利用上限・無料枠 |
| 説明 | 質問数の月次上限件数・月次無料枠・アラート閾値と、FAQ件数の月次無料枠を課金対象別の行で保持する。管理者ユーザーが変更できるのは質問数行の上限件数とアラート閾値だけとし、無料枠とFAQ行は運営者設定専用とする。質問数の `threshold=NULL` は上限OFF、FAQの `threshold` はNULL固定で月次上限なしを表す |
| 主管 FR | FR-121, FR-122, FR-125, FR-127 |
| 保持期間 / 保持起点 | 永久(プロジェクト存続中) / 設定日時 |
| `contract_owner_user_id` | あり(オーナー境界判定用に非正規化保持。`projects` 経由でも到達可能だが認可ヘルパの一次判定に用いる)|
| `retention_class` | `general` |
| 設計判断 | 質問数上限と課金対象別無料枠を同じ行モデルで扱う。SCR-021 / SCR-021-M1 は質問数行の上限とアラートだけを参照・更新し、`free_quota` は変更しない。FAQは運営者設定専用で、上限・上限アラートを持たず、利用量による新規作成停止を行わない。未設定時は無料枠デフォルトへフォールバックし、質問数だけ上限デフォルトも解決する |

#### 3.24.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | プロジェクト ID | `project_id` | TEXT | - | NO |  | ○ | ① |  | `projects(id)` 参照 |
| 3 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照(オーナー境界判定用に非正規化)|
| 4 | リソース種別 | `resource_kind` | TEXT | - | NO |  |  | ① |  | §3.24.7 |
| 5 | 月次上限件数 | `threshold` | INTEGER | - | YES |  |  |  |  | 質問数は値あり=上限ON、NULL=上限OFF。FAQは NULL(上限なし)|
| 6 | 月次無料枠 | `free_quota` | INTEGER | - | YES |  |  |  |  | NULL = デフォルト無料枠を適用 |
| 7 | アラート閾値 | `alert_thresholds` | TEXT | - | NO |  |  |  | `'[]'` | JSON配列。質問数は `25` / `50` / `80` / `90` / `100` を複数保持可能。空配列は通知なし。FAQは空配列固定 |
| 8 | 設定元 | `source` | TEXT | - | NO |  |  | ① | `'owner'` | `owner`(オーナー / PJ 管理者設定)/ `operator`(運営者上書き)|
| 9 | 理由 | `reason` | TEXT | - | YES |  |  |  |  | 運営者上書き(`source='operator'`)時の理由・チケットID |
| 10 | 有効期限 | `valid_until` | TEXT | - | YES |  |  |  |  | 運営者上書きの有効期限(ISO 8601 UTC、NULL = 無期限)|
| 11 | 有効フラグ | `valid` | INTEGER | - | NO |  |  |  | `1` | 論理削除フラグ |
| 12 | 設定者 | `created_by` | TEXT | - | YES |  |  |  |  | 設定したユーザー ID(運営者上書き時は運営者 ID)|
| 13 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 14 | 更新日時 | `updated_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.24.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.24.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `project_id` | `projects` | `id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |

#### 3.24.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_pql_project_kind_source` | `(project_id, resource_kind, source)` WHERE `valid = 1` | ○ | プロジェクト × 課金対象 × 設定元で一意(有効行のみ)|
| 2 | `idx_pql_project_kind` | `(project_id, resource_kind)` |  | プロジェクト別利用設定解決 |
| 3 | `idx_pql_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |

#### 3.24.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `resource_kind` | `resource_kind IN ('question_monthly_limit','faq_monthly_limit')` |
| 2 | (暗黙) | CHECK | `alert_thresholds` | `json_valid(alert_thresholds) AND json_type(alert_thresholds) = 'array'`。要素の許可値・重複排除・昇順化はAPI層で保証 |
| 3 | (暗黙) | CHECK | `source` | `source IN ('owner','operator')` |
| 4 | (暗黙) | CHECK | `valid` | `valid IN (0,1)` |
| 5 | (暗黙) | 複合CHECK | 上限対象 | `(resource_kind = 'question_monthly_limit' AND (threshold IS NOT NULL OR alert_thresholds = '[]')) OR (resource_kind = 'faq_monthly_limit' AND threshold IS NULL AND alert_thresholds = '[]')` |
| 6 | (暗黙) | 複合CHECK | 管理主体 | `resource_kind = 'question_monthly_limit' OR source = 'operator'`。FAQに `source='owner'` の行を許可しない |

既存データ移行時は `alert_enabled=0` を `[]`、`alert_enabled=1 AND alert_frequency='first'` を `[80]`、`alert_enabled=1 AND alert_frequency='staged'` を `[80,100]` へ変換する。移行後は `alert_thresholds` のみを参照する。

質問数の無料枠と月次上限は独立した境界として扱うため、`free_quota <= threshold` は制約としない。これにより、利用者は非表示の無料枠値に依存せず、無料枠より低い月次上限も設定できる。

#### 3.24.7 コード値・区分値

| カラム名 | 値 | 意味 | デフォルト推奨値(プロジェクト単位)|
|---|---|---|---|
| `resource_kind` | `question_monthly_limit` | 質問数の月次上限件数 | 上限=KV `usage-limit:default:question` / 無料枠=KV `usage-limit:free-default:question`(初期 1,000)|
| `resource_kind` | `faq_monthly_limit` | FAQ 件数の無料枠(コード値は互換性のため維持、月次上限なし)| 無料枠=KV `usage-limit:free-default:faq`(初期 100)|

質問数上限の最小 / 最大件数は KV `usage-limit:min` / `usage-limit:max` を入力ガードに用いる。FAQには上限値を保持しない。KV ミラーは `usage-limit:<contract_owner_user_id>:<project_id>`(30s TTL)。

---

### 3.25 `service_announcements`

#### 3.25.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `service_announcements` |
| 論理名 | お知らせ(Control Plane) |
| 説明 | 運営者発信のお知らせ本体 |
| 主管 FR | - |
| 保持期間 / 保持起点 | 5 年 / 配信日時 |
| `contract_owner_user_id` | なし(全契約横断 or `announcement_audiences` で限定) |
| `retention_class` | `operator_high_priv` |
| 設計判断 | 配信対象は `announcement_audiences`(§3.26)で M:N 管理。`announcement_audiences` 行が 0 件のお知らせは「全契約配信」を意味する。`importance` 4 値は共有概念正本(`critical` はメール強制送信)|

#### 3.25.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | タイトル | `title` | TEXT | - | NO |  |  |  |  | 1〜200 文字 |
| 3 | 本文(HTML)| `body_html` | TEXT | - | NO |  |  |  |  | サニタイズ済 HTML |
| 4 | 重要度 | `importance` | TEXT | - | NO |  |  |  |  | `low` / `normal` / `high` / `critical` |
| 5 | 公開日時 | `published_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 取り下げ日時 | `retracted_at` | TEXT | - | YES |  |  |  |  | 取り下げ済みのみセット |
| 7 | 作成者 | `created_by` | TEXT | - | YES |  | ○ |  |  | `users(id)` 参照(削除でも履歴維持) |
| 8 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.25.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.25.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `created_by` | `users` | `id` | NO ACTION | SET NULL |

#### 3.25.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_announcements_published` | `published_at DESC` WHERE `retracted_at IS NULL` |  | 公開中の新着 |

#### 3.25.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `title` | `length(title) BETWEEN 1 AND 200` |
| 2 | (暗黙) | CHECK | `importance` | `importance IN ('low','normal','high','critical')` |

#### 3.25.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `importance` | `low` | 低 |
| `importance` | `normal` | 通常 |
| `importance` | `high` | 高 |
| `importance` | `critical` | クリティカル(メール強制送信、共有概念正本)|

[06_メッセージ一覧.md §2.4](06_メッセージ一覧.md) 参照。

---

### 3.26 `announcement_audiences`

#### 3.26.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `announcement_audiences` |
| 論理名 | お知らせ配信対象(M:N)|
| 説明 | お知らせの配信先 owner 限定指定(行 0 件 = 全契約配信)|
| 主管 FR | - |
| 保持期間 / 保持起点 | 親お知らせに従属 / - |
| `contract_owner_user_id` | (限定) |
| `retention_class` | `operator_high_priv` |
| 設計判断 | `valid` 持たず(M:N 関係系)。配信対象は中間テーブルで M:N 管理する(`service_announcements` 側に JSON 配列の多値列は持たない)。1NF 整合・JOIN 可能性・150 契約スケール時の性能を担保する。行 0 件 = 全契約配信(`contract_owners.contract_status='active' AND valid=1` の動的解決)、行 1 件以上 = 限定配信。配信ファンアウトは `announcement_recipients` で user 単位に展開される |

#### 3.26.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | お知らせ ID | `announcement_id` | TEXT | - | NO | ① | ○ |  |  | 複合 PK / `service_announcements(id)` 参照 |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO | ② | ○ |  |  | 複合 PK / `contract_owners(user_id)` 参照 |
| 3 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.26.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `(announcement_id, contract_owner_user_id)`(複合)|

#### 3.26.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `announcement_id` | `service_announcements` | `id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |

#### 3.26.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_announcement_audiences_owner` | `(contract_owner_user_id, announcement_id)` |  | 契約別逆引き |

#### 3.26.6 制約

(なし)

#### 3.26.7 コード値・区分値

(なし)

---

### 3.27 `announcement_recipients`

#### 3.27.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `announcement_recipients` |
| 論理名 | お知らせ受信者 |
| 説明 | お知らせ配信先 / 配信集計 / 監査 |
| 主管 FR | - |
| 保持期間 / 保持起点 | 5 年 / 配信日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(配信履歴)。`service_announcements` 物理削除時に CASCADE で連鎖物理削除。配信ファンアウトの user 単位レコード |

#### 3.27.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | お知らせ ID | `announcement_id` | TEXT | - | NO |  | ○ |  |  | `service_announcements(id)` 参照 |
| 3 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  |  |  |  | (FK 制約なし、削除耐性)|
| 4 | ユーザー ID | `user_id` | TEXT | - | NO |  |  |  |  | (FK 制約なし、同上)|
| 5 | 送信日時 | `sent_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 配信状態 | `delivery_status` | TEXT | - | NO |  |  |  |  | `pending` / `delivered` / `failed` |
| 7 | 既読日時 | `read_at` | TEXT | - | YES |  |  |  |  | - |

#### 3.27.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.27.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `announcement_id` | `service_announcements` | `id` | NO ACTION | CASCADE |

#### 3.27.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_ar_announcement` | `announcement_id` |  | お知らせ別 |
| 2 | `idx_ar_owner_user` | `(contract_owner_user_id, user_id)` |  | ユーザー別逆引き |

#### 3.27.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `delivery_status` | `delivery_status IN ('pending','delivered','failed')` |

#### 3.27.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `delivery_status` | `pending` | 配信待ち |
| `delivery_status` | `delivered` | 配信完了 |
| `delivery_status` | `failed` | 配信失敗 |

---

### 3.28 `inbox_messages`

#### 3.28.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `inbox_messages` |
| 論理名 | 受信箱(Tenant Plane) |
| 説明 | 利用者管理者のお知らせ・通知の既読管理 |
| 主管 FR | FR-182-192 |
| 保持期間 / 保持起点 | 1 年 / 既読日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(ログ系)。**お知らせ二層構成**: Control Plane(`service_announcements` / `announcement_recipients`)= 運営者作成お知らせ本体 + 配信集計・監査、Tenant Plane(`inbox_messages`)= 利用者管理者の受信箱(既読管理)。`announcement` カテゴリは Control → Tenant の二層経由、`billing` / `system` は `inbox_messages` 直接生成。重複生成対策: `(contract_owner_user_id, dedup_key)` で 60 分以内は 1 件に集約 |

#### 3.28.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | 受信者ユーザー ID | `recipient_user_id` | TEXT | - | NO |  | ○ |  |  | `users(id)` 参照 |
| 4 | カテゴリ | `category` | TEXT | - | NO |  |  |  |  | `billing` / `announcement` / `system` |
| 5 | 優先度 | `priority` | TEXT | - | NO |  |  |  |  | `low` / `normal` / `high` / `critical` |
| 6 | タイトル | `title` | TEXT | - | NO |  |  |  |  | 1〜200 文字 |
| 7 | 本文(HTML)| `body_html` | TEXT | - | NO |  |  |  |  | サニタイズ済 |
| 8 | 由来種別 | `source_kind` | TEXT | - | YES |  |  |  |  | - |
| 9 | 由来 ID | `source_id` | TEXT | - | YES |  |  |  |  | - |
| 10 | 重複キー | `dedup_key` | TEXT | - | NO |  |  |  |  | 集約用キー |
| 11 | 既読日時 | `read_at` | TEXT | - | YES |  |  |  |  | - |
| 12 | 非表示日時 | `hidden_at` | TEXT | - | YES |  |  |  |  | - |
| 13 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.28.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.28.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `recipient_user_id` | `users` | `id` | NO ACTION | CASCADE |

#### 3.28.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_inbox_owner_user_unread` | `(contract_owner_user_id, recipient_user_id, read_at, created_at DESC)` |  | 未読 + 新着 |
| 2 | `idx_inbox_dedup` | `(contract_owner_user_id, dedup_key, created_at)` |  | 60 分集約判定 |

#### 3.28.6 制約

| No | 制約名 | 種別 | 対象カラム | 条件・内容 |
|---:|---|---|---|---|
| 1 | (暗黙) | CHECK | `category` | `category IN ('billing','announcement','system')` |
| 2 | (暗黙) | CHECK | `priority` | `priority IN ('low','normal','high','critical')` |
| 3 | (暗黙) | CHECK | `title` | `length(title) BETWEEN 1 AND 200` |

#### 3.28.7 コード値・区分値

| カラム名 | 値 | 意味 |
|---|---|---|
| `category` | `billing` | 課金 |
| `category` | `announcement` | お知らせ(Control → Tenant 経由)|
| `category` | `system` | システム |
| `priority` | `low` | 低 |
| `priority` | `normal` | 通常 |
| `priority` | `high` | 高 |
| `priority` | `critical` | クリティカル(メール強制送信)|

---

### 3.29 `withdrawal_requests`

#### 3.29.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `withdrawal_requests` |
| 論理名 | 退会申請 |
| 説明 | 退会申請レコード(30 日猶予) |
| 主管 FR | FR-189 |
| 保持期間 / 保持起点 | 7 年 / 申請日時 |
| `contract_owner_user_id` | あり |
| `retention_class` | `billing` |
| 設計判断 | `valid` 持たず(法定保管 7 年テーブル)。退会日時の証跡は `applied_at` で表現 |

#### 3.29.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | 契約 owner ID | `contract_owner_user_id` | TEXT | - | NO |  | ○ |  |  | `contract_owners(user_id)` 参照 |
| 3 | 申請日時 | `applied_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 4 | 申請者 | `applied_by` | TEXT | - | NO |  | ○ |  |  | `users(id)` 参照 |
| 5 | 理由 | `reason` | TEXT | - | YES |  |  |  |  | - |
| 6 | 削除予定日 | `scheduled_deletion_at` | TEXT | - | NO |  |  |  |  | 申請日 + 30 日 |

#### 3.29.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.29.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `contract_owner_user_id` | `contract_owners` | `user_id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `applied_by` | `users` | `id` | NO ACTION | NO ACTION |

#### 3.29.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `idx_withdrawal_owner_scheduled` | `(contract_owner_user_id, scheduled_deletion_at)` |  | 削除予定日順 |

#### 3.29.6 制約

(なし)

#### 3.29.7 コード値・区分値

(なし)

---

### 3.30 `terms_versions`

#### 3.30.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `terms_versions` |
| 論理名 | 規約版数(全契約横断) |
| 説明 | 利用規約 / プライバシーポリシーの版(`doc_type` で文書を区別。文書ごとに独立して版管理する)|
| 主管 FR | - |
| 保持期間 / 保持起点 | 永久 / 制定日時 |
| `contract_owner_user_id` | なし(全契約横断) |
| `retention_class` | `general` |
| 設計判断 | `valid` 持たず(全契約横断、版は永久保持)|

#### 3.30.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | 文書種別 | `doc_type` | TEXT | - | NO | ○ |  |  |  | `terms_of_service`(利用規約)/ `privacy_policy`(プライバシーポリシー)|
| 2 | 版 | `version` | TEXT | - | NO | ○ |  |  |  | バージョン文字列(文書ごとに独立採番)|
| 3 | 発効日 | `effective_date` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 4 | 本文(HTML)| `body_html` | TEXT | - | NO |  |  |  |  | - |
| 5 | 差分サマリ | `diff_summary` | TEXT | - | YES |  |  |  |  | - |
| 6 | 通知送信日時 | `notification_sent_at` | TEXT | - | YES |  |  |  |  | - |
| 7 | 同意期限日数 | `consent_deadline_days` | INTEGER | - | NO |  |  |  | `14` | 再同意期限 |
| 8 | 作成日時 | `created_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |

#### 3.30.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `(doc_type, version)`(複合主キー。文書 × 版で一意)|

#### 3.30.4 外部キー

(なし、全契約横断)

#### 3.30.5 インデックス

(なし、PK 検索のみ)

#### 3.30.6 制約

(なし)

#### 3.30.7 コード値・区分値

| カラム | 値 | 意味 |
|---|---|---|
| `doc_type` | `terms_of_service` | 利用規約(SCR-010)|
| `doc_type` | `privacy_policy` | プライバシーポリシー(SCR-020)|

---

### 3.31 `terms_agreements`

#### 3.31.1 概要

| 項目 | 内容 |
|---|---|
| テーブル名 | `terms_agreements` |
| 論理名 | 規約同意 |
| 説明 | 利用者ごとの利用規約 / プライバシーポリシー同意履歴(`doc_type` で文書ごとに個別記録)|
| 主管 FR | - |
| 保持期間 / 保持起点 | 7 年 / 同意日時 |
| `contract_owner_user_id` | なし(`users` 経由) |
| `retention_class` | `billing` |
| 設計判断 | `valid` 持たず(法定保管 7 年テーブル)。`(user_id, doc_type, terms_version)` で一意(文書ごとに独立して同意を記録)|

#### 3.31.2 カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 説明 |
|---:|---|---|---|---:|---|---|---|---|---|---|
| 1 | ID | `id` | TEXT | - | NO | ○ |  |  |  | ULID |
| 2 | ユーザー ID | `user_id` | TEXT | - | NO |  | ○ | ① |  | `users(id)` 参照 |
| 3 | 文書種別 | `doc_type` | TEXT | - | NO |  | ② | ① |  | `terms_of_service` / `privacy_policy`。`terms_versions(doc_type, version)` 参照の一部 |
| 4 | 規約版 | `terms_version` | TEXT | - | NO |  | ② | ① |  | `terms_versions(version)` 参照の一部 |
| 5 | 同意日時 | `agreed_at` | TEXT | - | NO |  |  |  |  | ISO 8601 UTC |
| 6 | 同意 IP(マスク済)| `agreed_ip_masked` | TEXT | - | YES |  |  |  |  | - |

#### 3.31.3 主キー

| 項目 | 内容 |
|---|---|
| 主キー名 | (暗黙、PRIMARY KEY 句で指定) |
| 対象カラム | `id` |

#### 3.31.4 外部キー

| No | 外部キー名 | 対象カラム | 参照先テーブル | 参照先カラム | 更新時 | 削除時 |
|---:|---|---|---|---|---|---|
| 1 | (暗黙) | `user_id` | `users` | `id` | NO ACTION | CASCADE |
| 2 | (暗黙) | `(doc_type, terms_version)` | `terms_versions` | `(doc_type, version)` | NO ACTION | NO ACTION |

#### 3.31.5 インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|---|---|---|---|
| 1 | `uq_terms_user_doc_version` | `(user_id, doc_type, terms_version)` | ○ | ユーザー × 文書 × 版で一意 |

#### 3.31.6 制約

(なし)

#### 3.31.7 コード値・区分値

(なし)

---

## 4. 横断仕様

### 4.1 ER 図

ER 図は本書とは別の HTML ファイル [ER図.html](ER図.html) で管理する(md に直接 Mermaid を埋め込むと PDF 出力時に縮小されすぎて可読性が落ちるため)。HTML 上でブラウザサイドレンダリング + ズーム / パン / 全体表示ボタンを利用可能。

- **掲載カラム**: 主要キー(PK / FK)+ 業務キー(`inquiry_code` / `widget_public_key` 等)+ 状態カラム(`inquiries.status` / `contract_status` 等)+ 特徴的なカラム(`actor_user_id` 等)
- **省略カラム**: `created_at` / `updated_at` / `valid` / `*_encrypted` / `*_key_version` / `metadata` 等の共通技術カラム(完全なカラム定義は §3 を参照)
- **FK 制約を持たないが論理的に紐づく参照**(`error_logs.contract_owner_user_id` / `error_logs.user_id` / `announcement_recipients.contract_owner_user_id` / `announcement_recipients.user_id`)は ER 図上で「FK制約なし」注記付きカラムとして表現し、関係線は引かない
- **ER 図の編集ルール**: 図を変更する場合は [ER図.html](ER図.html) の `<pre id="mermaid-src">` ブロックを直接編集する(本書 md には Mermaid を二重保持しない)


### 4.2 命名規則・型方針

| 対象 | 規則 |
|---|---|
| テーブル | snake_case 複数形 |
| 主キー | 文字列 ID(ULID)/ `*_versions` 等の例外あり |
| 外部キー | `{table_singular}_id` |
| 日時 | TEXT(ISO 8601 UTC、NFR-1103) |
| 状態 | `status` または `{対象}_status` |
| 真偽値 | INTEGER 0/1 |
| JSON | TEXT 列に JSON 文字列で保存 |
| 暗号化 | `{column}_encrypted`(AES-256-GCM) + `{column}_hmac`(検索用)の 2 列構成 |

### 4.3 SaaS データ分離

- 全契約スコープのテーブルに `contract_owner_user_id` カラム必須(全契約横断テーブルを除く ── §4.4 参照)
- 「契約境界(owner)」を表す FK 系列 → `contract_owners(user_id)` を参照(契約境界違反を物理的に弾く)。「操作者・主体(actor)」系 → `users(id)` を参照
- API / 認可レイヤで境界検証: `principal.contractOwnerUserId === target.contract_owner_user_id`([08_認証・認可設計.md §5](08_認証・認可設計.md))
- 違反時は **404 偽装**(403 だと相手契約のリソース存在が漏れる)

#### 4.3.1 オーナー / プロジェクト管理者 / メンバーの判定

- **オーナー判定**: `contract_owners.user_id = users.id` 行が存在するユーザー(行存在 = オーナー)
- **プロジェクト管理者**: 非オーナー かつ `project_users.role='admin' AND valid=1` を 1 件以上保持するユーザー
- **メンバー**: 非オーナー かつ `project_users.role='member' AND valid=1` のみを保持するユーザー
- 同一ユーザーがオーナー兼任 / 複数プロジェクト所属する場合の権限解決は、認可フロー先頭の `isOwner` bypass を最優先([08_認証・認可設計.md §5](08_認証・認可設計.md) を正本)
- 1 契約 = 1 オーナー固定は `contract_owners.user_id PRIMARY KEY` で DB レベル担保(`is_owner` カラムや `owner_account_id` 自己参照は持たない)

### 4.4 全契約横断テーブル(`contract_owner_user_id` を持たない)

| テーブル名 | 理由 |
|---|---|
| `email_suppression_list` | メール bounce / 苦情の全契約横断管理(R-003 共通ドメイン対策)|
| `terms_versions` | 規約 / プライバシーポリシーの版(全利用者共通)|
| `service_announcements` | 運営者発信のお知らせ本体(配信先は `announcement_audiences` / `announcement_recipients` で契約別)|

### 4.5 状態遷移

#### 4.5.1 FAQ 状態遷移(`faqs.status`)

| From | To | トリガー | 条件 |
|---|---|---|---|
| - | `draft` | FAQ 作成 | admin のみ |
| `draft` / `hidden` ↔ `published` | (相互) | SCR-006 で「状態」ラジオを選択して保存(`PATCH /faqs/{id}`)| admin のみ。状態遷移ガードなし(`draft`/`published`/`hidden` を自由遷移)|
| `draft` ↔ `hidden` | (相互) | 同上 | admin のみ |
| any | `deleted` | 論理削除 | admin のみ |
| `deleted` | `draft` | 復元 | **運営者のみ**(IF #4)|

**運用方針**: `draft` / `published` / `hidden` の 3 状態は SCR-006 編集画面の「状態」ラジオで選択し「保存」ボタンで相互に自由遷移する。専用の公開 API・状態遷移ガードは持たない。`published` を選択して保存する操作が管理者(admin)による公開前の内容確認(FR-045)を兼ねる。`published` 設定時のみ保存前に確認ダイアログを表示する(UI 上の確認であり遷移制約ではない)。

#### 4.5.2 未解決質問の状況遷移(`inquiries.status`、共有概念正本)

| From | To | トリガー | 条件 | 通知 |
|---|---|---|---|---|
| - | `open` | 未解決質問登録 | システム自動 | なし |
| `open` | `closed` | SCR-005 詳細画面の手動操作 | なし | なし |
| `closed` | `open` | SCR-005 詳細画面の手動操作(再オープン)| なし | なし |

FAQ 下書き保存・FAQ 公開成功は本状況を変更しない(連動ロジックなし)。再オープン回数に制限を設けない。

#### 4.5.4 通知状態遷移(`notification_logs.delivery_state`)

| From | To | トリガー |
|---|---|---|
| - | `queued` | Queue 投入 |
| `queued` | `sending` | Worker 起動 |
| `sending` | `sent` | Resend API 成功 |
| `sending` | `failed` | Resend API 失敗(恒久)── 当方再送対象(最大 3 回、NFR-506)|
| `sent` | `delivered` | `email.delivered` Webhook(終了状態)|
| `sent` | `bounced` | `email.bounced` Webhook(ハード / ソフト 5 連続)|
| `sent` | `complained` | `email.complained` Webhook |
| `sent` | `delayed` | `email.delivery_delayed` Webhook(Resend 再送中)|
| `delayed` | `delivered` / `bounced` | 後続結果 |
| `bounced` | `suppressed` | サプレスリスト追加 |
| `complained` | `suppressed` | 即時永久サプレス(復帰不可)|
| `failed` | `queued` | 当方再送(最大 3 回、3 回失敗 + 24 時間経過で admin へ system 通知)|

#### 4.5.5 契約状態遷移(`contract_owners.contract_status`、共有概念正本)

詳細は [10_課金・請求設計.md §4 / §5](10_課金・請求設計.md) を正本とする。本書では概要のみ:

| From | To | トリガー | reason |
|---|---|---|---|
| - | `active` | 契約作成 | - |
| `active` | `suspended` | IF #1(支払い失敗 7 日猶予経過)| `payment_failure_grace_expired` |
| `active` | `suspended` | IF #1(運営者手動)| `operator_manual` / `tos_violation` |
| `suspended` | `active` | IF #1(支払再開 / 解除)| - |
| `active` / `suspended` | `deleted_pending` | admin 退会申請(SCR-014)| - |
| `deleted_pending` | `active` / `suspended`(元の状態)| admin 撤回(30 日以内)| - |
| `deleted_pending` | `deleted` | 30 日経過 + 物理削除 Batch | - |

`contract_status × valid` の整合(`contract_owners` 複合 CHECK で強制):

| `contract_status` | `valid` | 状態 |
|---|---|---|
| `active` / `suspended` | 1 | オーナー稼働中 |
| `deleted_pending` | 1 | オーナー退会申請中(30 日猶予)|
| `deleted` | 0 | オーナー退会完了(`valid=0` と必ず同時セット = CHECK)|

メンバーの論理削除は `users.valid=0` で表現する(`contract_owners` 行を持たないため)。

### 4.6 データ削除モード

**設定**: `contract_owners.data_deletion_mode`(`physical` / `anonymize`、初期値 `physical`)で保持する独立カラム。

**論理削除** は `UPDATE … SET valid=0` で行い、90 日経過後の物理削除バッチ(DD13)で物理削除モード(`physical` / `anonymize`)に従って実体を消去する。SCR-009-M1 の「プロジェクトから外す」は対象 `project_users` 行だけを論理削除する。オーナーによるメンバーの契約からの削除は `users` と当該ユーザーの全 `project_users` を論理削除する。SCR-004-M1(編集モード)のプロジェクト削除は `projects` と関連 `project_users` 行を論理削除する。

#### 4.6.1 削除モード対応表

| 対象テーブル | 論理削除 | 物理削除(90 日後バッチ or 即時、`data_deletion_mode='physical'`)| 匿名化(`data_deletion_mode='anonymize'`)|
|---|---|---|---|
| `users` | `valid=0` | DELETE(ON DELETE CASCADE で関連行連鎖)| `email_encrypted` → `anon_<sha256>`, `name` → 「匿名」 |
| `contract_owners` | `valid=0`(`contract_status='deleted'` と必ず同時セット = CHECK)| DELETE(`users` CASCADE 経由)| (匿名化対象外、`users` 連鎖物理削除)|
| `project_users` | `valid=0` | DELETE(`users` または `projects` CASCADE 経由)| (匿名化対象外、CASCADE 連鎖物理削除)|
| `projects` | `status='deleted', valid=0` | DELETE(関連 `faqs` / `inquiries` 等は CASCADE 経由)| `name` → 「削除済みプロジェクト」 |
| `allowed_domains` | `valid=0` | DELETE | (匿名化対象外)|
| `project_quota_limits` | `valid=0` | DELETE(`projects` / `contract_owners` 物理削除時に CASCADE 連鎖)| (匿名化対象外)|
| `faqs` | `status='deleted', valid=0` | DELETE | `title` / `body` 空字 |
| `question_logs` | `valid=0` | DELETE | `user_question` 空字, `ip_address` / `user_agent` NULL, 集計値維持 |
| `question_log_faq_refs` | (`valid` なし、M:N 関係系)| `question_logs` / `faqs` 物理削除時に CASCADE 連鎖 | (匿名化対象外)|
| `inquiries` | `valid=0` | DELETE | `user_question` 空字, `inquiry_code` 維持 |
| `billing_subscriptions` | `valid=0` | (物理削除バッチ対象外、永久保持)| (匿名化対象外、電子帳簿保存法)|
| `usage_metering` | (`valid` なし、法定保管 7 年)| (物理削除バッチ対象外、7 年保持)| (匿名化対象外。プロジェクト単位計測値は契約退会後も 7 年保持)|
| `notification_logs` | (`valid` なし、ログ系で論理削除概念外)| DELETE | `recipient_email_hmac` → SHA-256, vars → 空 JSON |
| `announcement_audiences` | (`valid` なし、配信関係 M:N)| `service_announcements` / `contract_owners` 物理削除時に CASCADE 連鎖 | (匿名化対象外)|
| `access_tokens` | (`valid` なし、`used_at` で代用)| DELETE | (常に物理削除、匿名化対象外)|
| `sessions` | (`valid` なし、`revoked_at` で代用)| DELETE | (匿名化対象外)|
| `audit_logs` | (`valid` なし、メイン側は API 操作ログ専用)| `created_at + retention 期間` 経過後に物理 DELETE(`general` 1y / `billing` 7y / `operator_high_priv` 5y)| (匿名化対象外。ハッシュチェーン正本は運営者側)|

#### 4.6.2 論理削除の物理削除バッチ(DD13 正本)

- 日次バッチで `valid=0 AND updated_at < now() - 90d` の行を物理削除モード(`contract_owners.data_deletion_mode` 準拠)に従って処理
- `users` 行が物理 DELETE された時点で `ON DELETE CASCADE` で関連 `contract_owners` / `project_users` / `sessions` / `access_tokens` / `terms_agreements` 等が連鎖物理削除
- `projects` 行が物理 DELETE された時点で `ON DELETE CASCADE` で関連 `faqs` / `inquiries` 等が連鎖物理削除
- `billing_subscriptions` は物理削除バッチの対象外(永久保持)

### 4.7 データ保持の調整制約(NFR-702)

- **データ保持期間はシステム固定値とし、契約・管理者ユーザーによる UI 上の変更は不可**(短縮・長期化いずれも不可)
- 固定値は本書 §2 のテーブル一覧「保持期間」列および要件定義 NFR-701 / NFR-705 を正本とする
- 短縮が必要な場合(法令・契約義務範囲内)はサポート窓口経由の運営者対応とする
- SCR-017 個人設定にデータ保持期間設定 UI を持たない([01_画面設計.md §5.24](01_画面設計.md) 参照)
- `usage_metering` / `billing_invoices` / `audit_logs(retention_class='billing')` は電子帳簿保存法・国税法の要請により最低 7 年保持(NFR-602b)、これを下回る短縮は不可

### 4.8 PII 取扱

| 観点 | 仕様 |
|---|---|
| 暗号化方式 | AES-256-GCM 列単位、オーナー派生鍵(NFR-319) |
| 対象テーブル | `users.email_encrypted` |
| 検索方式 | 暗号化 + `*_hmac` カラム(HMAC-SHA256)で復号不要 |
| 鍵ローテーション | 年次 Master Key ローテーション時に KDF 再実行。60 日 dual-decrypt 期間(NFR-321) |
| 鍵派生入力 | `deriveOwnerKey(masterKey, contractOwnerUserId)`(`contractOwnerUserId` = `contract_owners.user_id`) |
| カード情報 | Stripe に委譲(PCI-DSS 範囲外) |

詳細は [09_セキュリティ設計.md §3 / §8](09_セキュリティ設計.md) を正本とする。

### 4.9 インデックス方針

| 観点 | 方針 |
|---|---|
| FAQ 検索(FTS5)| `faq_search_fts` 仮想テーブル、`(project_id, status='published')` 前段フィルタ |
| 未解決質問一覧 | `(contract_owner_user_id, project_id, status, updated_at DESC)` |
| お知らせ一覧(NFR-106)| `(recipient_user_id, read_at)` / `(recipient_user_id, category, created_at DESC)` |
| 未読件数(NFR-106)| KV キャッシュ `inbox:unread:<user_id>`(TTL 60 秒) + DB フォールバック |
| ユニーク | `inquiry_code`, `widget_public_key`, `access_tokens.token_hash`, `(contract_owner_user_id, billing_year_month)` |
| 監査ログ | `(contract_owner_user_id, action, created_at)`, `(retention_class, created_at)`(保持期間バッチ用)|

## 5. 未決事項

| No | 内容 | 確認先 | 期限 | ステータス |
|---|---|---|---|---|
| 1 | 利用上限テーブルの構成 | - | - | 既決: レート制限は契約単位で `owner_quota_overrides`(`resource_kind` = レート種別のみ)、月次上限件数・無料枠はプロジェクト単位で `project_quota_limits`(§3.24)に分離 |
| 2 | `question_log_faq_refs` のスキーマ(本書 §3.13 で新規記述。詳細設計側との整合確認待ち)| 詳細設計 DD03 | - | 確認待ち |
