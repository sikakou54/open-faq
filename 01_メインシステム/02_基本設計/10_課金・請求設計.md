# 課金・請求設計書(メイン)

## 1. 文書概要

### 1.1 目的

メインシステムの課金・契約・請求・決済を一元化する。**MVP の課金モデル**(完全従量課金 + 月次無料枠、無料トライアル期間なし)、契約状態(`contract_owners.contract_status` 4 値)、契約ライフサイクル、契約別利用上書き、決済フロー(Stripe)を定義する。

### 1.2 対象範囲

- 対象: 利用者向け課金 UI(SCR-015 課金部 / SCR-028 オーナーセクション §(4) 退会導線 / SCR-024)、契約状態管理、月次利用量計測、Stripe 連携、請求書管理
- 対象外: Stripe Webhook の一次受信・DLQ・リプレイ([02_運営者システム/個別設計書群/11_課金請求設計書.md §13](../../02_運営者システム/個別設計書群/11_課金請求設計書.md) 正本)

### 1.3 版数

| 項目 | 値 |
|---|---|
| 版数 | 1.0 |
| 更新日 | 2026-05-17 |

### 1.4 関連ドキュメント

| ドキュメント名 | 役割 | 参照先 |
|---|---|---|
| 索引 | 11 ドキュメント体系の俯瞰 | [00_索引.md](00_索引.md) |
| 共有概念対応表 | `contract_status` / 契約上書きテーブル | [../../共有/共有概念.md](../../共有/共有概念.md) |
| 権限設計書 | 契約別利用制限 | [05_権限設計書.md](05_権限設計書.md) |
| API 設計書 | 連携 IF #1 / #10 | [03_API設計書.md](03_API設計書.md) |
| エラー設計書 | 課金エラー(`E-BILL-*`、`E-QUOTA-*`)| [06_エラー設計書.md](06_エラー設計書.md) |
| メッセージ一覧 | 課金・契約メッセージ / `critical` 通知 | [07_メッセージ一覧.md](07_メッセージ一覧.md) |
| 認証・認可設計書 | サスペンション中の操作制限 | [09_認証認可設計書.md](09_認証認可設計書.md) |
| 運営者側 課金・請求設計書 | Stripe Webhook / DLQ / 月次請求確定 cron(正本) | [../../02_運営者システム/個別設計書群/11_課金請求設計書.md](../../02_運営者システム/個別設計書群/11_課金請求設計書.md) |
| 要件 §8.11 | 課金単位 / 機能要件の正本 | [../01_要件定義書.md](../01_要件定義書.md) |

## 2. 契約モデル(MVP は単一プラン、共有概念正本)

### 2.1 単一プラン定義

MVP は **Free / Basic / Pro / Enterprise の階層プランを採用しない**。以下の単一モデルを全契約に適用する。

| 項目 | MVP 仕様 | 正本 |
|---|---|---|
| 課金モデル | **完全従量課金制 + 月次無料枠** | 要件 §8.11.1 / 基本設計書 §6.6 |
| 契約開始 | 新規登録完了時点で `active`(**無料トライアル期間なし**)。支払方法登録は任意、無料枠超過時にウィジェット質問受付を停止して必須化(支払方法ゲート、FR-136。契約は `active` 維持)| §5.1 |
| 再課金リトライ | **Stripe Smart Retries 委譲、独自リトライ禁止** | §11.4 |
| 契約別カスタマイズ | `owner_quota_overrides`(`resource_kind` 統一実装)で契約単位に上書き(運営者操作)| §3 / [05_権限設計書.md §7](05_権限設計書.md) |
| 月次境界 | JST 暦月、無料枠は毎月リセット | §8.2 |
| 訂正請求 | Stripe Credit Note API、運営者の手動操作のみ(自動禁止)| §11.5 |

### 2.2 階層プランへの拡張余地

将来的にプラン階層を導入する場合(Future)、本書は `pricing_plans` テーブル + `contract_owners.plan_id` 追加で拡張可能な構造を残す。MVP では `pricing_plans` テーブルは作成しない。

## 3. 契約別利用上書き

運営者が契約単位で `owner_quota_overrides` テーブルを操作する(SCR-093)。**`resource_kind` で統一実装**(`rate_limit_overrides` / `usage_limit_overrides` の別テーブルではない、テーブル定義書 04 §3.25 参照)。レート制限系(`*_per_*`)と月次上限件数系(`*_monthly_limit`)を同一テーブルで扱う。

| `resource_kind` | 用途 | 主管 SCR(運営者)| KV ミラー |
|---|---|---|---|
| `widget_ask_per_min` | ウィジェット質問レート | SCR-093 | あり(60s TTL)|
| `widget_inquiry_per_min` | ウィジェット未解決質問送信レート | SCR-093 | あり |
| `widget_chat_per_min` | ウィジェットチャット投稿レート | SCR-093 | あり |
| `email_per_hour` | メール送信レート | SCR-093 | あり |
| `admin_api_per_min` | 管理 API レート | SCR-093 | あり |
| `question_monthly_limit` | 質問数の月次上限件数 | SCR-093(運営者上書き)/ SCR-015-M1(オーナー設定)| あり |
| `faq_monthly_limit` | FAQ 件数の月次上限件数 | SCR-093 / SCR-015-M1 | あり |
| `chat_room_monthly_limit` | 個別チャット部屋数の月次上限件数 | SCR-093 / SCR-015-M1 | あり |

詳細は [04_テーブル定義書.md §3.25](04_テーブル定義書.md) 参照。

## 4. 契約状態(`contract_owners.contract_status` 4 値 = 共有概念正本)

(基本設計書 §4.9 から移管)

| 状態 | 説明 | 利用可否 | システム動作 | 関連メッセージID |
|---|---|---|---|---|
| `active` | 有効 | 全機能 | 通常 | - |
| `suspended` | サスペンション中(支払い失敗 / 運営者手動停止)| オーナーのみ、課金画面 / 退会画面 / データエクスポート画面 | ウィジェット応答は機能停止旨を返す(**423**、FR-138)| MSG-BILL-CONTRACT-SUSPENDED |
| `deleted_pending` | 退会申請中(30 日猶予)| オーナーのみ、退会キャンセル / データエクスポート | データ参照可、新規操作不可 | MSG-BILL-DELETE-PENDING |
| `deleted` | 削除済み | × | アカウントスラグのみ `accounts_retired` に永久保持(運営者側)。`contract_status='deleted'` 設定時は必ず `contract_owners.valid=0` も同時セット(両者は同時遷移)| MSG-BILL-DELETED |

#### `contract_status` × `valid` の整合(03_テーブル設計.md §3.1 と同期)

| ユーザー種別 | `contract_owners.contract_status` | `valid`(行種別)| 状態 |
|---|---|---|---|
| オーナー(`contract_owners` 行存在)| `active` / `suspended` | `contract_owners.valid=1` + `users.valid=1` | オーナー稼働中 / サスペンション中 |
| オーナー(`contract_owners` 行存在)| `deleted_pending` | `contract_owners.valid=1` + `users.valid=1` | オーナー退会申請中(30 日猶予) |
| オーナー(`contract_owners` 行存在)| `deleted` | `contract_owners.valid=0` + `users.valid=0` | オーナー退会完了(`valid=0` と必ず同時セット) |
| メンバー(`contract_owners` 行なし)| (該当なし、`contract_owners` 行を持たない)| `users.valid=1` | メンバー有効 |
| メンバー(`contract_owners` 行なし)| (該当なし、`contract_owners` 行を持たない)| `users.valid=0` | メンバー論理削除済み(`users.valid=0` のみ) |

オーナー退会完了時の `valid=0` は退会バッチ([DD14_バッチ・非同期処理.md](../03_詳細設計/DD14_バッチ・非同期処理.md))で `contract_status='deleted'` 更新と同一トランザクション内で `contract_owners.valid=0` + `users.valid=0` を同時セットする。メンバー論理削除時は `contract_owners` 行を持たないため `contract_status` は無関係で、`users.valid=0` のみ更新する。

### 4.1 状態遷移詳細(付録 B.5 から移管)

| From | To | トリガー | reason | セッション処理 |
|---|---|---|---|---|
| - | `active` | 契約作成 | - | - |
| `active` | `suspended` | IF #1(支払い失敗 7 日猶予経過)| `payment_failure_grace_expired` | 継続(課金 / エクスポート / 退会のみ)|
| `active` | `suspended` | IF #1(運営者手動)| `operator_manual` / `tos_violation` | **5 秒以内全削除**(FR-022)|
| `suspended` | `active` | IF #1(支払再開 / 解除)| - | 再ログイン要求 |
| `active` | `deleted_pending` | admin 退会申請(SCR-024)| - | 継続(参照中心)|
| `deleted_pending` | `active` / `suspended`(元の状態)| admin 撤回(30 日以内)| - | 継続 |
| `deleted_pending` | `deleted` | 30 日経過 + 物理削除 Batch | - | 全セッション無効化 |
| `suspended` | `deleted_pending` | 運営者退会・削除移行 | - | - |

## 5. 契約ライフサイクル

### 5.1 契約開始フロー(無料トライアル期間なし)

| イベント | 変更前状態 | 変更後状態 | トリガー | 関連API |
|---|---|---|---|---|
| 契約開始 | (新規)| `active` | SCR-002 新規登録完了 | `POST /accounts` |
| 支払方法登録(任意)| `active` | `active`(支払方法登録済)| 支払方法登録 + Setup Intent 成功 | `POST /billing/payment-method` |

- **無料トライアル期間は設けない**。新規登録完了時点で契約は即 `active` となり、月次無料枠の範囲を無料で利用できる。
- **支払方法の登録は任意**。未登録でも無料枠内の利用およびダッシュボード閲覧は可能。
- **支払方法ゲート(FR-136)**: 無料枠 80% 到達時に支払方法登録が必要になる旨を SCR-015 ダッシュボードのバナー + 受信箱 / メールで事前告知する(§8.2 / §15)。
- **支払方法未登録の契約が月次無料枠を超過した場合、契約状態は `active` のまま、ウィジェットの新規質問受付のみを停止**する(応答 429 + `Retry-After`、エンドユーザーへ連絡先メール誘導)。**契約サスペンションは行わず管理画面は全機能利用可能**。ダッシュボードには無料枠超過と支払方法登録誘導(PaymentMethodBanner)を表示する。FAQ 件数・個別チャット部屋数の無料枠 100% 超過新規作成は支払方法未登録時に拒否し登録へ誘導する。
- **支払方法登録済の契約**は、無料枠超過分を事後課金とし(ウィジェットは継続)、FAQ 件数・個別チャット部屋数の 100% 超過新規作成のみ拒否する(§8.2)。
- ウィジェット質問受付停止は支払方法登録(Setup Intent 成功)で即時再開する。以降の無料枠超過分は事後課金となる。

### 5.2 サスペンションフロー(§6.2.7 から移管)

起点イベント:
- 支払い失敗 連携 IF #1 受信 → `reason=payment_failure_grace_expired`(**7 日猶予タイマー開始**)
- 運営者手動停止 → `reason=operator_manual` / `tos_violation`(即時 suspended)

> **支払方法ゲート(FR-136)は契約サスペンションではない**。支払方法未登録の契約が無料枠を超過した場合は契約 `active` のままウィジェット質問受付のみを停止する(§8.2 / DD13 §3.7a)。本フローの対象外。

認可ゲート(`contract_owners.contract_status=suspended` 中):
- **許可**: 課金画面 / データエクスポート画面 / 退会画面のみアクセス可
- **その他**: 403
- **ウィジェット応答**: 423(FR-138)

解除条件:
- `payment_failure_grace_expired` / `operator_manual` / `tos_violation`: 顧客管理側の連携 IF #1 受信(支払い成功 / 運営者解除)で `contract_owners.contract_status='active'` へ

### 5.3 退会・解約フロー(§6.2.8 から移管)

```
admin が SCR-024 で退会申請
  → 再認証 FR-005
  → contract_owners.contract_status='deleted_pending'
  → ウィジェット応答停止、新規 FAQ 登録不可
  → 当月末まで利用可
  → 月末締めで利用停止(月割りなし)
  → 30 日エクスポート猶予開始
  → 物理削除バッチ実行: contract_owners.contract_status='deleted' + contract_owners.valid=0 + users.valid=0(同時セット)
  → 全データ削除 + announcement_recipients 削除
```

| 観点 | 仕様 |
|---|---|
| 解約予告期間 | 当月末まで利用可、**月末締めで停止**(予告期間なし)|
| データエクスポート猶予 | 退会確定から **30 日**、SCR-024 退会申請画面 / SCR-015 課金部から CSV エクスポート可(旧 SCR-016 経由は廃止)|
| 物理削除 | 30 日経過後にバッチで物理削除(NFR-704)|
| クーリングオフ | B2B サービスのため特商法適用外。利用規約に明示 |
| 月途中の日割り | しない(FR-125)|
| 未請求残高返金 | 返金しない旨を利用規約に明記 |
| 解約 vs 退会 | 解約申請(サブスクリプション終了)と退会(契約削除)は別フロー |

## 6. 契約とオーナーの関係

| 項目 | 内容 |
|---|---|
| 契約単位 | `contract_owners` 行 = 1 契約(`user_id` PRIMARY KEY で 1 契約 = 1 オーナー固定)|
| オーナー / 組織との関係 | 契約 = 1 オーナーアカウント。組織概念は MVP 範囲外 |
| 複数契約可否 | 同一メールで複数契約は不可(`users.email_hmac` UNIQUE)|
| 契約管理者 | オーナーアカウント(MVP では譲渡不可、FR-333)|
| 契約停止時の利用制限 | 課金画面 / 退会画面 / データエクスポートのみ |

## 7. プラン変更時の仕様(MVP は契約上書きのみ)

MVP は単一プランのため、プラン階層変更は存在しない。**契約別上書きの追加・変更・削除** のみが行われる。

| 変更種別 | 反映タイミング | 課金処理 | 利用制限の扱い | 備考 |
|---|---|---|---|---|
| 契約上書き追加(運営者操作)| 即時(KV TTL 60s 経過後)| 翌月から | 即時適用 | SCR-093 / 4-eyes 承認ログ |
| 契約上書き削除 | 即時 | 翌月から標準値 | 即時標準値復帰 | SCR-093 / 4-eyes 承認ログ |

## 8. 課金方式 / 課金単位

### 8.1 課金単位(要件 §8.11.1 / 基本設計書 §6.6.1 正本)

| 課金対象 | 計測単位 | 計測タイミング | 失敗時の扱い | MVP 無料枠初期値 | MVP 超過課金単価初期値 |
|---|---|---|---|---|---|
| 質問数 | 1 質問 = エンドユーザーが「送信」操作した 1 リクエスト | リクエスト到達時(同期)| AI 推論失敗時もカウントするが `metering_billable=false` | 1,000 件 / 月 | 0.5 円 / 件 |
| FAQ 件数 | 公開状態の FAQ 件数(下書き・非公開・削除除外)| 月末 23:59:59 JST スナップショット | n/a | 100 件 | 5 円 / 件 / 月 |
| 個別チャット部屋数 | 1 部屋 = 新規 `chat_rooms` レコード作成 1 件 | 部屋作成成功時 | メッセージ投稿・再入室では追加カウントしない | 30 部屋 / 月 | 30 円 / 部屋 |
| AI 利用コスト(原価)| 推論呼び出しトークン換算(社内可視化用)| 呼び出し成功時のみ | 失敗は計測のみ・課金対象外。再試行は最初の 1 回のみ計測 | サービス側吸収(MVP)| n/a(質問数単価に内包)|

### 8.2 80/100/125% 三段階アクション(FR-122)

**判定基準は課金対象ごとの「月次上限件数」**(オーナーが SCR-015-M1 で設定、未設定時はデフォルト推奨値、運営者上書きは SCR-093。§8.5 参照)。80/100/125% は設定上限件数に対する割合で課金対象ごとに独立判定する。表示はすべて「件数(¥円相当)」で行い、円相当 = 単価 × 件数(表示のみ)。

> **無料枠と上限件数の区別**: 「無料枠」は単価が 0 円となる件数(課金の境界、§8.1。超過分は事後課金)。「月次上限件数」はオーナーが暴走コスト防止のために設定する利用の上限(本節の 80/100/125% 判定の基準)。両者は独立で、上限件数は無料枠より大きく設定できる。いずれの契機でも**契約は `active` のまま、ウィジェットの新規質問受付のみを停止**する(契約サスペンションは行わない)。

下表は **80/100/125% アクション**(月次上限件数比、課金対象ごとに独立判定)。質問数 100% 停止は支払方法の有無に関わらず適用される。なお **支払方法未登録の契約が無料枠を超過した場合も FR-136 の支払方法ゲートにより同様にウィジェット質問受付を停止する**(契約 `active` 維持、429。管理者向けは支払方法登録誘導、復帰は支払方法登録)。

| データ種別 | 80% 到達(上限件数比)| 100% 到達(上限件数比)| 125% 到達(上限件数比)|
|---|---|---|---|
| 質問数 | inbox 通知(billing / normal)| **ウィジェット新規質問受付を停止**(429 + `Retry-After`=当月末。エンドユーザーへ連絡先メール誘導、MSG-WIDGET-QUESTION-LIMIT)+ オーナーへ inbox + email(**`critical`**、MSG-SCR-015-LIMIT-REACHED)。契約は `active` のまま。復帰=翌月リセット or SCR-015-M1 で上限引き上げ | 同左(既に停止中)+ 運営者へエスカレーション |
| FAQ 件数 | inbox 通知 | 新規 FAQ 登録を **拒否**(既存編集は可)| 同左 + 運営者へエスカレーション |
| 個別チャット部屋数 | inbox 通知 | 新規部屋作成を **拒否**(既存返信は可)| 同左 + 運営者へエスカレーション |

### 8.3 月次境界

JST 暦月。無料枠は毎月 1 日 00:00 JST にリセット。

### 8.4 通貨 / 税計算

| 項目 | 内容 |
|---|---|
| 通貨 | JPY |
| 税計算 | 日本国内 10% 固定か顧客管理 IF #10 経由か(未決事項 No.1)|

## 9. 請求対象

| 対象 | 課金条件 | 金額算出方法 | 備考 |
|---|---|---|---|
| 月次無料枠超過分(質問数)| 100% 超過 + **支払方法登録済**| (超過件数 × 単価)| **拒否しない**、事後課金 |
| 月次無料枠超過分(FAQ 件数 / チャット部屋)| 100% 超過 + **支払方法登録済**| (超過件数 × 単価)| 拒否(`E-QUOTA-*` 429 + Retry-After)|
| 月次無料枠内 | 課金しない | 0 円 | 支払方法未登録でも利用可 |
| 無料枠超過 + **支払方法未登録** | 課金しない | 0 円 | 契約 `active` 維持、ウィジェット質問受付停止(429、支払方法ゲート FR-136)。支払方法登録で再開 |
| サスペンション中 | 課金しない(月次集計対象外)| 0 円 | - |

## 10. 請求状態

| 請求状態 | 説明 | システム動作 | 関連エラーID |
|---|---|---|---|
| 未請求 | 月次集計前 | - | - |
| 請求済み | 請求書発行済、Stripe 決済中 | - | - |
| 支払い成功 | Stripe 決済成功 | - | - |
| 支払い失敗(Smart Retries 中)| Stripe Smart Retries 中 | - | - |
| **支払い失敗確定**(3 回失敗後)| Smart Retries 終了 | 顧管 IF #1「決済失敗確定」イベント | E-BILL-PAYMENT-FAILED |
| 返金済み | Stripe Credit Note API | - | - |
| 取消済み | 運営者手動 | - | - |

## 11. 決済フロー

| フローID | フロー名 | 概要 | 関連API | 外部サービス |
|---|---|---|---|---|
| BILL-FLOW-001 | 支払い方法登録 | Stripe Setup Intent → 支払方法保存 | `POST /billing/payment-method` | Stripe |
| BILL-FLOW-002 | 初回決済 | 無料枠超過分の月次請求確定時(支払方法登録済の契約)| (Stripe 経由) | Stripe |
| BILL-FLOW-003 | 継続課金(月次)| 月初 1 日 02:00 JST cron(運営者側 §11 正本)| (cron + IF #10) | Stripe |
| BILL-FLOW-004 | 支払い失敗 | Stripe Smart Retries → 3 回失敗 → IF #1 | (IF #1 受信)| Stripe |
| BILL-FLOW-005 | 返金 | Stripe Credit Note API(運営者手動)| (運営者側 API)| Stripe |
| BILL-FLOW-006 | 訂正請求 | Stripe Credit Note API(運営者手動、自動禁止)| (運営者側 API)| Stripe |

### 11.4 Stripe Smart Retries 委譲(基本設計書 §6.6.4)

| 観点 | 仕様 |
|---|---|
| 再課金リトライ | Stripe Smart Retries に委譲、**独自リトライ実装は行わない** |
| 失敗確定 | 最大 3 回失敗後にメイン側へ「決済失敗確定」イベント送信(連携 IF #1)|
| 猶予期間 | 7 日間(顧客管理側からのイベント受信時刻起点)|
| 猶予中の操作 | 管理者ユーザーは管理画面の課金画面・データエクスポート画面・退会画面のみアクセス可、それ以外は 403 |
| 解除条件 | 管理者ユーザーが課金画面から手動で支払方法更新 → 即時再決済 → 成功なら顧客管理側が解除イベント発火 → 連携 IF #1 で `contract_owners.contract_status='active'` |
| 訂正請求 | Stripe Credit Note API 経由、**運営者の手動操作のみ**(自動禁止)|

## 12. 外部決済サービス連携(Stripe)

| 連携先 | 連携内容 | 連携方式 | Webhook 有無 | 備考 |
|---|---|---|---|---|
| Stripe | サブスクリプション / 支払方法 / Smart Retries / Credit Note | REST API + Webhook | あり(IF #10 経由、運営者側 §13 正本)| - |

## 13. Webhook 処理(運営者側 §13 が正本、本書は参照)

メイン側で扱う Webhook 関連処理:

- **IF #1(契約停止イベント)受信**: `contract_owners.contract_status` 更新 + キャッシュ無効化 + セッション処理
- **IF #10 経由で渡される Stripe 関連の通知**

詳細仕様は [02_運営者システム/個別設計書群/11_課金請求設計書.md §13](../../02_運営者システム/個別設計書群/11_課金請求設計書.md) を正本とする。

## 14. 請求書・領収書

| 帳票 | 発行タイミング | 記載項目 | ダウンロード可否 | 保持期間 |
|---|---|---|---|---|
| 請求書 | 月次集計確定時 | 利用量 / 請求金額 / 税 / 期間 | SCR-015 から PDF ダウンロード(R2 署名 URL 有効期限 5 分)| **7 年**(電子帳簿保存法、運営者側正本)|
| 領収書 | 支払い成功時 | 支払金額 / 支払日 | SCR-015 から PDF ダウンロード | 7 年 |

帳票仕様(PDF 出力有無 / 税計算ルール / インボイス制度対応)は未決 ── 未決事項 No.1 参照。

## 15. 課金・契約エラー時の動作

| ケース | システム動作 | ユーザー表示 | メッセージID |
|---|---|---|---|
| 月次上限件数 100% 到達(FAQ / チャット部屋)| 新規操作 429 + `Retry-After`(月末まで)| 「上限に到達しました」+ 課金ダッシュボード誘導 | MSG-QUOTA-* |
| 月次上限件数 100% 到達(質問数) | **ウィジェット新規質問受付を停止**(429 + `Retry-After`=月末)。契約は `active`。エンドユーザーへ連絡先メール誘導 | 管理者:「質問数が上限に達しました。ウィジェットは現在制限中です」/ EU: 連絡先メール誘導 | MSG-SCR-015-LIMIT-REACHED / MSG-WIDGET-QUESTION-LIMIT |
| 月次無料枠 100% 超過(質問数、支払方法登録済)| 課金のみ(超過分は事後課金、ウィジェットは上限件数まで継続)| 「無料枠を超過しました(超過分は次月請求)」 | MSG-QUOTA-QUESTIONS-OVERAGE |
| 契約停止中(`contract_status='suspended'`)| 課金 / 退会 / データエクスポート以外 403 | サスペンション理由 + 再決済誘導 | MSG-BILL-CONTRACT-SUSPENDED |
| 支払い失敗 | 7 日猶予期間カウント開始 + メール通知(**`critical`**)| 「お支払いが確認できませんでした」+ 課金画面誘導 | MSG-BILL-PAYMENT-FAILED |
| 支払い方法未登録のまま無料枠超過(支払方法ゲート)| 契約 `active` 維持、ウィジェット質問受付停止(429 + メール誘導)+ オーナーへ通知(**`critical`**)。管理画面は全機能利用可。支払方法登録で即時再開 | 管理者:「無料枠を超過しました。継続利用にはお支払い方法を登録してください」/ EU: 連絡先メール誘導 | MSG-BILL-PAYMENT-METHOD-REQUIRED / MSG-WIDGET-QUESTION-LIMIT |
| 退会済み契約 | 403、ログイン拒否 | 「アカウントが削除されています」 | MSG-BILL-DELETED |

## 16. 月次集計フロー(§6.2.10 から移管)

| 項目 | 仕様 |
|---|---|
| 実行頻度 | 月初 1 日 02:00 JST(運営者側 §11 正本) |
| 対象期間 | 前月の利用量 |
| 処理内容 | 1. `usage_metering` から月次集計、2. 契約上書き適用、3. `billing_invoices` 作成、4. Stripe 請求作成(IF #10 経由)、5. 通知メール(`high`) |
| 冪等性 | 同一月の二重実行はスキップ(`billing_invoices.unique(owner, period)`)|
| エラー処理 | 個別契約失敗時は他契約継続、DLQ 投入 |
| 監査 | `audit_logs.action='billing.monthly_close'`(retention_class=`billing`) |

## 17. 未決事項

| No | 内容 | 確認先 | 期限 | ステータス |
|---|---|---|---|---|
| 1 | 請求書帳票仕様(PDF 出力有無 / 税計算ルール / インボイス制度対応の項目)| 既存ドキュメントに帳票フォーマットなし | - | 要確認 |
