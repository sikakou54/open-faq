# メール設計書

## 1. 文書概要

### 1.1 目的

本書は **メインシステム(利用者向け FAQ ウィジェット SaaS)が送信する全電子メールの件名・本文・テンプレート変数・送信契機を集中定義する正本** である。実装者・QA・カスタマーサポートが本書の文言と差分なくメール内容を確認できることを保証する。

### 1.2 対象範囲

- システムから自動送信される全メール(本書 §4 のテンプレート ID `TPL-*` 全件)
- 各テンプレートの件名 / プレーンテキスト本文 / HTML 本文(プレースホルダ含む)
- 送信契機(通知 ID `NOTIF-*`)/ 配信先 / 重要度 / 添付の有無 / 失効・リンク有効期限
- メール共通要件(送信元・Reply-To・i18n・サニタイズ・配信信頼性)

**対象外**:

- アプリ内お知らせ受信箱のテンプレート(`inbox` チャネル限定の `NOTIF-CHAT_HOLD_CHECK` のように email を含まないものは [06_メッセージ一覧.md](06_メッセージ一覧.md) §6 を正本とする)
- 個別の文言バリエーション(`MSG-SCR-*` 系の画面文言)
- 運営者システムから送信されるメール(別ドキュメント [../../02_運営者システム/02_基本設計/11_メール設計.md](../../02_運営者システム/02_基本設計/11_メール設計.md))

### 1.3 版数

| 項目 | 値 |
|---|---|
| 版数 | 1.0 |
| 作成日 | 2026-05-19 |
| 更新日 | 2026-05-19 |

### 1.4 関連ドキュメント

- 通知契機・チャネル・配信先 / 重要度: [06_メッセージ一覧.md](06_メッセージ一覧.md) §6.1
- 通知重要度の正本ルール: [../../共有/共有概念.md](../../共有/共有概念.md) §通知重要度 4 値
- セキュリティ要件(配信信頼性 / DMARC 等): [09_セキュリティ設計.md](09_セキュリティ設計.md) §11
- 機能要件: [../01_要件定義/index.md](../01_要件定義/index.md) FR-140〜149b
- エラー設計: [05_エラー設計.md](05_エラー設計.md)(`E-MAIL-*`)
- 利用規約再同意 / 課金イベントとの整合: [10_課金・請求設計.md](10_課金・請求設計.md)

---

## 2. 共通基準

### 2.1 送信元 / Reply-To / 差出人表記

| 項目 | 値 |
|---|---|
| Envelope From | `bounces+<deliveryId>@<service-domain>` (VERP) |
| Header From | `open-faq <noreply@<service-domain>>` 固定 |
| Reply-To | 設定しない(NULL) |
| List-Unsubscribe | マーケティング系 `low` 通知にのみ設定。個別チャット通知には設定しない |
| DMARC / DKIM / SPF | 全テンプレートで pass を必須(NFR-905、09 セキュリティ §11.3)|

「このメールに直接返信せず、お問い合わせ画面または各プロジェクトのお問い合わせ先からご返信ください」のフッタ文言を必ず本文末尾に付与する。

### 2.2 件名規則

- 件名長: 半角換算 70 文字以内(モバイル一覧で折り返しが発生しない目安)
- プレースホルダ `{...}` は本書 §4 各テンプレートの変数表で定義
- `critical` 重要度の利用規約改定通知のみ `【重要】` プレフィックスを付与
- `【open-faq】` のサービス名プレフィックスは付けない(`Header From` で識別可能なため)

### 2.3 本文構造

全テンプレートは以下の 6 ブロックで構成される:

1. 宛名(`{recipient_display_name} 様` / 不明時は `お客様`)
2. 一段落の状況説明(何が起きたか)
3. 行動を促す主要 CTA(リンクまたはボタン)
4. 副次情報(失効日時・チャット ID 等のメタデータ表)
5. 不審な操作の場合の連絡先案内(`critical` テンプレートのみ)
6. 共通フッタ(返信不可注意 / サービス名 / 配信元署名)

### 2.4 文字コード / 言語 / 時刻

- 文字コード: UTF-8
- HTML 版とテキスト版を両方生成し、`multipart/alternative` で送信
- 全文 i18n キー化必須。MVP 対応言語: 日本語(`ja`)
- 本文内の時刻表記は **JST 固定** で `YYYY-MM-DD HH:mm`(秒は含めない)。タイムゾーンを `(JST)` と明示

### 2.5 サニタイズ

- エンドユーザー入力(質問本文・チャット本文)を埋め込む場合は HTML エスケープ + 200 字制限 + 末尾省略記号 `…` を付与
- 引用ブロックは罫線で視覚的に分離し、サービスからの文言と区別

### 2.6 配信失敗時の挙動

- 一時失敗(4xx SMTP / network): 指数バックオフで最大 3 回再送
- 恒久失敗(5xx SMTP / hard bounce): `users.email_bounced_at` を記録、以降同一宛先への自動配信を抑止
- `critical` 重要度の `LOCKOUT_NOTIFY` のみ、bounce 状態でも再送試行を続行(認証関連の到達性最優先)

### 2.7 添付ファイル

| テンプレート | 添付ファイル | 形式 |
|---|---|---|
| `TPL-BILLING_INVOICE_ISSUED` | 請求明細 PDF | `invoice_{year}{month}.pdf`、≤ 1 MB |

その他のテンプレートには添付ファイルを付けない。

---

## 3. テンプレート一覧

メール送信を含む全 23 テンプレートを以下に索引する。詳細(件名 + 本文)は §4 を参照。

| # | テンプレートID | 通知ID | 重要度 | 配信先 | 強制送信 | リンク有効期限 |
|---|---|---|---|---|---|---|
| 1 | TPL-EMAIL_VERIFY | NOTIF-EMAIL_VERIFY | critical | admin(本人) | ◯ | 24h |
| 2 | TPL-PASSWORD_RESET | NOTIF-PASSWORD_RESET | critical | admin(本人) | ◯ | 1h |
| 3 | TPL-ADMIN_USER_REGISTER | NOTIF-ADMIN_USER_REGISTER | critical | admin(招待対象) | ◯ | 7d |
| 4 | TPL-PROJECT_ADMIN_INVITE | NOTIF-PROJECT_ADMIN_INVITE | critical | admin(招待対象) | ◯ | 7d |
| 5 | TPL-PROJECT_CONTACT_VERIFY | (オーナー操作派生) | critical | プロジェクト連絡先 | ◯ | 24h |
| 6 | TPL-LOCKOUT_NOTIFY | NOTIF-LOCKOUT_NOTIFY | critical | ロック本人 + オーナー + 全 PJ 管理者 | ◯ | (リンクなし)|
| 7 | TPL-CHAT_CREATED | NOTIF-CHAT_CREATED | normal | エンドユーザー | ◯ | 30d |
| 8 | TPL-CHAT_REPLY_TO_USER | NOTIF-CHAT_REPLY_TO_USER | normal | エンドユーザー | ◯ | (再入室 URL は保管期間内有効)|
| 9 | TPL-CHAT_REPLY_TO_STAFF | NOTIF-CHAT_REPLY_TO_STAFF | normal | admin | × | (管理画面 URL)|
| 15 | TPL-DELETION_REMINDER | NOTIF-DELETION_REMINDER | high | admin(オーナー)| ◯ | 退会発効日まで |
| 16 | TPL-BILLING_INVOICE_ISSUED | NOTIF-BILLING_INVOICE_ISSUED | high | admin(オーナー)| ◯ | 30d(PDF 再 DL)|
| 17 | TPL-BILLING_PAYMENT_FAILED | NOTIF-BILLING_PAYMENT_FAILED | high | admin(オーナー)| ◯ | (再決済リンク)|
| 18 | TPL-BILLING_SUSPENSION | NOTIF-BILLING_SUSPENSION | critical | admin(オーナー + 全 PJ 管理者)| ◯ | (リンクなし)|
| 19 | TPL-PAYMENT_METHOD_REQUIRED | NOTIF-PAYMENT_METHOD_REQUIRED | critical | admin(オーナー)| ◯ | (課金設定リンク)|
| 22 | TPL-TERMS_REVISION | NOTIF-TERMS_REVISION | critical | admin(オーナー + 全 PJ 管理者)| ◯ | 発効日 + 14d |
| 23 | TPL-SERVICE_ANNOUNCEMENT | NOTIF-SERVICE_ANNOUNCEMENT | normal/high/critical | admin(範囲指定可)| 運営者選択 | 運営者入力 URL |

`NOTIF-CHAT_HOLD_CHECK` はチャネルが `inbox` 限定(メール送信なし)のため本書の対象外。`NOTIF-SYSTEM_NOTICE` は inbox/email 兼用だがサブ契機ごとに動的に件名・本文が組まれるため、共通テンプレートとして §4.24 にメタテンプレートを定義する。

---

## 4. テンプレート詳細

各テンプレートは以下のセクション構造で記載する:

- **発火条件**: どの操作・イベントで送信されるか
- **テンプレート変数**: 本文中の `{...}` 変数
- **件名**: 件名(JA)
- **本文(テキスト版)**: プレーンテキスト本文
- **本文(HTML 版)**: HTML 版の構造(本文要素を箇条書きで定義)
- **備考**: 注意事項・将来拡張

### 4.1 TPL-EMAIL_VERIFY(アカウント登録: メール確認)

**発火条件**: SCR-002 新規登録の「アカウント作成」ボタン押下直後、`access_tokens.purpose='email_verify'` 発行時。

**テンプレート変数**:

| 変数 | 説明 | 例 |
|---|---|---|
| `{recipient_display_name}` | 受信者の表示名(未設定時は空文字)| 佐藤 太郎 |
| `{verify_url}` | 確認 URL(24h 有効)| https://app.example.com/auth/verify?token=... |
| `{expire_at_jst}` | 失効日時(JST、`YYYY-MM-DD HH:mm`)| 2026-05-20 14:30 |
| `{requester_ip}` | リクエスト元 IP(国情報を補助表記)| 203.0.113.42(JP)|

**件名**: `メール確認をお願いします`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq へのご登録ありがとうございます。
以下のリンクからメールアドレスの確認を完了してください。

▶ メールを確認する
{verify_url}

このリンクは {expire_at_jst}(JST) まで有効です。

リクエスト元:
  - IP: {requester_ip}

ご自身でこの操作を行っていない場合は、本メールを破棄してください。第三者が
あなたのメールアドレスで登録を試みた可能性がありますが、リンクをクリックし
ない限り、アカウントは作成されません。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: テキスト版と同等の段落構造 + 「メールを確認する」プライマリボタン(リンク `{verify_url}`)。失効日時とリクエスト元 IP は背景色 `#f5f5f5` のメタデータ表で表示。

**備考**: 失敗(リンク有効期限切れ等)時はログインページで `E-AUTH-EMAIL_VERIFY-EXPIRED` を表示し、再送導線を提供。

---

### 4.2 TPL-PASSWORD_RESET(パスワード再設定)

**発火条件**: SCR-003 パスワード再設定の「再設定リンクを送信する」ボタン押下時、`access_tokens.purpose='password_reset'` 発行時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者の表示名 |
| `{reset_url}` | 再設定 URL(1h 有効)|
| `{expire_at_jst}` | 失効日時(JST)|
| `{requester_ip}` | リクエスト元 IP |
| `{requester_ua_summary}` | User-Agent 要約(ブラウザ名 + OS のみ)|

**件名**: `パスワード再設定のご案内`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq のパスワード再設定リクエストを受け付けました。
以下のリンクから新しいパスワードを設定してください。

▶ パスワードを再設定する
{reset_url}

このリンクは {expire_at_jst}(JST) まで有効です(発行から 1 時間)。

リクエスト元:
  - IP: {requester_ip}
  - 端末: {requester_ua_summary}

ご自身で操作を行っていない場合は、本メールを破棄しアカウントの不正利用を
ご確認ください。心当たりのないリクエストが続く場合はサポートまでご連絡く
ださい。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: プライマリボタン「パスワードを再設定する」+ リクエスト元のメタデータ表 + 警告ボックス(背景色 `#fff3cd`)で「心当たりのないリクエストの場合」案内。

**備考**: 連続失敗ロックアウト(FR-007)発動時は本テンプレートは送信せず `TPL-LOCKOUT_NOTIFY` に切り替わる。

---

### 4.3 TPL-ADMIN_USER_REGISTER(メンバー招待・再送 共通)

**発火条件**: SCR-017-M1 メンバー招待モーダルの「招待メールを送信する」/「招待メールを再送する」ボタン押下時、`access_tokens.purpose='activation'` 発行時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_email}` | 招待対象のメールアドレス(招待時点で `users.name=NULL` のため宛先表示は email を用いる。FR-016e)|
| `{inviter_owner_name}` | 招待元オーナーの表示名 |
| `{inviter_admin_name}` | 招待操作者の表示名(オーナー以外の管理者の場合)|
| `{project_name}` | 招待対象プロジェクト名 |
| `{assigned_role_label}` | 付与ロール表示(`プロジェクト管理者` / `プロジェクトメンバー`)|
| `{activation_url}` | アクティベーション URL(7d 有効、SCR-031 メンバーアカウント有効化ページに着地)|
| `{expire_at_jst}` | 失効日時(JST)|

**件名**: `open-faq の管理者ユーザー登録を完了してください`

**本文(テキスト版)**:

```
{recipient_email} 様

{inviter_owner_name} 様の open-faq アカウントから、{project_name} のメン
バーとして招待を受けました。以下のリンクからお名前(表示名)と初回パスワー
ドを設定し、利用規約・プライバシーポリシーに同意のうえ、アカウント登録を
完了してください。

▶ アカウント登録を完了する
{activation_url}

このリンクは {expire_at_jst}(JST) まで有効です(発行から 7 日)。
失効後に登録する場合は、招待元の管理者へ再送をご依頼ください。

招待内容:
  - プロジェクト: {project_name}
  - 付与ロール: {assigned_role_label}
  - 招待元オーナー: {inviter_owner_name}
  - 招待操作者: {inviter_admin_name}

心当たりがない場合は本メールを破棄してください。リンクをクリックしない限
りアカウントは有効化されません。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 招待内容を背景色 `#eef6ff` のパネルで表示 + プライマリボタン「アカウント登録を完了する」+ 失効日時を強調表示。本文の必須項目に「氏名(表示名)入力 + 初回パスワード設定 + 利用規約・プライバシーポリシー同意」を含める。

**着地ページ**: SCR-031 メンバーアカウント有効化(01_画面設計.md §5.20c)。

**備考**: 再送時は **旧 `access_tokens` を失効させ新トークンを発行** したうえで本テンプレートで再送信(`MSG-SCR-017-M1-TOAST-002` と整合)。本文は新規招待時と同一で、差出人による文脈は本文中の「招待操作者」フィールドで補足する。氏名は招待時点で未登録のため、宛先表示は `{recipient_email}` を用いる。

---

### 4.4 TPL-PROJECT_ADMIN_INVITE(プロジェクト管理者招待)

**発火条件**: SCR-017-M1 メンバー招待モーダルで `role='admin'` を指定して招待を実行した時、招待対象メールアドレスへ送信(FR-015 / FR-015e / FR-030a)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_email}` | 招待対象メールアドレス(表示名は未登録のため使用しない)|
| `{inviter_owner_name}` | 招待元オーナーの表示名 |
| `{project_name}` | 新規作成されたプロジェクト名 |
| `{activation_url}` | アクティベーション URL(7d 有効、SCR-031 メンバーアカウント有効化ページに着地)またはログイン URL(既存アカウントの場合)|
| `{expire_at_jst}` | 失効日時(JST)|

**件名**: `{project_name} のプロジェクト管理者に招待されました`

**本文(テキスト版)**:

```
{recipient_email} 様

{inviter_owner_name} 様から open-faq の {project_name} のプロジェクト管理
者として招待されました。以下のリンクからお名前(表示名)と初回パスワード
を設定し、利用規約・プライバシーポリシーに同意のうえ、アカウントを有効化
してください。

▶ アクティベーションを完了する
{activation_url}

このリンクは {expire_at_jst}(JST) まで有効です(発行から 7 日)。

付与される権限:
  - プロジェクト: {project_name}
  - ロール: プロジェクト管理者(プロジェクトの設定変更、メンバー招待・
    ロール変更、アカウント削除が可能)

すでに open-faq のアカウントをお持ちの場合は、リンク先で同一メールアドレ
スのアカウントに当該プロジェクトの管理者割当が追加されます。

心当たりがない場合は本メールを破棄してください。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: プロジェクト名を大きく表示 + 「付与される権限」をリスト表示 + プライマリボタン「アクティベーションを完了する」。本文の必須項目に「氏名(表示名)入力 + 初回パスワード設定 + 利用規約・プライバシーポリシー同意」を含める。

**着地ページ**: SCR-031 メンバーアカウント有効化(01_画面設計.md §5.20c)。

**備考**: 既存アカウントがある場合(FR-021c 名前解決)は `{activation_url}` がログイン URL になり、ログイン後に当該プロジェクトに `project_users.role='admin'` 行が追加される(SCR-031 はバイパス、氏名は既存値を保持)。

---

### 4.5 TPL-PROJECT_CONTACT_VERIFY(プロジェクト連絡先メール確認)

**発火条件**: SCR-010-M1 プロジェクト設定モーダルで連絡先メールアドレスが新規入力 / 変更された時、ウィジェット上での表示前確認用に送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{project_name}` | プロジェクト名 |
| `{verify_url}` | 確認 URL(24h 有効)|
| `{expire_at_jst}` | 失効日時(JST)|
| `{operator_owner_name}` | 設定操作したオーナーの表示名 |

**件名**: `{project_name} のお問い合わせ先メールアドレスを確認してください`

**本文(テキスト版)**:

```
お客様

{operator_owner_name} 様が open-faq の {project_name} のお問い合わせ先メー
ルアドレスとしてこのアドレスを登録しました。FAQ ウィジェット上でお問い合
わせ先として表示するため、以下のリンクから確認をお願いします。

▶ メールアドレスを確認する
{verify_url}

このリンクは {expire_at_jst}(JST) まで有効です(発行から 24 時間)。

確認が完了するまで、FAQ ウィジェット上にお問い合わせ先は表示されません。

心当たりがない場合は本メールを破棄してください。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: プロジェクト名を強調 + 確認用プライマリボタン + 確認前は非表示である旨を注記。

**着地ページ**: **SCR-032 プロジェクト連絡先メール確認完了**(01_画面設計.md §5.20d、共通領域、未認証可)。トークン検証成功時に `projects.contact_email_verified_at=now()` をセットし、結果(完了 / 期限切れ / 既使用)を表示する。

**備考**: 受信者はオーナーや管理者である必要はなく、第三者(例: サポート窓口担当者の共有メールアドレス)でも構わない。アカウント作成や認証は不要で、トークン保有者のみが本フローを進められる。トークンは `access_tokens.purpose='contact_verify'`(24 時間)、`meta` に `projectId` を JSON 保持(FR-033a)。

---

### 4.6 TPL-LOCKOUT_NOTIFY(ログインロックアウト発動)

**発火条件**: FR-007 連続ログイン失敗閾値到達時。本人 + 同一オーナースコープで `project_users.role='admin' AND valid=1` を 1 件でも保持する全ユーザー(オーナーは作成時に自動で admin 行を保持するため本クエリ単独でオーナー + 全プロジェクト管理者を網羅)へ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者の表示名 |
| `{locked_account_email}` | ロックされたアカウントのメールアドレス |
| `{lockout_started_at_jst}` | ロックアウト開始日時(JST)|
| `{lockout_window_minutes}` | 自動解除までの分数(MVP: 30)|
| `{requester_ip}` | 最後の失敗試行 IP |
| `{requester_geo}` | IP からの推定国・地域(IP geolocation)|
| `{contact_url}` | サポート問い合わせ URL(**サービス提供者が運用する外部サポートページ**、環境変数 `SUPPORT_CONTACT_URL` 等で設定可能)|

**件名**: `【セキュリティ】ログインロックアウトが発動しました`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq のアカウントで連続ログイン失敗を検知し、ロックアウトが発動しまし
た。

  - 対象アカウント: {locked_account_email}
  - ロックアウト開始: {lockout_started_at_jst}(JST)
  - 自動解除: {lockout_window_minutes} 分後
  - 最終試行元: {requester_ip}({requester_geo})

ご自身による試行の場合、自動解除後に正しいパスワードでログインしてくださ
い。パスワードを失念した場合は SCR-003 パスワード再設定をご利用ください。

ご自身による試行でない場合、第三者が認証情報を試行している可能性がありま
す。直ちに以下を実施してください:

  1. パスワードを再設定する
  2. 心当たりのないログイン履歴がないか確認する
  3. 必要に応じて以下からサポートへ連絡する
     {contact_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 警告アイコン + 赤系背景 `#fff5f5` のヘッダ + 「ロック詳細」「ご自身による試行の場合」「不正試行が疑われる場合」の 3 ブロック。

**着地ページ**: `{contact_url}` は **外部サポートページ**(管理画面外、本書 SCR 体系の対象外)。SCR ID は付与しない。

**備考**: 同一インシデントで同一受信者に短時間で複数回送信されないよう、`notification_logs` で 60 分以内の重複配信を抑止。

---

### 4.7 TPL-CHAT_CREATED(個別チャット開始)

**発火条件**: エンドユーザーが FAQ ウィジェットから個別チャットを開始し、`chat_rooms` の新規行作成に成功した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{chat_code}` | チャット ID(公開コード)|
| `{project_name}` | 対象プロジェクト名 |
| `{reentry_url}` | 再入室 URL(30d 有効)|
| `{retention_days}` | 保管日数(30)|

**件名**: `個別チャットを開始しました(チャット ID: {chat_code})`

**本文(テキスト版)**:

```
お客様

{project_name} の個別チャットを開始しました。

  - チャット ID: {chat_code}

担当者からの返信は本メールアドレス宛にお知らせします。
このメールに記載のリンクから、いつでもチャット画面に戻ることができます。

▶ チャット画面に戻る
{reentry_url}

(リンクは {retention_days} 日間有効です。期限を過ぎるとチャット履歴は
自動削除されます。)

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: チャット ID を大きく表示 + 「チャット画面に戻る」プライマリボタン。

---

### 4.8 TPL-CHAT_REPLY_TO_USER(担当者返信通知 → エンドユーザー)

**発火条件**: 管理者(オーナー / プロジェクト管理者 / メンバー)が SCR-033 個別チャット部屋(利用者側)でエンドユーザーに返信を投稿した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{chat_code}` | チャット ID |
| `{project_name}` | プロジェクト名 |
| `{reply_excerpt}` | 返信本文の抜粋(200 字、HTML エスケープ済み)|
| `{reentry_url}` | 再入室 URL |

**件名**: `お問い合わせに返信がありました`

**本文(テキスト版)**:

```
お客様

{project_name} の個別チャット {chat_code} に担当者から返信がありまし
た。

(担当者からの返信)
{reply_excerpt}

続きの内容や追加の質問は、以下のチャット画面でご確認・ご返信ください。

▶ チャット画面を開く
{reentry_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 返信本文を背景色 `#f5f5f5` の引用ブロックで表示 + 「チャット画面を開く」プライマリボタン。

**備考**: 機密情報の漏洩を防ぐため返信本文は **200 字制限 + HTML sanitize** を必須(FR-145 / 共通基準 §2.5)。

---

### 4.9 TPL-CHAT_REPLY_TO_STAFF(エンドユーザー投稿 → 担当者)

**発火条件**: エンドユーザーが個別チャット部屋に新規投稿した時。配信先は当該プロジェクトの通知対象メンバー(プロジェクト関連通知トグル ON のメンバー全員)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者の表示名 |
| `{chat_code}` | チャット ID |
| `{project_name}` | プロジェクト名 |
| `{end_user_post_excerpt}` | エンドユーザー投稿の抜粋(200 字、HTML エスケープ済み)|
| `{chat_room_url}` | 担当者向けチャット画面 URL(SCR-033 個別チャット部屋 deeplink)|

**件名**: `{project_name} の個別チャットに投稿があります({chat_code})`

**本文(テキスト版)**:

```
{recipient_display_name} 様

{project_name} の個別チャット {chat_code} にエンドユーザーから新しい
投稿がありました。

(エンドユーザーからの投稿)
{end_user_post_excerpt}

▶ チャット画面を開く
{chat_room_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 投稿本文を引用ブロックで表示 + プライマリボタン。

**備考**: 強制送信ではない(`×`)。プロジェクト関連通知トグル OFF のメンバーには送信しない。

---

### 4.10 TPL-CHAT_RESOLUTION_CHECK(廃止)

> 個別チャットに利用者待ち・解決確認・自動進行を設けないため、このテンプレートは送信しない。以下は旧仕様の参照記録であり実装対象外。

**発火条件**: 担当者が SCR-033 で「利用者待ち」判定を行い、最終投稿から 7 日経過した時(段階 2)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{inquiry_code}` | 問い合わせ ID |
| `{project_name}` | プロジェクト名 |
| `{reentry_url}` | 再入室 URL |
| `{auto_advance_at_jst}` | 段階 4 への自動移行日時(JST、+7d)|

**件名**: `お問い合わせ({inquiry_code})の解決状況を確認させてください`

**本文(テキスト版)**:

```
お客様

{project_name} のお問い合わせ {inquiry_code} について、その後ご不明点はご
ざいませんでしょうか?

担当者からの最終回答以降、追加のご投稿がない状態が続いています。
解決済みの場合は本メールへの操作は不要です。

追加で確認したい内容がある場合は、以下のチャット画面からご返信ください。

▶ チャット画面を開く
{reentry_url}

{auto_advance_at_jst}(JST) までにご投稿がない場合、最終確認メールをお送り
した上で、自動的にお問い合わせをクローズいたします。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 段階表示(段階 2 / 4 で自動クローズ)+ プライマリボタン。

---

### 4.11 TPL-CHAT_FINAL_CONFIRM(廃止)

> 個別チャットに最終確認・自動クローズを設けないため、このテンプレートは送信しない。以下は旧仕様の参照記録であり実装対象外。

**発火条件**: 段階 2 から 7 日経過してもエンドユーザー投稿がない場合(段階 4)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{inquiry_code}` | 問い合わせ ID |
| `{project_name}` | プロジェクト名 |
| `{reentry_url}` | 再入室 URL |
| `{auto_close_at_jst}` | 自動クローズ日時(JST、+3d)|

**件名**: `【最終確認】お問い合わせ({inquiry_code})をクローズいたします`

**本文(テキスト版)**:

```
お客様

{project_name} のお問い合わせ {inquiry_code} について、現在も追加のご投稿
を確認できていません。

このまま {auto_close_at_jst}(JST) までにご返信がない場合、本お問い合わせ
は自動的にクローズいたします。

再度ご相談がある場合は、以下のチャット画面からご返信いただくか、改めて新
しいお問い合わせをお送りください。

▶ チャット画面を開く
{reentry_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 警告色背景 `#fff3cd` のヘッダ + 自動クローズ日時を強調表示。

---

### 4.12 TPL-CHAT_AUTO_CLOSED(廃止)

> 個別チャットに自動クローズを設けないため、このテンプレートは送信しない。以下は旧仕様の参照記録であり実装対象外。

**発火条件**: 段階 4 から 3 日経過しても投稿がない場合(段階 6、自動クローズ実行)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{inquiry_code}` | 問い合わせ ID |
| `{project_name}` | プロジェクト名 |
| `{closed_at_jst}` | クローズ日時(JST)|
| `{reopen_form_url}` | 再開フォーム URL(**ウィジェット設置サイト上の新規問い合わせ作成 UI**、具体的な URL は当該プロジェクトのウィジェット埋め込み済みページに依存)|

**件名**: `お問い合わせ {inquiry_code} を自動的にクローズしました`

**本文(テキスト版)**:

```
お客様

{project_name} のお問い合わせ {inquiry_code} を {closed_at_jst}(JST) 時
点で自動的にクローズいたしました。

クローズ理由: 段階 4 の最終確認メール送信後、規定期間内に追加のご投稿が
ありませんでした。

再度同じ件についてご相談がある場合は、新しいお問い合わせをお送りください。

▶ 新しくお問い合わせをする
{reopen_form_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: クローズ理由を強調 + 再開フォームへのプライマリボタン。

**着地ページ**: `{reopen_form_url}` は **ウィジェット設置サイト**(管理画面外、本書 SCR 体系の対象外)。当該プロジェクトのウィジェット埋め込み済みページ URL を指す。

---

### 4.13 TPL-CHAT_CLOSED(廃止)

> 個別チャットに手動終了を設けないため、このテンプレートは送信しない。以下は旧仕様の参照記録であり実装対象外。

**発火条件**: 担当者が SCR-033 で「チャットを終了する」を実行した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{inquiry_code}` | 問い合わせ ID |
| `{project_name}` | プロジェクト名 |
| `{closed_at_jst}` | クローズ日時(JST)|
| `{close_note_excerpt}` | 担当者入力のクローズメモ(200 字、任意。空欄時は当該行を出力しない)|
| `{reopen_form_url}` | 再開フォーム URL(**ウィジェット設置サイト上の新規問い合わせ作成 UI**、具体的な URL は当該プロジェクトのウィジェット埋め込み済みページに依存)|

**件名**: `お問い合わせ {inquiry_code} を終了しました`

**本文(テキスト版)**:

```
お客様

{project_name} のお問い合わせ {inquiry_code} を {closed_at_jst}(JST) 時
点で担当者が終了いたしました。

担当者からのメモ:
{close_note_excerpt}

再度ご相談がある場合は、新しいお問い合わせをお送りください。

▶ 新しくお問い合わせをする
{reopen_form_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: クローズ理由が担当者メモのみであることを明示 + 再開フォームへのプライマリボタン。

**着地ページ**: `{reopen_form_url}` は **ウィジェット設置サイト**(管理画面外、本書 SCR 体系の対象外)。当該プロジェクトのウィジェット埋め込み済みページ URL を指す。

---

### 4.14 TPL-FAQ_REGISTERED(廃止)

> 未解決質問はエンドユーザー連絡先を持たず、FAQ 登録進捗の管理に限定するため、このテンプレートは送信しない。以下は旧仕様の参照記録であり実装対象外。

**発火条件**: SCR-012 で FAQ が公開され、当該 FAQ が「過去の問い合わせから昇格」した場合に、関連 `inquiries` のエンドユーザーへ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{inquiry_code}` | 元の問い合わせ ID |
| `{project_name}` | プロジェクト名 |
| `{faq_title_excerpt}` | FAQ の質問抜粋(100 字)|
| `{widget_url}` | 当該プロジェクトのウィジェット URL |

**件名**: `{project_name}: いただいたご質問が FAQ に追加されました`

**本文(テキスト版)**:

```
お客様

過去にお寄せいただいたお問い合わせ({inquiry_code})を参考に、FAQ を更新
しました。同じご質問が他のお客様にも役立つ内容として共有させていただきま
した。

追加された FAQ:
  {faq_title_excerpt}

最新の FAQ はこちらからご確認いただけます。

▶ FAQ を見る
{widget_url}

このメールは {project_name} の FAQ 改善通知です。今後同様の通知を希望さ
れない場合は、本メール下部の配信停止リンクからお手続きください。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: FAQ 質問抜粋をハイライト + 「FAQ を見る」セカンダリボタン + List-Unsubscribe ヘッダに対応する配信停止リンク。

**備考**: `low` 重要度のため強制送信なし。`List-Unsubscribe` ヘッダで RFC 8058 ワンクリック配信停止を実装する。

---

### 4.15 TPL-DELETION_REMINDER(退会猶予期間案内)

**発火条件**: SCR-024 退会申請後、退会発効日(申請当月末)の 7 日前 / 当日 / 完全削除 3 日前の 3 回送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | オーナー表示名 |
| `{withdrawal_phase}` | フェーズ(`pre7d` / `effective` / `pre3d_purge`)|
| `{service_stop_at_jst}` | サービス停止日時(申請当月末日 23:59 JST)|
| `{data_purge_at_jst}` | 完全削除日時(停止 + 30 日)|
| `{export_url}` | データエクスポート画面 URL |
| `{cancel_withdrawal_url}` | 退会取消 URL(発効前のみ)|

**件名**:

- pre7d: `退会まで残り 7 日: データのエクスポートをご確認ください`
- effective: `退会が発効しました: データエクスポートは 30 日以内にお願いします`
- pre3d_purge: `【最終案内】データ完全削除まで残り 3 日`

**本文(テキスト版・pre7d)**:

```
{recipient_display_name} 様

open-faq へのご退会申請ありがとうございました。
{service_stop_at_jst}(JST) にサービス利用が停止されます。

データのエクスポートをご希望の場合は、停止前または停止後 30 日以内に以下
の画面から実施してください。

▶ データをエクスポートする
{export_url}

退会を取り消す場合は発効日まで以下のリンクからキャンセル可能です。

▶ 退会を取り消す
{cancel_withdrawal_url}

データ完全削除予定日: {data_purge_at_jst}(JST、サービス停止から 30 日)
完全削除後は復元できません。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: フェーズに応じてヘッダ色を変える(pre7d: 黄、effective: 橙、pre3d_purge: 赤)+ 退会取消リンクは `pre7d` フェーズのみ表示 + データエクスポートボタンは 3 フェーズ共通。

**備考**: フェーズごとに件名・本文を分岐させる単一テンプレート。`{withdrawal_phase}` で本文セクションの出し分けを行う。

---

### 4.16 TPL-BILLING_INVOICE_ISSUED(月次請求確定通知)

**発火条件**: FR-148 月次請求確定バッチ完了時、当該契約のオーナーへ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | オーナー表示名 |
| `{billing_period_label}` | 対象期間(`2026 年 4 月`)|
| `{invoice_amount_jpy}` | 請求金額(¥1,234)|
| `{question_count}` | 質問数 |
| `{chat_count}` | チャット数 |
| `{overage_amount_jpy}` | 上限超過分(¥0 の場合は記載省略)|
| `{invoice_pdf_url}` | 明細 PDF DL URL(30d 有効)|
| `{billing_screen_url}` | SCR-015 課金部 deeplink |

**件名**: `{billing_period_label} の請求書が発行されました(¥{invoice_amount_jpy})`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq の {billing_period_label} ご利用分の請求書を発行しました。

  - 請求金額: ¥{invoice_amount_jpy}
  - 対象期間: {billing_period_label}
  - 質問数: {question_count}
  - チャット数: {chat_count}
  - 上限超過分: ¥{overage_amount_jpy}

明細 PDF を添付しています。また以下からも DL いただけます(30 日間有効)。

▶ 請求明細 PDF を開く
{invoice_pdf_url}

請求書一覧および過去の請求は以下からご確認いただけます。

▶ 請求画面を開く
{billing_screen_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 請求サマリ表 + プライマリボタン「請求画面を開く」+ PDF DL セカンダリボタン。

**備考**: PDF は §2.7 のとおり必ず添付。`{overage_amount_jpy}` が `0` の場合は当該行を出力しない。

---

### 4.17 TPL-BILLING_PAYMENT_FAILED(支払い失敗通知)

**発火条件**: Stripe Webhook で支払い失敗(`invoice.payment_failed`)を受信、Smart Retries が継続中の場合に送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | オーナー表示名 |
| `{billing_period_label}` | 対象期間 |
| `{invoice_amount_jpy}` | 請求金額 |
| `{failure_reason}` | Stripe 失敗理由(`card_declined` 等の人間可読版)|
| `{retry_count}` | 再試行回数(1〜3)|
| `{next_retry_at_jst}` | 次回再試行日時(JST、最終再試行時は「再試行なし」)|
| `{payment_method_url}` | SCR-015 課金部の支払い方法変更 URL |
| `{suspension_at_jst}` | サスペンション開始予定日時(最終再試行失敗時)|

**件名**: `【重要】請求のお支払いに失敗しました({billing_period_label})`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq {billing_period_label} 分(¥{invoice_amount_jpy})のお支払いに失敗
しました。

  - 失敗理由: {failure_reason}
  - 再試行回数: {retry_count} / 3
  - 次回再試行: {next_retry_at_jst}(JST)
  - サスペンション予定: {suspension_at_jst}(JST)

支払い方法の更新で多くの場合解決できます。以下から速やかにご確認・ご更新
ください。

▶ 支払い方法を更新する
{payment_method_url}

最終再試行も失敗した場合、サスペンションが発動しサービス利用が制限されま
すのでご注意ください。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 警告色背景 + 再試行回数を視覚的に表示 + プライマリボタン。

**備考**: Stripe Smart Retries のスケジュール(発生から 3 / 5 / 7 日後)に従い最大 3 回送信。失敗理由テキストは英→日変換マップを用意。

---

### 4.18 TPL-BILLING_SUSPENSION(サスペンション開始/解除)

**発火条件**: Stripe Smart Retries 最終失敗時のサスペンション開始、または支払い完了時の解除。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{suspension_event}` | `start` / `release` |
| `{suspension_at_jst}` | サスペンション開始日時(JST、`start` 時)|
| `{released_at_jst}` | サスペンション解除日時(JST、`release` 時)|
| `{billing_period_label}` | 対象期間 |
| `{invoice_amount_jpy}` | 請求金額(`start` 時)|
| `{payment_method_url}` | 支払い方法変更 URL(`start` 時)|

**件名**:

- start: `【重要】サービス利用が停止されました(請求未払い)`
- release: `サービス利用が再開されました`

**本文(テキスト版・start)**:

```
{recipient_display_name} 様

{billing_period_label} 分(¥{invoice_amount_jpy})のお支払いが完了しなかっ
たため、{suspension_at_jst}(JST) より open-faq の管理画面および FAQ ウィ
ジェットの利用を停止しました。

サービスを再開するには支払い方法を更新し、未払い分を決済してください。

▶ 支払い方法を更新する
{payment_method_url}

ご不明な点はサポートまでご連絡ください。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(テキスト版・release)**:

```
{recipient_display_name} 様

{released_at_jst}(JST) にサービス利用が再開されました。
ご対応ありがとうございました。引き続きどうぞよろしくお願いいたします。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: `start` は赤系警告 + 停止範囲リスト、`release` は緑系 + 簡潔な解除通知。

**備考**: オーナー + 全プロジェクト管理者へ配信(`critical`)。

---

### 4.19 TPL-PAYMENT_METHOD_REQUIRED(無料枠超過・支払方法登録要請通知)

**発火条件**: 支払方法未登録の契約の **いずれかのプロジェクト** が当該プロジェクトの月次無料枠(質問数 / FAQ 件数 / 個別チャット部屋数のいずれか)を超過し、支払方法ゲート(FR-136)により当該プロジェクトのウィジェットの新規質問受付を停止した時点で送信(`critical` 重要度、強制送信)。**契約サスペンションは行わず、管理画面は引き続き全機能利用可能**。無料枠判定はプロジェクト単位、支払方法は契約単位。配信先はオーナー + 当該プロジェクトの管理者。あわせて無料枠 80% 到達時に予告通知を送る場合は `NOTIF-SYSTEM_NOTICE` のサブ契機を用いる。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名(オーナー / 当該 PJ 管理者)|
| `{project_name}` | 無料枠を超過したプロジェクト名 |
| `{exceeded_resource_label}` | 超過した無料枠の種別(`質問数` / `FAQ 件数` / `個別チャット部屋数`)|
| `{stopped_at_jst}` | ウィジェット質問受付停止日時(JST)|
| `{billing_setup_url}` | 課金設定 URL(SCR-015 課金部)|

**件名**: `【重要】{project_name} が無料枠を超過しました — お支払い方法の登録が必要です`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq のプロジェクト「{project_name}」の月次無料枠({exceeded_resource_label})を超過しました。
お支払い方法が未登録のため、{stopped_at_jst}(JST) より当該プロジェクトのウィジェットの
新規質問受付を停止しています(管理画面は引き続きご利用いただけます)。

ウィジェットの質問受付を再開するには、お支払い方法を登録してください。
登録後ただちに再開し、超過分は事後課金となります。

▶ 支払い方法を登録する
{billing_setup_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 超過種別カード + ウィジェット停止の案内 + プライマリボタン(支払い方法を登録する)。

---

### 4.22 TPL-TERMS_REVISION(利用規約改定告知)

**発火条件**: FR-148b 利用規約改定発効日の 30 日前に送信。オーナー + 全プロジェクト管理者へ配信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{effective_at_jst}` | 改定発効日時(JST)|
| `{summary_text}` | 改定要約(運営者入力、HTML sanitize 済)|
| `{new_terms_url}` | 新規約 URL |
| `{consent_deadline_jst}` | 同意期限(発効日 + 14d、JST)|
| `{consent_screen_url}` | SCR-025 規約再同意割込み deeplink |

**件名**: `【重要】利用規約改定のお知らせ(発効日: {effective_at_jst})`

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq の利用規約を {effective_at_jst}(JST) より改定いたします。

改定要約:
{summary_text}

新しい利用規約全文は以下をご確認ください。

▶ 新しい利用規約を読む
{new_terms_url}

ご利用継続のためには、{consent_deadline_jst}(JST、発効日 + 14 日) までに
管理画面で再同意をお願いいたします。期限を過ぎると一部機能の利用が制限さ
れる場合があります。

▶ 管理画面で再同意する
{consent_screen_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: 改定要約をパネル表示 + 「新規約を読む」「再同意する」の 2 プライマリボタン。`critical` 重要度のため強制送信。

---

### 4.23 TPL-SERVICE_ANNOUNCEMENT(運営からのお知らせ)

**発火条件**: 連携 IF #7 で運営者システムが配信トリガーした時。重要度 `normal` / `high` / `critical` は配信時に運営者が選択。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{announcement_title}` | お知らせタイトル(運営者入力)|
| `{announcement_body_html}` | お知らせ本文(運営者入力、HTML sanitize 済)|
| `{operator_signature}` | 配信元署名(`open-faq 運営事務局`)|

**件名**: `{announcement_title}`(運営者入力。`critical` の場合は `【重要】` プレフィックスを自動付与)

**本文(テキスト版)**:

```
{recipient_display_name} 様

open-faq 運営事務局よりお知らせいたします。

{announcement_body_html}

──────────────────────────────────────────────
このメールに直接返信しないでください。
{operator_signature}
```

**本文(HTML 版)**: 運営者入力本文は HTML 版でリッチテキスト(リスト・太字・リンクのみ許可)で表示。

**備考**: 運営者システム側 SCR-API_KEY-ANNOUNCE 等の配信トリガー画面が正本(別ドキュメント)。本書ではメインシステム側の受信時表現のみを定義する。

---

### 4.24 TPL-SYSTEM_NOTICE(サブ契機ごとの定型文 = メタテンプレート)

**発火条件**: 上限接近・超過、契約上書き反映等の運用系イベント(FR-149a)。サブ契機ごとに別 i18n キーを持つ。

**テンプレート変数(共通)**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{event_type}` | サブ契機名(`quota_warning_80` / `quota_exceeded` / `operator_override_applied` 等)|
| `{event_at_jst}` | 発生日時(JST)|
| `{related_resource}` | 関連リソース名(プロジェクト名)。上限・無料枠系はプロジェクト単位 |
| `{action_url}` | 対応画面 URL(上限系は SCR-036 上限管理 / その他 SCR-015 / SCR-028 等)|

**件名**: サブ契機ごとに以下のマッピング(上限・無料枠系はプロジェクト名を含める):

| event_type | 件名 |
|---|---|
| quota_warning_80 | {related_resource}: 当月の質問数が上限の 80% に達しました |
| quota_exceeded | {related_resource}: 当月の質問数が上限を超過しました |
| operator_override_applied | 運営者によりプロジェクト上限が更新されました |

> 上限・無料枠系イベントの配信先はオーナー + 当該プロジェクトの管理者(プロジェクト単位、§6 通知契機)。

**本文(テキスト版・テンプレート骨格)**:

```
{recipient_display_name} 様

(event_type 別の定型文。例: quota_warning_80)
{event_at_jst}(JST) 時点で、プロジェクト「{related_resource}」の当月の質問数が
設定上限件数の 80% に達しました。

  - 対象: {related_resource}
  - 発生日時: {event_at_jst}(JST)

詳細・対応方法は以下からご確認ください。

▶ 詳細を見る
{action_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq <noreply@<service-domain>>
```

**本文(HTML 版)**: サブ契機ごとに色を変える(警告系: 黄、超過系: 赤、情報系: 青)。

**備考**: 各サブ契機は本書 §5.2 「将来追加」表で管理する。新規イベント追加時は本書 + `06_メッセージ一覧.md` §6.1 + i18n キー定義の 3 か所を同期更新する。

---

## 5. 配信運用

### 5.1 配信先解決ロジック

| 通知 ID | 配信先解決 |
|---|---|
| TPL-EMAIL_VERIFY / PASSWORD_RESET | 操作者本人のメールアドレス(セッションから)|
| TPL-ADMIN_USER_REGISTER / PROJECT_ADMIN_INVITE | 招待時に入力されたメールアドレス(`access_tokens.target_email`)|
| TPL-PROJECT_CONTACT_VERIFY | プロジェクト連絡先メールアドレス |
| TPL-LOCKOUT_NOTIFY | ロック対象者 + 同一オーナースコープで `project_users.role='admin' AND valid=1` を 1 件でも保持する全ユーザー(オーナー + 全プロジェクト管理者を網羅)|
| TPL-CHAT_CREATED / CHAT_REPLY_TO_USER | チャット開始時に登録したエンドユーザーのメールアドレス(`chat_rooms.end_user_id` → `end_users.email_encrypted`)|
| TPL-CHAT_REPLY_TO_STAFF | 当該プロジェクトの「チャット応対通知」トグル ON のメンバー全員 + オーナー |
| TPL-DELETION_REMINDER / PAYMENT_METHOD_REQUIRED / BILLING_INVOICE_ISSUED / BILLING_PAYMENT_FAILED | 契約のオーナー(`contract_owners` 行が存在するユーザー)のみ |
| TPL-BILLING_SUSPENSION / TERMS_REVISION | オーナー + 同一オーナースコープ配下の全プロジェクト管理者 |
| TPL-SERVICE_ANNOUNCEMENT | 配信時に運営者が指定する範囲(全契約 / 単一契約 / 特定プロジェクト)|
| TPL-SYSTEM_NOTICE | サブ契機ごとに定義(主にオーナー + 関連プロジェクト管理者)|

### 5.2 重要度別の強制送信ルール(共有概念正本)

- `critical`: 受信オプトアウト不可、必ずメール送信。`TPL-EMAIL_VERIFY` / `PASSWORD_RESET` / `ADMIN_USER_REGISTER` / `PROJECT_ADMIN_INVITE` / `PROJECT_CONTACT_VERIFY` / `LOCKOUT_NOTIFY` / `BILLING_SUSPENSION` / `PAYMENT_METHOD_REQUIRED` / `TERMS_REVISION` / `SERVICE_ANNOUNCEMENT`(`critical` 選択時)
- `high`: 強制送信(オプトアウト不可、ただし将来的にユーザー単位で停止可能化を検討。MVP では強制)
- `normal`: 受信オプトアウト可能(プロジェクト関連通知トグルで一括制御)
- `low`: 個別オプトアウト + `List-Unsubscribe` ヘッダで RFC 8058 対応

### 5.3 重複配信抑止

| 抑止対象 | 抑止窓 | 実装方針 |
|---|---|---|
| TPL-LOCKOUT_NOTIFY 同一受信者 | 60 分 | `notification_logs(user_id, template_id, sent_at)` 検索 |
| TPL-CHAT_REPLY_TO_STAFF 同一スタッフ × 同一 chat room | 5 分 | スタッフが連続投稿チェック中の場合をまとめる |
| TPL-BILLING_PAYMENT_FAILED 同一 invoice | Stripe Smart Retries 1 回につき 1 通 | `invoice_id` で重複検知 |

### 5.4 配信ログ

- 全送信は `notification_logs` に行を記録(03 テーブル設計参照)
- 列: `id` / `user_id` / `template_id` / `subject` / `recipient_email` / `sent_at` / `delivery_status`(`queued` / `sent` / `bounced` / `failed`)/ `provider_message_id` / `error_text`
- 保持期間: 90 日(NFR-704 一般ログ準拠)

### 5.5 テスト送信

- 開発・ステージング環境では `MAIL_SAFE_SINK` 環境変数で全宛先を `dev-mail-sink@<internal-domain>` に強制リダイレクト
- 件名にプレフィックス `[STAGING]` / `[DEV]` を自動付与

---

## 6. テンプレート開発・運用フロー

### 6.1 新規テンプレート追加手順

1. 通知契機を [06_メッセージ一覧.md](06_メッセージ一覧.md) §6.1 に追加(NOTIF-* 行)
2. 本書 §3 索引 + §4 詳細にテンプレート行を追加
3. テンプレート変数を確定し i18n キーファイル(`locales/ja/email_templates.yml`)に追加
4. HTML / テキスト両版を実装し、ステージング `MAIL_SAFE_SINK` で目視確認
5. `check-spec-sync.sh` を実行し SC-001〜SC-011 全パスを確認

### 6.2 既存テンプレート変更手順

- 軽微な文言修正(誤字 / 表現変更): 本書 §4 のみ更新 + 変更履歴に記載
- 件名変更 / 主要 CTA 変更: 本書 §4 + 06 メッセージ一覧 §6 + ステージング再送テスト
- 配信先・重要度変更: 本書 §5.1 / §5.2 + 06 メッセージ一覧 §6.1 + 共有概念.md の整合確認
