# DD01: アカウント・ユーザー管理

## 0. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | DD01: アカウント・ユーザー管理 |
| 詳細設計ID | DD01 |
| 対象システム | FAQ AI ウィジェット SaaS / メインシステム |
| 関連機能ID | FR-001〜022 / FR-325〜330(URL規約 / Onboarding / 情報設計) |
| 作成日 | 2026-05-17 |
| 版数 | v1.2 |
| ステータス | 承認済 |

## 1. 対象範囲

| 種別 | ID | 名称 |
|---|---|---|
| 機能 | FR-001〜009 | 新規登録 / メール確認 / パスワード再設定 / ログイン / ログアウト / 再認証 / ロックアウト |
| 機能 | FR-015〜022 | 管理者ユーザー登録・停止・削除・有効化 / メール再送 / 退会 |
| 機能 | FR-011 | 利用規約再同意フロー |
| 画面 | SCR-001 | ログイン |
| 画面 | SCR-002 | 新規登録 |
| 画面 | SCR-003 | パスワード再設定 |
| 画面 | SCR-017 | メンバー(プロジェクト) |
| 画面 | SCR-017-M1 | メンバー招待 / 編集モーダル(招待モードは氏名フィールドなし、編集モードは氏名 read-only)|
| 画面 | SCR-026 | 契約概要(OnboardingChecklist + 利用中のプロジェクト) |
| 画面 | SCR-028 | 個人設定 |
| 画面 | SCR-039 | 契約メンバー |
| 画面 | SCR-041 | 契約設定 |
| 画面 | **SCR-031** | **メンバーアカウント有効化(招待リンク先、氏名 + 初回パスワード + 規約同意、未認証画面)** |
| 機能 | FR-325〜327 | URL ルーティング 4 系列規約 / 初回セットアップガイド / 契約共通項目の影響範囲表示 |
| API | `/auth/*` `/admin-users/*` `/me/email-verification/*` `/me/terms*` `/me/onboarding-status` `/auth/invitations/{token}/preview` `/auth/invitations/{token}/activate` | 認証認可・管理者ユーザー操作・規約同意・オンボーディング状態・招待トークン検証 + メンバーアカウント有効化(SCR-031)|
| テーブル | `users` `contract_owners` `project_users` `sessions` `access_tokens` | アカウント基盤(認証情報 = `users`、オーナー専有属性 = `contract_owners`、プロジェクト別ロール `admin` / `member` = `project_users.role`)|

## 2. 収録ロジック・対応章

| 元章 | 元タイトル | 概要 |
|---|---|---|
| §4 | 利用者・権限詳細設計 | 権限設計書 / 認証認可設計書への参照（正本は基本設計） |
| §6 SCR-001〜003,017 | 画面詳細設計 | ログイン / 新規登録 / パスワード再設定 / 管理者ユーザー一覧（正本は基本設計） |
| §7 | 機能詳細設計（アカウント・ユーザー機能） | 認証フロー / 管理者ユーザー管理 / 規約同意（正本は基本設計） |
| §3.3.1 | `worker-main-api` モジュール構成 | `routes/auth.ts` `routes/admin-users.ts` `routes/email-verification.ts` `routes/terms.ts` |

## 3. 詳細設計本文

### 3.1 利用者・権限詳細設計（参照のみ）

ユーザー種別、3 ロール(オーナー / プロジェクト管理者 / メンバー)、画面別権限マトリクス、認証フロー、Principal 構築は [../02_基本設計/04_権限設計.md](../02_基本設計/04_権限設計.md) および [../02_基本設計/08_認証・認可設計.md](../02_基本設計/08_認証・認可設計.md) を正本とする。

利用者側アカウントはオーナーとメンバーに分かれ、メンバーはプロジェクトごとに `admin` / `member` を持つ。SCR-017-M1の「プロジェクトから外す」は対象 `project_users` 行だけを論理削除する。契約全体のアカウント削除はオーナーがSCR-039から `DELETE /owner/members/{userId}` を実行し、`users.valid=0` と全割当・全セッション・未使用招待を同一トランザクションで失効する。

**個別チャット担当者の付け替え(FR-086a〜b 連動)**: アカウント論理削除(`users.valid=0`)と同一トランザクション内で、当該ユーザーが担当する個別チャット部屋(`chat_rooms.assignee_user_id` = 削除対象、`assignee_user_id` は NOT NULL)を、当該プロジェクトのデフォルト担当者(`projects.default_assignee_user_id`、有効な場合)→ 未設定 / 無効ならオーナーへ付け替える。あわせて当該ユーザーを指す `projects.default_assignee_user_id` を `NULL` 化する(以後の新規チャットはオーナーへフォールバック)。解決ロジックは [DD06_個別チャット.md](DD06_個別チャット.md) §3.4a `resolveInitialAssignee` を再利用し、「担当者未割当の部屋を作らない」不変条件を維持する。

### 3.2 状態詳細設計（参照のみ）

契約状態（`contract_owners.contract_status`: `active` / `suspended` / `deleted_pending` / `deleted`）の全状態遷移は [../02_基本設計/03_テーブル設計.md](../02_基本設計/03_テーブル設計.md) §4.5.5 を正本とする。

### 3.3 画面詳細設計（参照のみ）

SCR-001〜003 / SCR-017 / SCR-026 / SCR-028 / SCR-039 / SCR-041 の表示・入力・操作・メッセージIDは基本設計を正本とする。

### 3.4 機能詳細設計（参照のみ）

機能別のロジック・データ・API 連動は [../02_基本設計/02_API設計.md](../02_基本設計/02_API設計.md) および [../02_基本設計/03_テーブル設計.md](../02_基本設計/03_テーブル設計.md) を正本とする。本書では実装モジュール構成のみを扱う。

### 3.5 実装モジュール構成

`app/workers/main-api/src/` 配下に以下のルート / ハンドラ / リポジトリを配置する。

```
src/
├── routes/
│   ├── auth.ts               # POST /auth/login / POST /auth/logout / POST /auth/password-reset
│   ├── admin-users.ts         # GET/POST /admin-users/*（管理者ユーザー CRUD）
│   ├── email-verification.ts # POST /me/email-verification/{verify,resend}
│   ├── invitations.ts        # POST /auth/invitations/{token}/preview / POST /auth/invitations/{token}/activate（SCR-031 メンバーアカウント有効化、未認証可、Turnstile 必須）
│   └── legal.ts              # GET /terms/current / POST /terms/agree(利用規約)/ GET /privacy/current / POST /privacy/agree(プライバシーポリシー)
├── handlers/                 # 上記 routes と 1:1
├── domain/
│   └── contract-status.ts    # contract_owners.contract_status 遷移ガード（active → suspended → deleted_pending → deleted）
├── repository/
│   ├── users.ts              # users(認証情報) CRUD
│   ├── contract-owners.ts    # contract_owners(オーナー専有属性 = 契約状態 / data_deletion_mode) CRUD
│   ├── project-users.ts      # project_users(プロジェクト割当 + role 列含む) CRUD
│   ├── sessions.ts
│   └── access-tokens.ts
├── middleware/
│   ├── authenticate.ts       # Cookie + CSRF
│   ├── require-active-contract.ts
│   ├── require-terms-agreement.ts
│   ├── require-reauth.ts     # 重要操作再認証
│   └── csrf.ts
└── lib/
    ├── argon2id.ts           # hashPassword / verifyPassword
    └── token.ts              # email_verify / password_reset / activation トークン
```

### 3.6 関連する横断設計

#### 3.6.1 トークン発行・検証

メール確認・パスワードリセット・管理者ユーザー有効化（activation）の各トークンは [DD08_トークン発行・検証.md](DD08_トークン発行・検証.md) の HMAC-SHA256 保存方式を利用する。一回限りトークンは `consumeToken()` を検証直後に必ず呼ぶ。

#### 3.6.2 認可ヘルパ

`requireTenant` / `requireProject` / `requireRole` の各ヘルパは [DD09_認可ヘルパ.md](DD09_認可ヘルパ.md) を利用する。

#### 3.6.3 監査ログ書込

`auth.signup` / `auth.login_success` / `auth.login_failure` / `auth.logout` / `auth.password_reset` / `auth.re_auth.success` / `auth.ip_blocked` / `auth.terms_agree` などの action コードはすべて [DD10_監査ログ書込・完全性検証.md](DD10_監査ログ書込・完全性検証.md) の `writeAudit()` で書込み、`retention_class=general` とする。

#### 3.6.4 暗号化・鍵管理

`email` / `phone` などの PII はオーナー派生鍵で AES-256-GCM 暗号化し、検索用に HMAC-SHA256 列（`email_hmac` 等）を併設する。詳細は [DD11_暗号化・鍵管理.md](DD11_暗号化・鍵管理.md) を参照。

### 3.7 オーナー / メンバー / プロジェクト割当

- **オーナー判定** = `contract_owners` 行(`valid=1`)を持つユーザー。`contract_owners.user_id` が当該オーナーの `users.id` を直接参照する(`is_owner` カラムも `owner_account_id` 自己参照も持たない)
- メンバーは `contract_owners` 行を持たず、`project_users.contract_owner_user_id` 経由で所属契約に紐付く
- メンバーの権限は `project_users.role`(`admin` / `member`)とプロジェクト割当の組合せで管理(個別権限フラグ方式は MVP で廃止)
- オーナーもプロジェクト作成時に `project_users(role='admin', valid=1)` 行を自動取得し、明示的なプロジェクト管理者として行を保持する。認可フロー先頭の `isOwner=true` bypass(`contract_owners` 行存在で判定)は維持し、`project_users` 行は通知宛先解決と画面表示の事実情報として参照する
- 新規オーナー登録時のトランザクション: `INSERT INTO users(...)` → `INSERT INTO contract_owners(user_id, contract_status='active', valid=1, ...)` を同一 BEGIN IMMEDIATE で実行(`users.id` = `contract_owners.user_id` = `contractOwnerUserId`、物理値は同一 ULID)
- 共有概念は [../../共有/共有概念.md](../../共有/共有概念.md) を正本とする

### 3.7a メンバーアカウント有効化フロー(SCR-031 / FR-016d / FR-016e)

招待されたメンバー本人が招待リンク先で氏名 + 初回パスワード + 規約同意を入力してアカウントを有効化するフローの実装ロジック。基本設計の正本は [../02_基本設計/08_認証・認可設計.md §3.4](../02_基本設計/08_認証・認可設計.md) と [../02_基本設計/02_API設計.md §5.1.7 / §5.1.8](../02_基本設計/02_API設計.md) を参照。

#### 3.7a.1 招待時の DB 状態作成(`POST /projects/{id}/members`)

```ts
await db.transaction(async (tx) => {
  // (1) users 行を name=NULL で作成(個人情報原則により、招待者は氏名を入力しない)
  const userId = ulid()
  await tx.users.insert({
    id: userId,
    email_encrypted: encrypt(email, ownerKey),
    email_hmac: hmac(email, ownerKey),
    name: null,                              // SCR-031 で本人が入力
    password_hash: null,                     // SCR-031 で本人が入力
    status: 'pending_activation',
    valid: 1,
  })
  // (2) project_users 予約行(valid=0)
  await tx.projectUsers.insert({
    user_id: userId,
    contract_owner_user_id: contractOwnerUserId,
    project_id: projectId,
    role: invitedRole,                       // 'admin' | 'member'
    valid: 0,                                // SCR-031 完了時に 1 に更新
  })
  // (3) access_tokens(activation, 7d, meta に invitedProjectId / invitedRole を保持)
  const rawToken = generateRawToken()
  await tx.accessTokens.insert({
    id: ulid(),
    user_id: userId,
    token_hash: hmac(rawToken, hashKey),
    purpose: 'activation',
    meta: JSON.stringify({ invitedProjectId: projectId, invitedRole }),
    expires_at: addDays(now(), 7),
  })
  // (4) 招待メール送信(TPL-ADMIN_USER_REGISTER または TPL-PROJECT_ADMIN_INVITE)
  await enqueueEmail({ template, activationUrl: buildActivationUrl(rawToken) })
})
```

#### 3.7a.2 プレビュー API(`POST /auth/invitations/{token}/preview`)

未認証可。トークン検証 + 招待情報パネル用のメタ情報を返却。

```ts
const { tokenRecord, userId } = await verifyToken(rawToken, 'activation', env)
const meta = JSON.parse(tokenRecord.meta)
const project = await db.projects.findById(meta.invitedProjectId)
const owner = await db.users.findById(project.contract_owner_user_id)
const user = await db.users.findById(userId)
return {
  userId,
  email: decrypt(user.email_encrypted, ownerKey),
  projectId: project.id,
  projectName: project.name,
  invitedRole: meta.invitedRole,
  inviterOwnerName: owner.name,
  expiresAt: tokenRecord.expires_at,
}
```

#### 3.7a.3 有効化 API(`POST /auth/invitations/{token}/activate`)

同一トランザクションで `users.name` / `users.password_hash` / `users.status` を更新 + `terms_agreements` INSERT + `project_users.valid=1` + `consumeToken()` + 監査ログ。

```ts
await verifyTurnstile(req.turnstileToken)
const { tokenRecord, userId } = await verifyToken(rawToken, 'activation', env)
const meta = JSON.parse(tokenRecord.meta)
validateDisplayName(req.displayName)        // 1〜100 文字
validatePassword(req.password)              // FR-006 強度要件
if (req.password !== req.passwordConfirm) throw E_INPUT_VALIDATION
if (!req.termsAgreed || !req.privacyAgreed) throw E_INPUT_REQUIRED

await db.transaction(async (tx) => {
  await tx.users.update(userId, {
    name: req.displayName.trim(),
    password_hash: await hashPassword(req.password),
    status: 'active',
    updated_at: now(),
  })
  // 利用規約・プライバシーポリシーを doc_type 別の 2 行で記録
  await tx.termsAgreements.insertMany([
    { id: ulid(), user_id: userId, doc_type: 'terms_of_service', terms_version: latestTermsVersion, agreed_at: now() },
    { id: ulid(), user_id: userId, doc_type: 'privacy_policy', terms_version: latestPrivacyVersion, agreed_at: now() },
  ])
  await tx.projectUsers.update({
    user_id: userId,
    project_id: meta.invitedProjectId,
  }, { valid: 1, updated_at: now() })
  await tx.accessTokens.update(tokenRecord.id, { used_at: now() })
  await tx.auditLogs.insert({
    action: 'user.activation_completed',
    actor_user_id: userId,
    target_user_id: userId,
    meta: JSON.stringify({ project_id: meta.invitedProjectId, role: meta.invitedRole }),
    retention_class: 'general',
  })
})
return { userId, redirectUrl: '/auth/login' }
```

#### 3.7a.4 招待再送(`POST /members/{id}/resend-invitation`)

旧 `access_tokens` を `used_at=now()` で失効させ、`meta` を同一(`invitedProjectId` / `invitedRole`)で新規トークンを発行。リクエストボディは空(`displayName` を受け付けない)。

#### 3.7a.5 名前解決による既存アカウント招待(FR-021c)

既存アカウントが同一オーナースコープに存在する場合は、SCR-031 をバイパスして `project_users` 行のみを `valid=1` で追加 + 既存アカウントのログイン URL を含む簡易メールを送信(氏名 / パスワードは既存値を保持)。

### 3.7b プロジェクト割当解除と契約削除

`DELETE /projects/{id}/members/{userId}`:

1. `requireProjectRole(..., 'admin')` と自己・オーナー保護を実行する。
2. 対象ユーザーの他プロジェクト有効割当数を取得する。
3. 0件なら `E-BIZ-MEMBER-NO-GRANT` を返し、更新しない。
4. 1件以上なら対象 `project_users` 行だけを `valid=0` にする。
5. `project.member_removed` を監査記録する。他プロジェクト、`users`、セッションは維持する。

`DELETE /owner/members/{userId}`:

1. `requireOwner()` とオーナー保護を実行する。
2. 担当チャットを `resolveInitialAssignee` で別担当へ付け替える。
3. `users.valid=0`、全 `project_users.valid=0`、全セッション失効、未使用招待失効を同一トランザクションで実行する。
4. `user.logical_delete` に参加プロジェクトID一覧を記録する。

### 3.8 退会フロー（参照のみ）

- オーナーが退会要求 → `UPDATE contract_owners SET contract_status='deleted_pending'` + `withdrawal_requests` レコード作成 + `scheduled_deletion_at`（30 日後）
- 30 日経過後、§14.1.9 定期削除 cron が `UPDATE contract_owners SET contract_status='deleted', valid=0` に更新し、後続バッチで `users.valid=0` を揃える
- 詳細は [DD14_バッチ・非同期処理.md](DD14_バッチ・非同期処理.md) を参照

### 3.9 フロント URL ルーティング規約(FR-325)

利用者向け管理コンソールの SPA は **スコープ別 4 系列** のルートツリーで構成する。スコープ判定はフロント router の `beforeEach` ガードと API ミドルウェア `authenticate.ts` + `requireTenant` の二重化で行う。

```
/auth/*                        ── 認証フロー(未認証可)
/owner/*                       ── 契約 WS(オーナー専有 = `contract_owners` 行存在)
/projects/:projectId/*         ── プロジェクト WS(`project_users` 保持者)
/account/*                     ── 共通領域(認証済全員)
/eu/*                          ── エンドユーザー(認証コンソール外、トークン認証)
```

#### 3.9.1 フロント router のガード(疑似コード)

```ts
router.beforeEach(async (to) => {
  const scope = parseScope(to.path)  // 'auth' | 'owner' | 'projects' | 'account' | 'eu'
  const me = await ensureSession()    // 未認証なら null

  if (scope === 'auth') {
    if (me) return resolveDefaultWorkspace(me)  // 認証済は既定 WS へリダイレクト(§4.4.7)
    return true
  }

  if (!me) return '/auth/login'

  if (scope === 'owner' && !me.isOwner) {
    return resolveDefaultWorkspace(me)  // 403 同等の扱い、UI 上は権限不足トーストを表示
  }

  if (scope === 'projects') {
    const projectId = to.params.projectId
    const projectUser = me.projectUsers.find(pu => pu.project_id === projectId)
    if (!me.isOwner && !projectUser) {
      // 割当解除は最後の1件を拒否するため、通常は projectUsers.length >= 1
      return resolveDefaultWorkspace(me)
    }
  }

  return true
})
```

#### 3.9.2 既定ワークスペース決定(§4.4.7 と同期)

`resolveDefaultWorkspace(me)` は次の順で評価する:

1. localStorage の `lastWorkspace` が現在の `project_users` で有効ならその WS のホーム
2. オーナーかつ未保存なら `/owner` (SCR-026)
3. メンバーで `project_users` 行が 1 件なら `/projects/{onlyRow.project_id}` (SCR-015)
4. メンバーで `project_users` 行が複数なら最新 `last_accessed_at` のプロジェクト

ユーザー削除はユーザーレベル論理削除(`users.valid=0`)のため、現存するメンバー(`users.valid=1`)は `project_users.valid=1` を 1 件以上満たし、「`project_users` 行が 0 件のケース」は構造的に発生しない。データ不整合等で `WHERE valid=1` 条件下で `projectUsers.length === 0` を検出した場合のフェイルセーフは Sentry へ重大度 high を通知し `/auth/login` へ強制ログアウトする(運営者 IF #2 強制ログアウト発火を期待)。

**認証ミドルウェアの追加チェック**: 認証成功直後に `users.valid=1` を確認し、`valid=0` の場合は 401 でログイン拒否(セッション失効扱い)。これにより論理削除済みユーザーのセッションが残存しても再アクセスを防止する。オーナーの場合は加えて `contract_owners.valid=1 AND contract_status NOT IN ('deleted')` も検証(`deleted_pending` は 30 日間の猶予期間中は許容)。

### 3.10 SCR-026 OnboardingChecklist 完了判定ロジック(FR-326)

オーナーがSCR-026契約概要に到達したとき、`GET /me/onboarding-status` で4ステップの完了状態を返す。

| ステップ | 完了条件(SQL レベル) | 返却フィールド |
|---|---|---|
| 1. プロジェクト作成 | `SELECT EXISTS(SELECT 1 FROM projects WHERE contract_owner_user_id = :contractOwnerUserId AND deleted_at IS NULL)` | `step_project_created: boolean` |
| 2. 最初のFAQ登録 | `SELECT EXISTS(... FROM faqs f JOIN projects p ... WHERE f.status='published' AND f.valid=1)` | `step_first_faq_published: boolean` |
| 3. ウィジェット動作確認 | `SELECT EXISTS(... FROM question_logs q JOIN projects p ... WHERE q.source='widget_preview' AND q.status='success')` | `step_widget_tested: boolean` |
| 4. サイト設置 | `SELECT EXISTS(... FROM widget_keys wk JOIN projects p ... WHERE wk.published_at IS NOT NULL)` | `step_widget_published: boolean` |

#### 3.10.1 表示制御

- 4ステップ全て `true` の場合、OnboardingChecklistを非表示にする。
- ステップ1が未完了の場合、後続CTAを非活性にする。
- 各ステップ CTA の遷移先 URL は §3.9 の規約に従う:
  - ステップ 1: `/owner/projects/new`(SCR-010-M1 を新規モードで開く)
  - ステップ2: `/projects/{projectId}/faqs`
  - ステップ3: `/projects/{projectId}/widget#preview`
  - ステップ4: `/projects/{projectId}/widget#install`

#### 3.10.2 実装モジュール配置

- ハンドラ: `app/workers/main-api/src/handlers/onboarding-status.ts`(新規)
- ルート: `app/workers/main-api/src/routes/me.ts` に `GET /me/onboarding-status` を追加
- 認可: `requireTenant` + `requireOwner`(`contract_owners` 行存在で判定)。非オーナーは 403 `E-AUTHZ-OWNER-ONLY`
- レスポンスキャッシュ: なし(画面ロードごとに4件の `EXISTS` を評価)

### 3.11 契約共通項目の「利用中のプロジェクト」集計(FR-327)

契約共通項目の「利用中のプロジェクト: N」表示用に参照プロジェクト件数を返す。今月の利用上限・無料利用枠はプロジェクト単位のため対象外とする。

- AI 利用コスト消化率等の契約共通指標: `SELECT COUNT(*) FROM projects WHERE contract_owner_user_id = :contractOwnerUserId AND deleted_at IS NULL`(契約配下の全プロジェクト数。契約全体に適用される指標のため、全プロジェクトが利用先となる)
- 集約レスポンスは `GET /me/onboarding-status` と同一 API にまとめて返却(`shared_assets_usage` フィールド)し、SCR-026 の 1 ロードで全データを揃える

N=0 の場合のラベル切替は基本設計 [01_画面設計.md §4.4.4](../02_基本設計/01_画面設計.md) のメッセージ ID 体系(`MSG-SCR-026-LBL-005` / `LBL-006`)に従う。

## 4. 関連設計

| 種別 | 参照先 |
|---|---|
| 要件 | [../01_要件定義/index.md](../01_要件定義/index.md) |
| 基本設計 | [../02_基本設計/index.md](../02_基本設計/index.md) |
| 権限設計 | [../02_基本設計/04_権限設計.md](../02_基本設計/04_権限設計.md) |
| 認証・認可設計 | [../02_基本設計/08_認証・認可設計.md](../02_基本設計/08_認証・認可設計.md) |
| 共有概念 | [../../共有/共有概念.md](../../共有/共有概念.md) |
| 運用設計 | [../04_運用設計/index.md](../04_運用設計/index.md) |
| 将来対応 | [../05_future/index.md](../05_future/index.md) |
| 関連 DD | [DD08_トークン発行・検証.md](DD08_トークン発行・検証.md) / [DD09_認可ヘルパ.md](DD09_認可ヘルパ.md) / [DD10_監査ログ書込・完全性検証.md](DD10_監査ログ書込・完全性検証.md) / [DD11_暗号化・鍵管理.md](DD11_暗号化・鍵管理.md) |

## 5. テスト観点

| AC ID | テスト ID | テスト方式 | テストファイル |
|---|---|---|---|
| AC-001 | `e2e-signup-001` | E2E | `apps/admin/e2e/auth/signup.spec.ts` |
| AC-002 | `e2e-email-verify-001` | E2E | `apps/admin/e2e/auth/email-verify.spec.ts` |
| AC-003 | `e2e-pwreset-001` | E2E | `apps/admin/e2e/auth/password-reset.spec.ts` |
| AC-004 | `e2e-login-001` / `e2e-logout-001` | E2E | `apps/admin/e2e/auth/login.spec.ts` / `.../logout.spec.ts` |
| AC-005 | `it-reauth-001` | Integration | `workers/main-api/test/integration/auth/reauth.test.ts` |
| AC-007 | `it-lockout-001` | Integration | `workers/main-api/test/integration/auth/lockout.test.ts` |
| AC-008 | `it-session-expire-001` / `it-session-expire-002` | Integration | `workers/main-api/test/integration/auth/session-expire.test.ts` |
| AC-009 | `e2e-admin-user-activation-001` | E2E | `apps/main/e2e/admin-users.spec.ts`(SCR-017-M1 招待 → 招待メール → SCR-031 で氏名 + 初回パスワード + 規約同意 → 有効化完了 → SCR-001 ログイン の一連シナリオ。氏名は SCR-017-M1 では入力せず SCR-031 でのみ本人入力、users.name が NULL → 設定済、users.status が pending_activation → active、project_users.valid が 0 → 1、access_tokens.used_at セットを検証)|
| AC-016 | `e2e-withdrawal-001` | E2E | `apps/admin/e2e/account/withdrawal.spec.ts` |
| AC-027 | `e2e-terms-modal-001` | E2E | `apps/admin/e2e/terms/reaccept.spec.ts` |
| AC-046 | `it-force-logout-001` | Integration | `workers/main-api/test/integration/owner/force-logout.test.ts` |

### 5.1 結合テスト例（ロックアウト）

```ts
// tests/integration/auth/login.test.ts
describe('POST /api/v1/auth/login', () => {
  it('正しい資格情報でログイン成功', async () => {
    await seedAccount({ email: 'admin@example.com', password: 'P@ss12345678', role: 'admin' });
    const res = await (await worker).fetch('http://localhost/api/v1/auth/login', {
      method: 'POST', body: JSON.stringify({ email: '...', password: '...' }),
    });
    expect(res.status).toBe(200);
    expect(res.headers.get('Set-Cookie')).toContain('session=');
  });
  it('5 回失敗でロックアウト', async () => {
    for (let i = 0; i < 5; i++) {
      await (await worker).fetch('...', { method: 'POST', body: JSON.stringify({ ..., password: 'wrong' }) });
    }
    const res = await (await worker).fetch('...', { method: 'POST', body: JSON.stringify({ ..., password: 'correct' }) });
    expect(res.status).toBe(423);  // LOCKED_OUT
  });
});
```

### 5.2 その他観点

| 観点 | 内容 |
|---|---|
| 単体テスト | `contract-status.ts` 遷移ガード（全 4 状態 × 全遷移を網羅） |
| 異常系 | ロックアウト / 期限切れトークン / リプレイトークン / 規約未同意時の API 拒否 |
| 境界値 | パスワード長 12 文字 / 失敗回数 5 回 / セッション TTL 12h |
| 権限 | オーナー専有機能（課金 / 退会 / 規約再同意）にメンバーアクセス時 403 |
| 性能 | `POST /api/v1/auth/login` p95 < 800ms（Argon2id verify 支配） |

## 6. 未確定事項・確認事項

| 確認事項ID | 確認内容 | 優先度 | ステータス |
|---|---|---|---|
| - | v1.0 リリース時点で全項目確定済み | 低 | 確認済 |
