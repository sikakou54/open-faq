# <span id="ERR-032"></span>ERR-032: IDEMPOTENT_REPLAY

> **このページは API レスポンスで返すエラー `IDEMPOTENT_REPLAY` の HTTP ステータス・分類・メッセージ・業務ユースケースID・API ID を定義します。**

| ID | 業務ユースケースID | API ID |
|----|----|----|
| ERR-032 | [UC-058](../../01_requirements/04_business_usecases/UC-058.md#UC-058) ・ [UC-065](../../01_requirements/04_business_usecases/UC-065.md#UC-065) | [API-059](../02_backend/03_apis/API-059.md#API-059) |

| エラーコード | HTTPステータス | エラー分類 |
|----|----|----|
| `IDEMPOTENT_REPLAY` | 200 | 業務 |
*種別 エラー定義 ・ 分類 業務 ・ ステータス ドラフト*

## <span id="def"></span>1. 定義

| 項目 | 値 |
|----|----|
| エラーコード | `IDEMPOTENT_REPLAY` |
| 分類コード(taxonomy) | — |
| HTTP ステータス | 200 |
| エラー分類 | 業務 |

## <span id="msg"></span>2. メッセージ

既存処理結果を返却(冪等リプレイ)。これはエラーではなく、同一リクエストの再送に対して前回の処理結果をそのまま返す正常応答(冪等)である。一覧管理の都合で本ページに掲載するが、利用者・呼び出し元へ失敗として通知しない。

## <span id="cond"></span>3. 発生条件・表示先・復旧手段

| 観点 | 内容 |
|----|----|
| 発生条件 | 外部 Webhook(Resend)で、過去に処理済みと同一のイベントが再送されたとき。重複処理を防ぐため前回結果を返す。エラーではなく正常応答(冪等)。 |
| 表示先 | なし(エラー表示は行わず、既存の処理結果を HTTP 200 で返却する) |
| 復旧手段 | 不要(正常応答のため利用者の対応は不要) |

