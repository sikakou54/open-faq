# <span id="API-056"></span>API-056: 退会申請

> **このページは 退会申請 API の契約を定義します。** オーナーの退会を申請する。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-056` |
| API名 | 退会申請 |
| トレーサビリティID | [TR-023](../../00_traceability/index.md#TR-023) ・ [TR-085](../../00_traceability/index.md#TR-085) |
| エンドポイント | `/withdrawal-requests` |
| HTTPメソッド | POST |
| 認証 | オーナー専有、再認証必須 |
| 認可 | オーナー専有 |

## 処理概要

オーナーの退会を申請する。

| 処理 ID | 処理内容                                       |
|---------|------------------------------------------------|
| P-01    | 認証・認可(オーナー専有、再認証必須)を検証する |
| P-02    | 退会申請を登録し受付状態とする                 |
| P-03    | 受付結果を返却する                             |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)と CSRF トークンを必要とする状態変更系である。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |
| `X-CSRF-Token: <token>` | 必須 | CSRF 対策(状態変更系) |
| `Idempotency-Key: <ULID>` | 任意 | 冪等性(書き込み再送の重複防止) |

> 本操作は再認証必須。直近の再認証([API-005](API-005.md#API-005))で得た再認証状態を伴う必要がある。

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

```json
{ "reason": "退会申請内容(理由等)" }
```

| 項目     | 型     | 説明                     |
|----------|--------|--------------------------|
| `reason` | string | 退会理由・申請内容(任意) |

## レスポンス

**レスポンス(2xx)**

退会申請の受付結果。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

| HTTP ステータス | エラーコード        | 内容               |
|-----------------|---------------------|--------------------|
| 403 | `PERMISSION_DENIED` → [ERR-032](../../05_errors/ERR-032.md#ERR-032) | メンバーは申請不可 |
## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_WITHDRAW_REQ`](../04_database/TBL-023.md#TBL-023) | `C---` |
| [`M_CONTRACT`](../04_database/TBL-002.md#TBL-002) | `-RU-` |

## 備考
