# <span id="API-064"></span>API-064: 自己プロフィール取得

> **このページは 自己プロフィール取得 API の契約を定義します。** 認証中の利用者自身の表示名・連絡先メールと確認状態・ユーザー種別・参加プロジェクト一覧を返す参照系 API。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-064` |
| API名 | 自己プロフィール取得 |
| トレーサビリティID | [TR-008](../../00_traceability/index.md#TR-008) |
| エンドポイント | `/me/profile` |
| HTTPメソッド | GET |
| 認証 | セッション(Cookie) |
| 認可 | 認証済み利用者(自分の情報) |

## 処理概要

認証中の利用者の表示名・連絡先メール(確認状態を含む)・ユーザー種別(オーナー / メンバー)・参加プロジェクト一覧を返す。個人設定画面([SCR-022](../../01_frontend/01_screens/SCR-022.md#SCR-022))の初期表示で使用する。

| 処理 ID | 処理内容 |
|---------|----------|
| P-01 | セッションから認証済み利用者を特定する(無効・失効は 401) |
| P-02 | 利用者の表示名・連絡先メールと確認状態を取得する |
| P-03 | 契約への所属からユーザー種別(オーナー / メンバー)を判定する |
| P-04 | 利用者が参加するプロジェクトの一覧(ID・名称・役割)を取得する |
| P-05 | 取得した情報をまとめて返す |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)による認証を必要とする参照系であり、CSRF トークンは不要である。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

## レスポンス

認証中の利用者のプロフィールと参加プロジェクト一覧を返す。

**レスポンス(200)**

```json
{
  "displayName": "山田 太郎",
  "email": "taro@example.com",
  "emailVerified": true,
  "userType": "owner",
  "projects": [
    { "id": "prj_01", "name": "サポートサイト", "role": "owner" }
  ]
}
```

| 項目 | 型 | 説明 |
|---|---|---|
| `displayName` | string | 表示名 |
| `email` | string | 連絡先メールアドレス |
| `emailVerified` | boolean | 連絡先メールの確認状態 |
| `userType` | enum | ユーザー種別(`owner` / `member`) |
| `projects` | array | 参加プロジェクト一覧 |
| `projects[].id` | string | プロジェクト ID |
| `projects[].name` | string | プロジェクト名 |
| `projects[].role` | string | 当該プロジェクトでの役割 |

## バリデーション

入力パラメータを持たない参照系のため、入力値の検証は行わない。

## エラー

| HTTP ステータス | エラーコード | 内容 |
|-----------------|---------------------|--------------------------------|
| 401 | `SESSION_EXPIRED` → [ERR-033](../../05_errors/ERR-033.md#ERR-033) | セッションが無効または失効している |

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`M_USER`](../04_database/TBL-001.md#TBL-001) | `-R--` |
| [`M_CONTRACT`](../04_database/TBL-002.md#TBL-002) | `-R--` |
| [`M_PRJ_USERS`](../04_database/TBL-003.md#TBL-003) | `-R--` |
| [`M_PROJECTS`](../04_database/TBL-004.md#TBL-004) | `-R--` |

## 備考

表示名・連絡先メールは全ユーザー共通の `M_USER` に保持する。ユーザー種別(オーナー / メンバー)は契約への所属で判定し、参加プロジェクト一覧はプロジェクト割当(`M_PRJ_USERS`)とプロジェクト(`M_PROJECTS`)から構成する。更新は別系統の API([API-012](API-012.md#API-012) 自己プロフィール更新)が担い、本 API は参照のみを行う。
