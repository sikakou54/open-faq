# <span id="API-058"></span>API-058: メール配信 IF(`EmailProvider`)

> **このページは メール配信 IF(`EmailProvider`) API の契約を定義します。** 送信(`send`)と Webhook 検証(`verifyWebhook`)を持つ内部抽象インターフェースです。MVP は `ResendEmailProvider` として実装します。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-058` |
| API名 | メール配信 IF(`EmailProvider`) |
| トレーサビリティID | [TR-056](../../00_traceability/index.md#TR-056) ・ [TR-058](../../00_traceability/index.md#TR-058) ・ [TR-063](../../00_traceability/index.md#TR-063) ・ [TR-064](../../00_traceability/index.md#TR-064) ・ [TR-065](../../00_traceability/index.md#TR-065) ・ [TR-066](../../00_traceability/index.md#TR-066) ・ [TR-068](../../00_traceability/index.md#TR-068) ・ [TR-069](../../00_traceability/index.md#TR-069) ・ [TR-076](../../00_traceability/index.md#TR-076) |
| エンドポイント | — |
| HTTPメソッド | — |
| 認証 | — |
| 認可 | — |

## 処理概要

送信(`send`)と Webhook 検証(`verifyWebhook`)を持つ内部抽象インターフェースです。MVP は `ResendEmailProvider` として実装します。

| 処理 ID | 処理内容 |
|----|----|
| P-01 | `send` で宛先・件名・本文をプロバイダへ渡しメールを送信する(冪等キーで重複送信を排除) |
| P-02 | 送信結果のメッセージ ID を返す |
| P-03 | `verifyWebhook` で受信した Webhook の署名を検証する |
| P-04 | 配信イベント種別・メッセージ ID・発生時刻を返す |

## インターフェース定義

送信(`send`)と Webhook 検証(`verifyWebhook`)を持つ内部抽象インターフェースです。MVP は `ResendEmailProvider` として実装します。

```
export interface EmailProvider {
  send(input: {
    to: string; from: string; reply_to?: string; subject: string;
    html: string; text?: string; headers?: Record<string, string>;
    idempotency_key: string;
  }): Promise<{ message_id: string }>;

  verifyWebhook(headers: Headers, body: string): Promise<{
    valid: boolean;
    event_type: 'sent'|'delivered'|'bounced'|'complained'|'failed'|'opened'|'clicked';
    message_id: string;
    timestamp: string;
  }>;
}
```

MVP は Resend 実装(`ResendEmailProvider`)。

**`send` の戻り値**

| 項目         | 型     | 説明                          |
|--------------|--------|-------------------------------|
| `message_id` | string | 送信したメールのメッセージ ID |

**`verifyWebhook` の戻り値**

| 項目 | 型 | 説明 |
|----|----|----|
| `valid` | boolean | Webhook 署名検証の成否 |
| `event_type` | enum | 配信イベント種別(`sent` / `delivered` / `bounced` / `complained` / `failed` / `opened` / `clicked`) |
| `message_id` | string | 対象メールのメッセージ ID |
| `timestamp` | string | イベント発生時刻 |

## リクエスト

本 API はリクエストボディ・パラメータを持たない。

## レスポンス

本 API の戻り値は本文の型定義を参照する。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([エラー設計](../../05_errors/index.md) の標準エラー体系に従う)。

## 利用テーブル

本 API はテーブルを直接読み書きしない(外部サービス連携 IF)。

## 備考
