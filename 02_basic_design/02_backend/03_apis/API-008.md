# <span id="API-008"></span>API-008: メンバーアカウント有効化

> **このページは メンバーアカウント有効化 API の契約を定義します。** 招待されたメンバー本人が氏名・初回パスワード・規約同意を登録し有効化を完了する。本処理は 1 つのトランザクションで実行する。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-008` |
| API名 | メンバーアカウント有効化 |
| 対応業務UC | [UC-006](../../../01_requirements/04_business_usecases/UC-006.md#UC-006) |
| 対応画面ID | [SCR-023](../../01_frontend/01_screens/SCR-023.md#SCR-023) |
| 対応画面イベントID | EVT-190 |
| エンドポイント | `/auth/invitations/{token}/activate` |
| HTTPメソッド | POST |
| 認証 | 未認証可(トークン認証のみ、Turnstile 必須) |
| 認可 | — |

## 処理概要

招待されたメンバー本人が氏名・初回パスワード・規約同意を登録し有効化を完了する。本処理は 1 つのトランザクションで実行する。

| 処理 ID | 処理内容 |
|----|----|
| P-01 | 招待トークンの有効性(期限・使用済み)を検証する |
| P-02 | トークンから招待先プロジェクトを取得する |
| P-03 | 予約 `M_USER` 行に氏名・パスワードを設定し `status='active'` とする(アカウント有効化) |
| P-04 | 利用規約・プライバシーポリシーの最新版への同意を記録する(各文書種別に 1 件) |
| P-05 | 対象プロジェクトへの割当を有効化する |
| P-06 | 招待トークンを使用済みにする |
| P-07 | 監査ログ(アカウント有効化完了)を記録する |

## リクエスト

#### 認証ヘッダ

本 API はセッション認証を必要とせず、検証用トークンを URL パスで受け取って検証する。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Idempotency-Key: <ULID>` | 任意 | 冪等性(再送の重複防止) |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{ "token": "招待トークン" }
```

| 項目    | 型     | 説明                             |
|---------|--------|----------------------------------|
| `token` | string | 招待トークン(必須・パス内で指定) |

#### Request Body

```json
{
  "displayName": "山田 太郎",
  "password": "P@ssw0rd-strong",
  "passwordConfirm": "P@ssw0rd-strong",
  "termsAgreed": true,
  "privacyAgreed": true,
  "turnstileToken": "..."
}
```

| 項目 | 型 | 説明 |
|----|----|----|
| `displayName` | string | 本人の表示氏名(必須・1〜100 文字、前後空白トリム) |
| `password` | string | 初回パスワード(必須・12 文字以上、英大文字 / 小文字 / 数字 / 記号のうち 3 種類以上) |
| `passwordConfirm` | string | パスワード確認用(必須・`password` と一致) |
| `termsAgreed` | boolean | 利用規約への同意(必須・`true`) |
| `privacyAgreed` | boolean | プライバシーポリシーへの同意(必須・`true`) |
| `turnstileToken` | string | Turnstile 検証トークン(必須) |

バリデーション:

- `displayName`: 必須、1〜100 文字(前後空白トリム)
- `password`: 12 文字以上、英大文字 / 小文字 / 数字 / 記号のうち 3 種類以上(FR-006)
- `passwordConfirm`: `password` と一致
- `termsAgreed` / `privacyAgreed`: 両方 `true` 必須(FR-137 / FR-139)
- `turnstileToken`: 検証成功(FR-149)

## レスポンス

**レスポンス(200)**

```json
{ "userId": "01HZ...", "redirectUrl": "/auth/login" }
```

| 項目          | 型     | 説明                                   |
|---------------|--------|----------------------------------------|
| `userId`      | string | 有効化が完了した利用者の ID            |
| `redirectUrl` | string | 有効化完了後の遷移先 URL(ログイン画面) |

## バリデーション

- `displayName`: 必須、1〜100 文字(前後空白トリム)
- `password`: 12 文字以上、英大文字 / 小文字 / 数字 / 記号のうち 3 種類以上(FR-006)
- `passwordConfirm`: `password` と一致
- `termsAgreed` / `privacyAgreed`: 両方 `true` 必須(FR-137 / FR-139)
- `turnstileToken`: 検証成功(FR-149)

## エラー

| HTTP ステータス | エラーコード | 内容 |
|----|----|----|
| 410 | `TOKEN_EXPIRED` → [ERR-008](../../05_errors/ERR-008.md#ERR-008) | トークン期限切れ |
| 410 | `TOKEN_USED` → [ERR-009](../../05_errors/ERR-009.md#ERR-009) | トークン使用済み |
| 400 | `VALIDATION_ERROR` → [ERR-001](../../05_errors/ERR-001.md#ERR-001) | 氏名長 / パスワード強度 / 規約同意未チェック |
| 400 | `TURNSTILE_FAILED` → [ERR-002](../../05_errors/ERR-002.md#ERR-002) | Turnstile 検証失敗 |
## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_ACCESS_TOKENS`](../04_database/TBL-014.md#TBL-014) | `-RU-` |
| [`M_USER`](../04_database/TBL-001.md#TBL-001) | `--U-` |
| [`M_PRJ_USERS`](../04_database/TBL-003.md#TBL-003) | `--U-` |
| [`T_TERMS_AGREE`](../04_database/TBL-024.md#TBL-024) | `C---` |
| [`H_AUDIT_LOGS`](../04_database/TBL-027.md#TBL-027) | `C---` |

## 備考
