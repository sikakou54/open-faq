# <span id="API-045"></span>API-045: 支払方法 取得・登録・更新

> **このページは 支払方法 取得・登録・更新 API の契約を定義します。** 支払方法の取得と登録 / 更新(クレジットカードのみ)。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-045` |
| API名 | 支払方法 取得・登録・更新 |
| トレーサビリティID | [TR-038](../../00_traceability/index.md#TR-038) ・ [TR-059](../../00_traceability/index.md#TR-059) |
| エンドポイント | `/billing/payment-method` |
| HTTPメソッド | GET / PUT |
| 認証 | Cookie + CSRF(PUT は再認証必須) |
| 認可 | **オーナー専有** |

## 処理概要

支払方法の取得と登録 / 更新(クレジットカードのみ)。

| 処理 ID | 処理内容 |
|----|----|
| P-00 | 再認証トークン(リクエストボディ `reauthToken`)を検証する(PUT 時必須) |
| P-01 | 認証・認可(オーナー専有、PUT は再認証必須)を検証する |
| P-02 | GET は登録済み支払方法(ブランド・下 4 桁等)を取得する |
| P-03 | PUT は課金プロバイダのトークンを検証し支払方法を登録 / 更新する(生データは保持しない) |
| P-04 | 結果を返却する |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)による認証を必要とし、登録・更新(PUT)時は CSRF トークンを併用する。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |
| `X-CSRF-Token: <token>` | PUT 時必須 | CSRF 対策(支払方法の登録・更新時) |
| `Idempotency-Key: <ULID>` | 任意 | 冪等性(PUT 再送の重複防止) |

> PUT(支払方法の登録・更新)は再認証必須。直近の再認証([API-005](API-005.md#API-005))で得た再認証状態を伴う必要がある。

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

```json
{ "reauthToken": "reauth_...", "paymentToken": "tok_..." }
```

| 項目 | 型 | 説明 |
|----|----|----|
| `reauthToken` | string | 再認証トークン(PUT 時のみ必須・API-005 で取得) |
| `paymentToken` | string | 課金プロバイダが発行した支払方法トークン。PUT 時のみ必須(GET ではボディなし) |

(GET: なし。PUT のみ。`paymentToken` は課金プロバイダ発行トークン)

## レスポンス

**レスポンス(200)**

GET: `{ "registered": true, "brand": "visa", "last4": "4242", "expMonth": 12, "expYear": 2028 }`(未登録時は `{ "registered": false }`)。PUT: `{ "registered": true, "brand": "visa", "last4": "4242" }`。

**GET レスポンス**

| 項目 | 型 | 説明 |
|----|----|----|
| `registered` | boolean | 支払方法が登録済みか(未登録時は本項目のみ `false` を返す) |
| `brand` | string | カードブランド |
| `last4` | string | カード番号下 4 桁 |
| `expMonth` | integer | カード有効期限(月) |
| `expYear` | integer | カード有効期限(年) |

**PUT レスポンス**

| 項目         | 型      | 説明                             |
|--------------|---------|----------------------------------|
| `registered` | boolean | 支払方法が登録済みか             |
| `brand`      | string  | 登録 / 更新したカードブランド    |
| `last4`      | string  | 登録 / 更新したカード番号下 4 桁 |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

| HTTP ステータス | エラーコード | 内容 |
|----|----|----|
| 401 | `REAUTH_REQUIRED` → [ERR-015](../../05_errors/ERR-015.md#ERR-015) | 再認証トークンが無効または未提示 |
| 400 | `VALIDATION_ERROR` → [ERR-001](../../05_errors/ERR-001.md#ERR-001) | 入力値の検証エラー |
| 402 | `PAYMENT_METHOD_DECLINED` → [ERR-030](../../05_errors/ERR-030.md#ERR-030) | カードが拒否された |
| 403 | `OWNER_ONLY` → [ERR-017](../../05_errors/ERR-017.md#ERR-017) | オーナー以外は不可 |
## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_BILL_SUBS`](../04_database/TBL-018.md#TBL-018) | `-RU-` |

## 備考
