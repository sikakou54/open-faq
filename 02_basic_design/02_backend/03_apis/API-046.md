# <span id="API-046"></span>API-046: プロジェクト上限・アラート取得

> **このページは プロジェクト上限・アラート取得 API の仕様を定義します。** 当該プロジェクトの質問数について利用状況・月次上限件数・アラート設定を返す。

| ID | 業務ユースケースID | イベント(画面ID EVT-NN) | テーブルID |
|----|----|----|----|
| API-046 | [UC-033](../../../01_requirements/04_business_usecases/UC-033.md#UC-033) ・ [UC-034](../../../01_requirements/04_business_usecases/UC-034.md#UC-034) ・ [UC-052](../../../01_requirements/04_business_usecases/UC-052.md#UC-052) ・ [UC-053](../../../01_requirements/04_business_usecases/UC-053.md#UC-053) | SCR-026 EVT-01 ・ SCR-027 EVT-01 | [TBL-006](../04_database/TBL-006.md#TBL-006) ・ [TBL-009](../04_database/TBL-009.md#TBL-009) ・ [TBL-020](../04_database/TBL-020.md#TBL-020) ・ [TBL-022](../04_database/TBL-022.md#TBL-022) ・ [TBL-026](../04_database/TBL-026.md#TBL-026) |

| API名 | エンドポイント | HTTPメソッド |
|----|----|----|
| プロジェクト上限・アラート取得 | `/projects/{id}/quota-limits` | GET |

## 処理概要

当該プロジェクトの質問数について利用状況・月次上限件数・アラート設定を返す。

| 処理 ID | 処理内容                                                  |
|---------|-----------------------------------------------------------|
| P-01    | 認証・認可(対象プロジェクトへのアクセス権)を検証する      |
| P-02    | 質問数の利用状況・月次上限・アラート設定を取得する        |
| P-03    | 上限消化時の参考課金額(`yenEquivalent`)を算出して返却する |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)による認証を必要とする。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{ "id": "プロジェクト ID" }
```

| 項目 | 型     | 説明                        |
|------|--------|-----------------------------|
| `id` | string | 対象プロジェクトの ID。必須 |

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

```json
{
  "projectId": "prj_...",
  "period": "2026-05-01T00:00:00Z",
  "questions": { "used": 750, "limitEnabled": true, "limit": 5000, "percentage": 15, "alertThresholds": [80, 100], "yenEquivalent": 2000, "source": "owner" }
}
```

| 項目 | 型 | 説明 |
|----|----|----|
| `projectId` | string | 対象プロジェクトの ID |
| `period` | string (ISO 8601) | 集計対象期間の起点 |
| `questions` | object | 質問数の利用状況・上限・アラート設定 |
| `　└ used` | integer | 当月の質問数 |
| `　└ limitEnabled` | boolean | 月次上限が有効か(OFF 時は `false`) |
| `　└ limit` | integer | 月次上限件数(OFF 時は `null`) |
| `　└ percentage` | integer | 上限に対する利用率(%、OFF 時は `null`) |
| `　└ alertThresholds` | array | アラート発火の閾値(%)の配列 |
| `　└ yenEquivalent` | integer | 上限消化時の参考課金額(OFF 時は `null`) |
| `　└ source` | enum | 上限設定元。`owner` |

`yenEquivalent` は上限件数をすべて利用した場合の最大課金額の参考値で、`billableCount = max(0, limit - 適用中の質問数無料利用枠)`、`yenEquivalent = billableCount × 質問単価` により算出する。**「質問数 無料枠」「質問数 超過単価(質問単価)」の値は [システム仕様書 §2(課金・利用量・上限)](../../07_system-spec.md#2-課金利用量上限) を正本とし、本書には数値を直書きしない**(無料枠・超過単価の改定はシステム仕様書の更新に追随する)。画面では「(上限件数) -(無料枠) =(課金対象件数)(¥(参考額) / 月)」の形式で表示する。上限 OFF 時は `limitEnabled=false`、`limit=null`、`percentage=null`、`yenEquivalent=null` を返す。SCR-026 は「今月の利用上限」を「OFF」と表示し、「利用量」には割合・件数比・ProgressBar・状態バッジを描画しない。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([エラー設計](../../05_errors/index.md) の標準エラー体系に従う)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`M_PRJ_QUOTA_LIMITS`](../04_database/TBL-009.md#TBL-009) | `-R--` |
| [`T_USAGE_METER`](../04_database/TBL-020.md#TBL-020) | `-R--` |
