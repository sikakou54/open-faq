# <span id="API-003"></span>API-003: ログアウト

> **このページは ログアウト API の仕様を定義します。** 現在のセッションを失効する。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-003` |
| API名 | ログアウト |
| トレーサビリティID | [TR-071](../../00_traceability/index.md#TR-071) ・ [TR-073](../../00_traceability/index.md#TR-073) |
| エンドポイント | `/auth/logout` |
| HTTPメソッド | POST |
| 認証 | Cookie + CSRF |
| 認可 | 認証済み |

## 処理概要

現在のセッションを失効する。

| 処理 ID | 処理内容                                     |
|---------|----------------------------------------------|
| P-01    | セッション Cookie と CSRF トークンを検証する |
| P-02    | 現在のセッションを失効する                   |
| P-03    | 完了結果を返す                               |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)と CSRF トークンを必要とする状態変更系である。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |
| `X-CSRF-Token: <token>` | 必須 | CSRF 対策(状態変更系) |
| `Idempotency-Key: <ULID>` | 任意 | 冪等性(書き込み再送の重複防止) |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

```json
{ "ok": true }
```

| 項目 | 型      | 説明                 |
|------|---------|----------------------|
| `ok` | boolean | セッション失効の成否 |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

想定エラーは本 API のエラー節を参照する(本 API 固有のエラーコードなし)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_SESSIONS`](../04_database/TBL-013.md#TBL-013) | `--U-` |

## 備考
