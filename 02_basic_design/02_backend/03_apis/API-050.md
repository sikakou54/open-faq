# <span id="API-050"></span>API-050: お知らせ一括既読

> **このページは お知らせ一括既読 API の仕様を定義します。** 選択した複数のお知らせを一括で既読にする(1 リクエスト上限 100 件)。

| ID | API名 | エンドポイント | HTTPメソッド |
|----|----|----|----| 
| API-050 | お知らせ一括既読 | `/me/announcements/read` | POST |

| 関連項目 | 内容 |
|----|----| 
| 業務ユースケース | [UC-045](../../../01_requirements/04_business_usecases/UC-045.md#UC-045) |
| イベント | [SCR-016 EVT-07](../../01_frontend/01_screens/SCR-016.md#SCR-016) / [SCR-016 EVT-08](../../01_frontend/01_screens/SCR-016.md#SCR-016) / [SCR-016 EVT-09](../../01_frontend/01_screens/SCR-016.md#SCR-016) |
| テーブル | [TBL-010](../04_database/TBL-010.md#TBL-010) / [TBL-021](../04_database/TBL-021.md#TBL-021) / [TBL-022](../04_database/TBL-022.md#TBL-022) / [TBL-027](../04_database/TBL-027.md#TBL-027) |

## 処理概要

選択した複数のお知らせを一括で既読にする(1 リクエスト上限 100 件)。

| 処理 ID | 処理内容                                                     |
|---------|--------------------------------------------------------------|
| P-01    | 認証・認可(オーナー / メンバー、CSRF)を検証する              |
| P-02    | 件数上限(100 件)を検証する                                   |
| P-03    | 指定または全件のお知らせを一括既読に更新する(既読済みは冪等)。`all` 指定時に `category`(お知らせ種別)が指定された場合は、当該種別の未読のみを既読化する |
| P-04    | 操作者・対象件数・実行日時を監査記録する                     |
| P-05    | 既読件数を返却する                                           |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)と CSRF トークンを必要とする状態変更系である。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |
| `X-CSRF-Token: <token>` | 必須 | CSRF 対策(状態変更系) |
| `Idempotency-Key: <ULID>` | 任意 | 冪等性(書き込み再送の重複防止) |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

```json
{ "ids": ["ann_...", "ann_..."] }
```

| 項目 | 型 | 説明 |
|----|----|----|
| `ids` | array | 既読にするお知らせ ID の配列(1 リクエスト上限 100 件)。`all` 未指定時は必須 |
| `all` | boolean | 全件既読を指定する場合に `true`。任意(`ids` の代わりに指定) |
| `category` | string | お知らせ種別での絞り込み(任意・`all:true` 指定時のみ有効。指定時は当該種別の未読のみ既読化・未指定=対象すべて) |

(全件既読を指定する場合は `{ "all": true }`。特定種別のみ全件既読化する場合は `{ "all": true, "category": "..." }`)

## レスポンス

**レスポンス(200)**

```json
{ "readCount": 8, "readAt": "..." }
```

| 項目        | 型                | 説明               |
|-------------|-------------------|--------------------|
| `readCount` | integer           | 今回既読にした件数 |
| `readAt`    | string (ISO 8601) | 一括既読の実行日時 |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

| HTTP ステータス | エラーコード       | 内容       |
|-----------------|--------------------|------------|
| 400 | `VALIDATION_ERROR` → [ERR-001](../../05_errors/ERR-001.md#ERR-001) | 100 件超過 |
## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_INBOX_MSG`](../04_database/TBL-022.md#TBL-022) | `--U-` |
| [`H_AUDIT_LOGS`](../04_database/TBL-027.md#TBL-027) | `C---` |
