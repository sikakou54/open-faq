# <span id="API-051"></span>API-051: お知らせ未読件数

> **このページは お知らせ未読件数 API の契約を定義します。** 未読件数と直近お知らせを返す。

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-051` |
| API名 | お知らせ未読件数 |
| トレーサビリティID | [TR-044](../../00_traceability/index.md#TR-044) ・ [TR-086](../../00_traceability/index.md#TR-086) |
| エンドポイント | `/me/announcements/unread-summary` |
| HTTPメソッド | GET |
| 認証 | Cookie |
| 認可 | オーナー / メンバー |

## 処理概要

未読件数と直近お知らせを返す。

| 処理 ID | 処理内容                               |
|---------|----------------------------------------|
| P-01    | 認証・認可(オーナー / メンバー)を検証する       |
| P-02    | 未読件数を集計し直近お知らせを取得する |
| P-03    | 整形して返却する                       |

## リクエスト

#### 認証ヘッダ

本 API は利用者セッション(Cookie)による認証を必要とする。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Cookie: session=<token>` | 必須 | 利用者セッション |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

```json
{ "unreadsCount": 5, "recent": [...] }
```

| 項目 | 型 | 説明 |
|----|----|----|
| `unreadsCount` | integer | 未読お知らせの件数 |
| `recent` | array\<object\> | 直近お知らせの配列(各要素は API-048 の `items` と同形式) |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([エラー設計](../../05_errors/index.md) の標準エラー体系に従う)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_INBOX_MSG`](../04_database/TBL-022.md#TBL-022) | `-R--` |

## 備考
