# <span id="API-059"></span>API-059: 外部 Webhook(Resend)

> **このページは 外部 Webhook(Resend) API の仕様を定義します。** メール配信状態 Webhook を受信し配信状態 / 抑制リストを更新する。

| ID | API名 | エンドポイント | HTTPメソッド |
|----|----|----|----| 
| API-059 | 外部 Webhook(Resend) | `/webhooks/resend` | POST |

| 関連項目 | 内容 |
|----|----| 
| 業務ユースケース | [UC-062](../../../01_requirements/04_business_usecases/UC-062.md#UC-062) / [UC-069](../../../01_requirements/04_business_usecases/UC-069.md#UC-069) |
| イベント | — |
| テーブル | [TBL-007](../04_database/TBL-007.md#TBL-007) / [TBL-026](../04_database/TBL-026.md#TBL-026) / [TBL-027](../04_database/TBL-027.md#TBL-027) / [TBL-002](../04_database/TBL-002.md#TBL-002) |

## 処理概要

メール配信状態 Webhook を受信し配信状態 / 抑制リストを更新する。

| 処理 ID | 処理内容 |
|----|----|
| P-01 | Resend 署名(HMAC-SHA256)を検証する(失敗時は 401) |
| P-02 | イベント種別を判定する |
| P-03 | `H_NOTIF_LOGS.delivery_state` を遷移する。通知のイベント発生時刻が記録済みの状態より古い場合は上書きしない(順序逆転・遅延到達への整合性保証) |
| P-04 | `bounced` / `complained` の場合は `M_EMAIL_SUPPRESS` に追加する(全プロジェクト横断) |
| P-05 | 受領応答を返す |

## リクエスト

#### 認証ヘッダ

本 API は Resend が付与する署名(HMAC-SHA256)を検証して受信元を認証する(認可はなし)。

| ヘッダ | 要否 | 説明 |
|---|---|---|
| `Svix-Signature: <署名>` | 必須 | Resend(Svix)署名。HMAC-SHA256 で検証(失敗時 401 [ERR-031](../../05_errors/ERR-031.md#ERR-031)) |

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

(Resend 仕様準拠):

```json
{
  "type": "email.delivered|email.bounced|email.complained|email.delivery_delayed",
  "data": {
    "message_id": "...",
    "to": "...",
    "subject": "...",
    "timestamp": "..."
  }
}
```

| 項目 | 型 | 説明 |
|----|----|----|
| `type` | enum | 配信イベント種別(`email.delivered` / `email.bounced` / `email.complained` / `email.delivery_delayed`) |
| `data` | object | イベント詳細データ |
| 　└ `message_id` | string | 対象メールのメッセージ ID |
| 　└ `to` | string | 宛先メールアドレス |
| 　└ `subject` | string | メール件名 |
| 　└ `timestamp` | string (ISO 8601) | イベント発生時刻 |

## レスポンス

本 API の戻り値は本文の型定義を参照する。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

| HTTP ステータス | エラーコード        | 内容                           |
|-----------------|---------------------|--------------------------------|
| 401 | `SIGNATURE_INVALID` → [ERR-031](../../05_errors/ERR-031.md#ERR-031) | 署名検証失敗 |
| 200 | `IDEMPOTENT_REPLAY` → [ERR-032](../../05_errors/ERR-032.md#ERR-032) | 既存処理結果を返却(冪等性違反) |
## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`H_NOTIF_LOGS`](../04_database/TBL-026.md#TBL-026) | `--U-` |
| [`M_EMAIL_SUPPRESS`](../04_database/TBL-007.md#TBL-007) | `C---` |
| [`H_AUDIT_LOGS`](../04_database/TBL-027.md#TBL-027) | `C---` |
