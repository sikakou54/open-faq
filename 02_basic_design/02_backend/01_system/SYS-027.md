# <span id="SYS-027"></span>SYS-027: 配信失敗通知の再送

> **このページは、配信に失敗した通知を検出し、再送上限の範囲内で自動的に再送するシステム処理 SYS-027 を定義します。** 処理概要 / 処理フロー図 / 入出力 / 処理項目定義 / 入出力一覧 / システムイベント一覧 の 6 セクションで記述します。

*種別 システム設計 ・ 優先度 P0 ・ ステータス ドラフト*

## 1. 処理概要

スケジューラが定期起動する再送処理が、配信に失敗した通知を検出し、抑制リスト該当外かつ再送上限未満の対象を上限の範囲内で再送する。再送結果に応じて配信状態と再送回数を更新し、再送上限到達分・抑制該当分は確定失敗として記録する。

再送上限は 3 回(設計値)とする。再送間隔は指数バックオフ(例: 1 回目 5 分後 / 2 回目 30 分後 / 3 回目 2 時間後。いずれも設計値)で、直近の配信失敗時刻からの経過時間が次回間隔に達した対象のみを各バッチで再送する。再送回数が上限(3 回)に到達した時点で再送を打ち切り、確定失敗として記録する。

| システム ID | 処理名 | 種別 | トリガー / スケジュール | 機能概要 |
|---|---|---|---|---|
| `SYS-027` | 配信失敗通知の再送 | batch | 定期起動の再送処理(失敗分の検出) | 配信失敗通知を検出し、抑制外かつ上限未満の対象を再送して配信状態・再送回数を更新する |

| 関連 | 内容 |
|---|---|
| 関連システム | — |
| トレーサビリティID | [TR-066](../../00_traceability/index.md#TR-066) |

## 2. 処理フロー図

```mermaid
flowchart TB
  S([スケジューラが再送処理を定期起動]) --> A[認証・データ取得]
  A --> B{再送対象あり}
  B -- なし --> Z([正常終了])
  B -- あり --> C[再送可否を判定<br/>(抑制リスト・再送上限)]
  C --> D{再送可<br/>(抑制外 かつ 上限未満)}
  D -- 可 --> E[通知を再送]
  E --> F[再送結果を取得]
  F --> G[配信状態・再送回数を更新]
  D -- 上限到達 / 抑制該当 --> H[確定失敗として記録]
  G --> X([完了])
  H --> X
```

## 3. 入出力

| 区分 | 内容 |
|---|---|
| 入力ソース | 定期起動の再送処理(配信失敗通知・配信状態・再送回数・抑制リスト・再送上限) |
| 出力先 | 通知の再送、配信状態・再送回数の更新、再送上限到達分・抑制該当分の確定失敗記録 |

## 4. 処理項目定義

| 項目 ID | ステップ | 説明 | 種別 | 実行条件 |
|---|---|---|---|---|
| <span id="PR-01"></span>`PR-01` | 再送処理起動 | スケジューラが定期的に再送処理を起動し、認証・データ取得を行う | トリガー | 定期スケジュール到来時 |
| <span id="PR-02"></span>`PR-02` | 再送対象抽出 | 配信に失敗し、まだ再送上限に達していない通知を再送対象として抽出する | 抽出 | — |
| <span id="PR-03"></span>`PR-03` | 再送可否判定 | 各対象について抑制リスト該当外かつ再送回数が上限(3 回。設計値)未満であり、かつ直近失敗時刻からの経過が次回バックオフ間隔(例 5 分 / 30 分 / 2 時間。設計値)に達していることを判定する | 判定 | 再送対象があるとき |
| <span id="PR-04"></span>`PR-04` | 通知再送 | 条件を満たす対象の通知を再送する | 通知 | 抑制外 かつ 上限未満のとき |
| <span id="PR-05"></span>`PR-05` | 配信状態更新 | 再送結果に応じて配信状態と再送回数を更新する | 記録 | 再送を実施したとき |
| <span id="PR-06"></span>`PR-06` | 確定失敗記録 | 再送回数が上限(3 回。設計値)に到達した分・抑制該当分は再送せず打ち切り、確定失敗として記録する | 例外 | 上限到達 または 抑制該当のとき |

## 5. 入出力一覧

本処理が再送に用いる配信IFと、配信状態・再送回数を記録する通知配信ログを示す。SEQ-098 に出現する API・テーブルのみを対象とする。

| 入出力 | 説明 | 種別 | I/O | CRUD | 参照 |
|---|---|---|---|---|---|
| 通知メール再送 | 配信失敗通知を再送する配信 API | API | 出力 | — | [API-040](../03_apis/API-040.md#API-040) |
| 配信状態確認 | 再送対象の配信状態を確認する API | API | 入力 | — | [API-058](../03_apis/API-058.md#API-058) |
| 通知配信ログ | 配信状態・再送回数を参照し、再送結果・確定失敗を更新する | テーブル | 入出力 | `- R U -` | [TBL-026](../04_database/TBL-026.md#TBL-026) |

## 6. システムイベント一覧

| SEV-ID | イベント ID | 項目 ID | イベント | 処理 |
|---|---|---|---|---|
| <span id="SEV-051"></span>SEV-051 | `SE-01` | [PR-04](#PR-04) | 通知再送 | 抑制外かつ上限未満の対象の通知を再送する |
| <span id="SEV-052"></span>SEV-052 | `SE-02` | [PR-05](#PR-05) | 配信状態更新 | 再送結果に応じて配信状態と再送回数を更新し、上限到達分・抑制該当分は確定失敗として記録する |
