# <span id="SYS-029"></span>SYS-029: 90日経過データ物理削除

> **このページは、論理削除から猶予期間(90 日)を経過したデータを依存関係の順序に従って物理削除し、削除内容を監査記録として残すシステム処理 SYS-029 を定義します。** 処理概要 / 処理フロー図 / 入出力 / 処理項目定義 / 入出力一覧 / システムイベント一覧 の 6 セクションで記述します。

*種別 システム設計 ・ 優先度 P0 ・ ステータス ドラフト*

## 1. 処理概要

退会などにより論理削除された契約・利用者・関連データのうち、論理削除日から猶予期間(90 日)を経過したものを日次で走査し、依存関係の順序に従って物理削除する。削除は従属する関連データから契約・利用者へと進め、削除内容を監査記録として残す。保持義務のあるデータは削除対象から除外し、猶予期間を経過したデータが無い場合は削除を行わず正常終了する。

| システム ID | 処理名 | 種別 | トリガー / スケジュール | 機能概要 |
|---|---|---|---|---|
| `SYS-029` | 90日経過データ物理削除 | batch | 日次の実行スケジュールによる自動起動 | 論理削除から 90 日を経過したデータを依存順に物理削除し、削除内容を監査記録に残す |

| 関連 | 内容 |
|---|---|
| 機能要件 (FR) | [FR-138](../../../01_requirements/02_functional_requirement/06_security-fr.md#FR-138) |
| 業務要件 (BR) | — |
| 業務ルール (RULE) | — |
| 関連システム | — |
| 対応業務UC | [UC-071](../../../01_requirements/04_business_usecases/UC-071.md#UC-071) |

## 2. 処理フロー図

```mermaid
flowchart TB
  S([日次スケジュールにより起動]) --> A[論理削除データを走査し<br/>論理削除から90日経過を判定]
  A --> B{削除対象が<br/>存在するか}
  B -- あり --> C[保持義務のあるデータを<br/>削除対象から除外]
  C --> D[従属データから契約・利用者へ<br/>依存順に物理削除]
  D --> E[削除内容を監査記録として残す]
  E --> F([正常終了:削除あり])
  B -- なし --> G([正常終了:削除なし])
```

## 3. 入出力

| 区分 | 内容 |
|---|---|
| 入力ソース | 日次の実行スケジュール(自動起動)、論理削除済みで論理削除日が記録されたデータ |
| 出力先 | 物理削除されたデータ(不可逆)、削除内容の監査記録 |

## 4. 処理項目定義

| 項目 ID | ステップ | 説明 | 種別 | 実行条件 |
|---|---|---|---|---|
| <span id="PR-01"></span>`PR-01` | 削除対象走査 | 論理削除済みデータを走査し、論理削除から猶予期間(90 日)を経過したものを物理削除の対象として抽出する | 判定 | — |
| <span id="PR-02"></span>`PR-02` | 保持義務除外 | 法令・運用上の保持義務があるデータを削除対象から除外する | 判定 | 削除対象が存在するとき |
| <span id="PR-03"></span>`PR-03` | 依存順物理削除 | 抽出した対象を依存関係の順序に従って物理削除する(従属する関連データから先に削除し、利用者・契約へと進める) | 更新 | 削除対象が存在するとき |
| <span id="PR-04"></span>`PR-04` | 監査記録 | 削除した内容を監査記録として残す | 記録 | 削除を実施したとき |
| <span id="PR-05"></span>`PR-05` | 対象なし正常終了 | 猶予期間を経過したデータが存在しない場合は削除を行わず正常終了する | 例外 | 削除対象が存在しないとき |

## 5. 入出力一覧

本処理が走査・物理削除の対象とするデータと、削除内容を残す監査記録のテーブルを示す。SEQ-092 に対応する関連テーブルを参照として示す。

| 入出力 | 説明 | 種別 | I/O | CRUD | 参照 |
|---|---|---|---|---|---|
| 契約 | 論理削除から90日経過した契約を依存順に物理削除する | テーブル | 出力 | `- R - D` | [TBL-001](../04_database/TBL-001.md#TBL-001) |
| 利用者 | 論理削除から90日経過した利用者を依存順に物理削除する | テーブル | 出力 | `- R - D` | [TBL-002](../04_database/TBL-002.md#TBL-002) |
| 監査記録 | 削除した内容を監査記録として残す | テーブル | 出力 | `C - - -` | [TBL-027](../04_database/TBL-027.md#TBL-027) |

## 6. システムイベント一覧

| SEV-ID | イベント ID | 項目 ID | イベント | 処理 |
|---|---|---|---|---|
| [SEV-055](../02_system_events/SEV-055.md#SEV-055) | `SE-01` | [PR-03](#PR-03) | 依存順物理削除 | 90 日を経過した対象を依存関係の順序に従って物理削除する |
| [SEV-056](../02_system_events/SEV-056.md#SEV-056) | `SE-02` | [PR-04](#PR-04) | 監査記録 | 削除した内容を監査記録として残す |

## 詳細設計への移管候補

- 参照制約・依存関係を踏まえた具体の削除順序(基本設計では従属→契約・利用者の方向のみ示す)。
- 保持義務のあるデータの除外判定ロジック(削除対象外の条件判定)。
- 対象の削除が失敗した場合の中止・再評価のリトライ方式。
