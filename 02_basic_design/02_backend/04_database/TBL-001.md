# <span id="TBL-001"></span>M_USER(ユーザーマスタ)

マスタ 新規

オーナー・メンバーを含む**すべてのユーザー**の認証情報(メール / パスワード / 氏名)を一元保持するユーザーマスタです。オーナーかどうかの判定は `M_CONTRACT.user_id` 参照で行い、本テーブルには契約属性を持ちません。招待直後は `status='pending_activation'` の予約行で `name` / `password_hash` 未設定とし、有効化で確定します。

<!-- p5-item -->
### <span id="item"></span>項目

本テーブルの基本情報です。

| 項目 | 内容 |
|---|---|
| テーブルID | `TBL-001` |
| 物理名 | `M_USER` |
| 論理名 | ユーザーマスタ |
| 概要 | オーナー・メンバーを含む**すべてのユーザー**の認証情報(メール / パスワード / 氏名)を一元保持するユーザーマスタです。オーナーかどうかの判定は `M_CONTRACT.user_id` 参照で行い、本テーブルには契約属性を持ちません。招待直後は `status='pending_activation'` の予約行で `name` / `password_hash` 未設定とし、有効化で確定します。 |
| 主キー | `id` |
| 論理削除 | あり(`valid` フラグ 0/1) |
| 監査項目(日時) | `created_at` / `updated_at` |
| 監査項目(実施者) | — |
<!-- /p5-item -->

### <span id="概要"></span>概要

| 項目       | 内容           |
|------------|----------------|
| テーブル名 | `M_USER`       |
| 論理名     | ユーザーマスタ |
| 主キー     | `id`           |
| 外部キー   | なし           |

### <span id="カラム定義"></span>カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 制約 |
|----|----|----|----|----|----|----|----|----|----|----|
| 1 | ユーザーID | `id` | TEXT | 26 | NOT NULL | ○ |  |  |  |  |
| 2 | メールアドレス | `email` | TEXT | 254 | NOT NULL |  |  | ○ |  | `length(email) <= 254`(RFC 5321 準拠の設計値) |
| 3 | パスワードハッシュ | `password_hash` | TEXT | 255 | NULL可 |  |  |  |  | Argon2id のエンコード文字列(最大 255 文字。§6.1 共通データ規約準拠)。平文は保持しない。招待直後(`pending_activation`)は未設定 |
| 4 | 氏名 | `name` | TEXT | 100 | NULL可 |  |  |  |  | `name IS NULL OR length(name) BETWEEN 1 AND 100`(設計値)。招待直後は未設定 |
| 5 | メール確認日時 | `email_verified_at` | TEXT |  | NULL可 |  |  |  |  |  |
| 6 | 状態 | `status` | TEXT |  | NOT NULL |  |  |  | `'active'` | `status IN ('pending_activation','active','suspended','deleted')` |
| 7 | 有効フラグ | `valid` | INTEGER |  | NOT NULL |  |  |  | `1` | `valid IN (0,1)` |
| 8 | 作成日時 | `created_at` | TEXT |  | NOT NULL |  |  |  |  |  |
| 9 | 更新日時 | `updated_at` | TEXT |  | NOT NULL |  |  |  |  |  |

### <span id="インデックス"></span>インデックス

| No  | インデックス名  | 対象カラム | UNIQUE | 用途                                   |
|-----|-----------------|------------|--------|----------------------------------------|
| 1   | `uq_user_email` | `email`    | ○      | メールアドレスの一意制約・ログイン照会 |

### <span id="コード値"></span>コード値・区分値

| カラム   | 値                   | 意味                       |
|----------|----------------------|----------------------------|
| `status` | `pending_activation` | 招待済み・未有効化(予約行) |
| `status` | `active`             | 有効                       |
| `status` | `suspended`          | 停止                       |
| `status` | `deleted`            | 論理削除                   |

### <span id="traceability"></span>トレーサビリティ

本テーブルを利用する業務スレッド(TR-ID): [TR-001](../../00_traceability/index.md#TR-001) ・ [TR-002](../../00_traceability/index.md#TR-002) ・ [TR-003](../../00_traceability/index.md#TR-003) ・ [TR-006](../../00_traceability/index.md#TR-006) ・ [TR-009](../../00_traceability/index.md#TR-009) ・ [TR-017](../../00_traceability/index.md#TR-017) ・ [TR-019](../../00_traceability/index.md#TR-019) ・ [TR-022](../../00_traceability/index.md#TR-022) ・ [TR-070](../../00_traceability/index.md#TR-070) ・ [TR-077](../../00_traceability/index.md#TR-077) ・ [TR-085](../../00_traceability/index.md#TR-085)。詳細は [トレーサビリティ一覧](../../00_traceability/index.md#matrix-main) のデータベース列を参照。
