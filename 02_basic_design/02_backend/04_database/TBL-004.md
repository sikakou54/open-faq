# <span id="TBL-004"></span>M_PROJECTS(プロジェクト)

FAQ プロジェクトとウィジェット設定を保持します。プロジェクトの所有者(作成者=オーナー)を `owner_user_id` で表し、テナント境界はプロジェクト自身(`id`)で表します。

| ID |
|----|
| TBL-004 |

### <span id="カラム定義"></span>カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 制約 |
|----|----|----|----|----|----|----|----|----|----|----|
| 1 | プロジェクトID | `id` | TEXT | 26 | NOT NULL | ○ |  |  |  |  |
| 2 | オーナーユーザーID | `owner_user_id` | TEXT | 26 | NOT NULL |  | `M_USER(id)` |  |  | プロジェクトの作成者=オーナー(立場判定の権威) |
| 3 | プロジェクト名 | `name` | TEXT | 100 | NOT NULL |  |  |  |  | `length(name) BETWEEN 1 AND 100` |
| 4 | 説明 | `description` | TEXT | 500 | NULL可 |  |  |  |  | `length(description) <= 500`(設計値) |
| 5 | 状態 | `status` | TEXT |  | NOT NULL |  |  |  | `'active'` | `status IN ('active','deleted')` |
| 6 | ウィジェット公開鍵 | `widget_public_key` | TEXT | 40 | NOT NULL |  |  | ○ |  | `pk_live_` + 32 文字(SCR-011 IT-02) |
| 7 | 連絡先メール | `contact_email` | TEXT | 254 | NULL可 |  |  |  |  |  |
| 8 | 連絡先確認日時 | `contact_verified_at` | TEXT |  | NULL可 |  |  |  |  |  |
| 9 | 設定JSON | `settings` | TEXT |  | NOT NULL |  |  |  | `'{}'` | ウィジェット表示設定の JSON 文字列。構造は「コード値・区分値」直後の `settings` JSON 構造を参照 |
| 10 | 有効フラグ | `valid` | INTEGER |  | NOT NULL |  |  |  | `1` | `valid IN (0,1)` |
| 11 | 削除日時 | `deleted_at` | TEXT |  | NULL可 |  |  |  |  | 状態が `deleted` になった時にセットする。保持期間・削除猶予の具体値は [システム仕様書 §4](../../07_system-spec.md#4-データ保持期間削除猶予) を参照し、物理削除は [SYS-027](../01_system/SYS-027.md#SYS-027) が担う |
| 12 | 通知送信抑制フラグ | `notification_suppressed` | INTEGER |  | NOT NULL | | |  | `0` | `notification_suppressed IN (0,1)`。`status`(有効/削除)とは別軸のフラグであり、送信品質悪化による一時的な抑制状態を表す(設計判断) |
| 13 | 作成日時 | `created_at` | TEXT |  | NOT NULL |  |  |  |  |  |
| 14 | 更新日時 | `updated_at` | TEXT |  | NOT NULL |  |  |  |  |  |

### <span id="インデックス"></span>インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|----|----|----|----|----|
| 1 | `idx_projects_status` | `(owner_user_id, status)` |  | オーナー別(My)プロジェクト一覧 |
| 2 | `idx_projects_widget_key` | `widget_public_key` | ○ | ウィジェット鍵検索 |

### <span id="コード値"></span>コード値・区分値

| カラム   | 値        | 意味 |
|----------|-----------|------|
| `status` | `active`  | [状態モデル §5](../../08_state-model.md#5-プロジェクト状態) |
| `status` | `deleted` | [状態モデル §5](../../08_state-model.md#5-プロジェクト状態) |
| `notification_suppressed` | `0` | 送信可(通常状態) |
| `notification_suppressed` | `1` | 送信抑制中(通知品質の悪化により自動抑制。悪化・回復の判定契機は [SYS-007](../01_system/SYS-007.md#SYS-007)、判定ロジックは [IPO-016](../../../03_detail_design/04_ipo/IPO-016.md#IPO-016) を参照) |

### <span id="settings"></span>`settings` JSON 構造(ウィジェット表示設定)

`settings`(JSON 文字列)はウィジェットの見た目・表示文言を保持する。画面 [SCR-011 ウィジェット設定](../../01_frontend/01_screens/SCR-011.md#SCR-011)(主色 IT-05 / 表示位置 IT-10 / 見出し IT-11 / 初期メッセージ IT-12)で設定し、[API-037 ウィジェット起動](../03_apis/API-037.md#API-037) の `projectConfig` へ整形して返却する。各キーは API 層でバリデーションする(下限・上限・列挙)。未設定キーは既定値を適用する。

| キー | 型 | 必須 | 既定値 | 制約 / 説明 | SCR-011 項目 | API-037 `projectConfig` 対応 |
|----|----|----|----|----|----|----|
| `headerColor` | string | 任意 | `"#5E6AD2"`(設計値) | ウィジェット主色。`#` + 6 桁 HEX(`^#[0-9A-Fa-f]{6}$`) | IT-05 主色 | `headerColor` |
| `placement` | string(enum) | 任意 | `"bottom-right"`(設計値) | 表示位置。`bottom-left`(左下)/ `bottom-right`(右下) | IT-10 表示位置 | `placement` |
| `headerTitle` | string | 任意 | `""`(未設定時はウィジェット既定文言) | ウィジェット展開時のヘッダー見出し。0〜60 文字(設計値・SCR-011 §4#9 を正本とする) | IT-11 見出し | `headerTitle` |
| `welcomeMessage` | string | 任意 | `""`(未設定時はウィジェット既定文言) | ウィジェットを開いた際にボットが最初に送る初期メッセージ。0〜200 文字(設計値・SCR-011 §4#10 を正本とする) | IT-12 初期メッセージ | (起動時にウィジェットへ反映) |

> [!NOTE]
> 連絡先メール(API-037 `projectConfig.contactEmail`)は `settings` JSON ではなく `contact_email` カラムから返却する。`settings` には公開キー(`widget_public_key` カラム)・連絡先(`contact_email` カラム)を重複保持しない。`placement` の enum 値(`bottom-left` / `bottom-right`)・文字数上限は API 層と SCR-011 の整合のための設計値であり、詳細設計で確定する。
