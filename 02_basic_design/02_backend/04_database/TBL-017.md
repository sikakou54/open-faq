# <span id="TBL-017"></span>T_INQUIRIES(未解決質問)

FAQ 登録前の未解決質問を保持します。

| ID |
|----|
| TBL-017 |

### <span id="カラム定義"></span>カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 制約 |
|---:|----|----|----|---:|----|----|----|----|----|----|
| 1 | ID | `id` | TEXT | 26 | NO | ○ |  |  |  |  |
| 2 | プロジェクト ID | `project_id` | TEXT | 26 | NO |  | `M_PROJECTS(id)` |  |  |  |
| 3 | 問い合わせコード | `inquiry_code` | TEXT | 20 | NO |  |  | ○ |  | 採番体系は「コード値・区分値」直後の `inquiry_code` 採番体系を参照 |
| 4 | 元質問ログ ID | `question_log_id` | TEXT | 26 | YES |  | `H_QUESTION_LOGS(id)` |  |  |  |
| 5 | 質問本文 | `user_question` | TEXT | 2000 | NO |  |  |  |  | `length(user_question) BETWEEN 1 AND 2000`(H_QUESTION_LOGS と整合。設計値) |
| 6 | 状況 | `status` | TEXT | \- | NO |  |  |  | `'open'` | `status IN ('open','closed')` |
| 7 | 有効フラグ | `valid` | INTEGER | \- | NO |  |  |  | `1` | `valid IN (0,1)` |
| 8 | 作成日時 | `created_at` | TEXT | \- | NO |  |  |  |  |  |
| 9 | 更新日時 | `updated_at` | TEXT | \- | NO |  |  |  |  |  |
| 10 | 削除日時 | `deleted_at` | TEXT | \- | YES |  |  |  |  | `valid=0` へ更新した時刻をセットする。保持期間・削除猶予は[システム仕様書 §4](../../07_system-spec.md#4-データ保持期間削除猶予)を参照し物理削除は[SYS-027](../01_system/SYS-027.md#SYS-027)が担う |

### <span id="インデックス"></span>インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|----|----|----|----|
| 1 | `idx_inquiries_project_status_created` | `(project_id, status, created_at DESC)` |  | プロジェクト × 状況別新着 |
| 2 | `uq_inquiries_code` | `inquiry_code` | ○ | 問い合わせコード一意 |
| 3 | `idx_inquiries_valid` | `valid` WHERE `valid = 0` |  | 論理削除対象抽出 |
| 4 | `idx_inquiries_deleted_at` | `deleted_at` WHERE `deleted_at IS NOT NULL` |  | 論理削除確定日時からの物理削除対象抽出 |

### <span id="コード値区分値"></span>コード値・区分値

| カラム名 | 値       | 意味 |
|----------|----------|------|
| `status` | `open`   | [状態モデル §4.2](../../08_state-model.md#42-未解決質問状態) |
| `status` | `closed` | [状態モデル §4.2](../../08_state-model.md#42-未解決質問状態) |

状態の意味は [状態モデル §4.2](../../08_state-model.md#42-未解決質問状態) を参照する。

### <span id="inquiry_code"></span>`inquiry_code` 採番体系

`inquiry_code` は未解決質問の利用者向け管理識別子で、[API-038 ウィジェット質問送信](../03_apis/API-038.md#API-038) のレスポンス `inquiryCode` として返却する(ウィジェットには描画しない)。プロジェクト横断で一意(インデックス `uq_inquiries_code`)。

採番体系(設計値):

- 形式: `INQ-YYYYMMDD-<ランダム英数字 6 桁>`(プレフィックス `INQ-` + 生成日(UTC)8 桁 + ハイフン + ランダムサフィックス 6 桁)。
- 桁数: 最大 20 文字(`INQ-` 4 + 日付 8 + `-` 1 + サフィックス 6 = 19 文字)。
- 例: `INQ-20260513-XYZ123`(API-038 のレスポンス例と整合)。

> [!NOTE]
> サフィックス長・文字集合(英大文字 + 数字など)は設計値であり、衝突回避のため一意制約違反時は再採番する。日付部のタイムゾーン(UTC)・桁構成は詳細設計で確定する。

> [!NOTE]
> [API-069 ウィジェット回答フィードバック送信](../03_apis/API-069.md#API-069) で `feedback=unhelpful` を受け付けた場合も本テーブルに登録する。元質問ログ(`question_log_id`)の [`H_QUESTION_LOGS.result_reason_code`](TBL-025.md#TBL-025) は `user_unresolved` とし、利用者が回答後に「役に立たなかった」と判断した未解決質問として扱う。
