# <span id="TBL-032"></span>T_BILLING_WEBHOOK_LOG(課金Webhook受信ログ)

トランザクション 課金 一定期間保持

課金プロバイダから受信した決済・契約状態通知を、検証結果・取込状態とともに記録します。重複受信の検出と取込失敗の再処理に用います。

<!-- p5-item -->
### <span id="item"></span>項目

本テーブルの基本情報と、参照元の業務ユースケース / API(逆引き)です。

| 項目 | 内容 |
|---|---|
| テーブルID | `TBL-032` |
| 物理名 | `T_BILLING_WEBHOOK_LOG` |
| 論理名 | 課金Webhook受信ログ |
| 概要 | 課金プロバイダから受信した通知を検証結果・取込状態とともに記録し、重複検出と失敗再処理に用います。 |
| 主キー | `id` |
| 論理削除 | なし(追記専用ログ・保持期間経過で物理削除) |
| 監査項目(日時) | `received_at` / `processed_at` |
| 監査項目(実施者) | —(外部システム起点) |
| 対応業務UC(逆引き) | [UC-061](../../../01_requirements/04_business_usecases/UC-061.md#UC-061) |
| 利用API(逆引き) | [API-060](../03_apis/API-060.md#API-060) |
<!-- /p5-item -->

### <span id="概要"></span>概要

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr>
<th>項目</th>
<th>内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>テーブル名</td>
<td><code>T_BILLING_WEBHOOK_LOG</code></td>
</tr>
<tr>
<td>論理名</td>
<td>課金Webhook受信ログ</td>
</tr>
<tr>
<td>主キー</td>
<td><code>id</code></td>
</tr>
<tr>
<td>外部キー</td>
<td><ul>
<li><code>contract_id</code> → <code>M_CONTRACT(id)</code>(特定できた場合のみ)</li>
</ul></td>
</tr>
</tbody>
</table>

### <span id="カラム定義"></span>カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 制約 |
|---:|----|----|----|---:|----|----|----|----|----|----|
| 1 | ID | `id` | TEXT | \- | NO | ○ |  |  |  |  |
| 2 | 課金プロバイダ | `provider` | TEXT | \- | NO |  |  |  |  |  |
| 3 | プロバイダ側イベント ID | `event_id` | TEXT | \- | NO |  |  | ○ |  | 重複排除キー |
| 4 | イベント種別 | `event_type` | TEXT | \- | NO |  |  |  |  |  |
| 5 | 契約 owner ID | `contract_id` | TEXT | \- | YES |  | `M_CONTRACT(id)` |  |  |  |
| 6 | 署名検証結果 | `signature_valid` | INTEGER | \- | NO |  |  |  |  | `signature_valid IN (0,1)` |
| 7 | 取込状態 | `status` | TEXT | \- | NO |  |  |  | `'received'` | `status IN ('received','processed','failed','skipped')` |
| 8 | 受信ペイロード | `payload` | TEXT | \- | NO |  |  |  |  |  |
| 9 | 失敗内容 | `error_detail` | TEXT | \- | YES |  |  |  |  |  |
| 10 | 受信日時 | `received_at` | TEXT | \- | NO |  |  |  |  |  |
| 11 | 取込日時 | `processed_at` | TEXT | \- | YES |  |  |  |  |  |

### <span id="インデックス"></span>インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|----|----|----|----|
| 1 | `uq_billing_wh_event` | `provider`, `event_id` | ○ | 重複受信の検出(冪等) |
| 2 | `idx_billing_wh_status` | `status` |  | 失敗再処理の抽出 |
| 3 | `idx_billing_wh_received` | `received_at` |  | 保持期間経過分の物理削除 |

### <span id="コード値区分値"></span>コード値・区分値

| カラム名 | 値 | 意味 |
|----------|------|------|
| `status` | `received` | 受信済み(未取込) |
| `status` | `processed` | 取込完了 |
| `status` | `failed` | 取込失敗(再処理対象) |
| `status` | `skipped` | 重複のため取込スキップ |
| `signature_valid` | `1` | 署名検証成功 |
| `signature_valid` | `0` | 署名検証失敗(不正受信) |

### <span id="usedby"></span>使用元(画面 / API)

このテーブルを読み書きする画面と API です(逆引き)。

**画面** — **API** [API-060](../03_apis/index.md#API-060)
