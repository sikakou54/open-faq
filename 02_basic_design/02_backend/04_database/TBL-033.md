# <span id="TBL-033"></span>TP_IMPORT_JOBS(FAQ取込ジョブ)

ワーク

FAQ 一括取込(CSV インポート)の取込ジョブの状態・進捗・結果サマリを保持するテーブルです。画面のポーリングによる進捗表示と結果確認に用います。

<!-- p5-item -->
### <span id="item"></span>項目

本テーブルの基本情報です。

| 項目 | 内容 |
|---|---|
| テーブルID | `TBL-033` |
| 物理名 | `TP_IMPORT_JOBS` |
| 論理名 | FAQ取込ジョブ |
| 概要 | FAQ 一括取込ジョブの状態・進捗・結果サマリを保持し、画面のポーリングによる進捗表示・結果確認に用います。 |
| 主キー | `id` |
| 論理削除 | なし(完了後に TTL で消去・追記) |
| 監査項目(日時) | `created_at` / `updated_at` |
| 監査項目(実施者) | —(取込ジョブ起点) |
<!-- /p5-item -->

### <span id="概要"></span>概要

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr>
<th>項目</th>
<th>内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>テーブル名</td>
<td><code>TP_IMPORT_JOBS</code></td>
</tr>
<tr>
<td>論理名</td>
<td>FAQ取込ジョブ</td>
</tr>
<tr>
<td>主キー</td>
<td><code>id</code></td>
</tr>
<tr>
<td>外部キー</td>
<td><ul>
<li><code>project_id</code> → <code>M_PROJECTS(id)</code></li>
</ul></td>
</tr>
</tbody>
</table>

### <span id="カラム定義"></span>カラム定義

| No | 論理名 | 物理名 | データ型 | 桁数 | NULL | PK | FK | UNIQUE | DEFAULT | 制約 |
|---:|----|----|----|---:|----|----|----|----|----|----|
| 1 | ジョブ ID | `id` | TEXT | 26 | NO | ○ |  |  |  | ULID(`job_` 接頭は付けない) |
| 2 | プロジェクト ID | `project_id` | TEXT | 26 | NO |  | `M_PROJECTS(id)` |  |  |  |
| 3 | 状態 | `status` | TEXT | \- | NO |  |  |  |  | `status IN ('queued','processing','completed','failed')`。取りうる値は「コード値・区分値」を参照 |
| 4 | 総行数 | `total_count` | INTEGER | \- | YES |  |  |  |  | 取込対象の総行数(受付確定後に設定) |
| 5 | 処理済み行数 | `processed_count` | INTEGER | \- | NO |  |  |  | `0` | 成功 + 失敗の合計 |
| 6 | 成功行数 | `succeeded_count` | INTEGER | \- | NO |  |  |  | `0` |  |
| 7 | 失敗行数 | `failed_count` | INTEGER | \- | NO |  |  |  | `0` |  |
| 8 | 失敗明細 | `error_summary` | TEXT | \- | YES |  |  |  |  | JSON。構造は「`error_summary` JSON 構造」を参照。最大件数は詳細設計(設計値) |
| 9 | 作成日時 | `created_at` | TEXT | \- | NO |  |  |  |  | ジョブ受付日時 |
| 10 | 更新日時 | `updated_at` | TEXT | \- | NO |  |  |  |  | 進捗・完了時に更新 |

### <span id="インデックス"></span>インデックス

| No | インデックス名 | 対象カラム | UNIQUE | 用途 |
|---:|----|----|----|----|
| 1 | `idx_import_jobs_project_created` | `(project_id, created_at)` |  | プロジェクト別の取込ジョブ抽出・新着順表示 |

### <span id="コード値区分値"></span>コード値・区分値

| カラム名 | 値 | 意味 |
|----------|------|------|
| `status` | `queued` | 受付済み(処理待ち) |
| `status` | `processing` | 取込処理中 |
| `status` | `completed` | 取込完了(全件成功 / 部分失敗を含む) |
| `status` | `failed` | ジョブ異常終了 / タイムアウト |

### <span id="error_summary"></span>`error_summary` JSON 構造(失敗明細)

`error_summary`(JSON 文字列)は、取込に失敗した行の明細を保持する。画面のエラー一覧表示に用いる。

| 項目 | 規約 |
|----|----|
| 全体の型 | JSON オブジェクト。例 `{ "errors": [ { "row": 12, "code": "CSV_INVALID", "message": "..." } ] }` |
| `errors` | 配列。失敗行ごとの明細を要素に持つ |
| `errors[].row` | 整数。CSV の行番号(ヘッダ行を除く 1 始まり) |
| `errors[].code` | 文字列。失敗理由のエラーコード(例 `CSV_INVALID`) |
| `errors[].message` | 文字列。失敗理由の説明 |
| 最大件数 | 詳細設計で定義する(設計値) |
| 既定値 | `NULL`(失敗行なし) |

### <span id="traceability"></span>トレーサビリティ

本テーブルを利用する業務スレッド(TR-ID): [TR-050](../../00_traceability/index.md#TR-050)。詳細は [トレーサビリティ一覧](../../00_traceability/index.md#matrix-main) のデータベース列を参照。
