| 画面 ID | 画面名 | トレーサビリティID |
|----|----|----|
| <span id="SCR-018"></span>SCR-018 | メール確認 | [TR-003](../../00_traceability/index.md#TR-003) |

| ステークホルダ                  | 対象 |
|---------------------------------|------|
| 対象ユーザー(認証前 / トークン) | ◯    |

## <span id="1-画面概要"></span>1. 画面概要

新規登録後にメール内の確認リンクから本人確認を完了する画面です。送信済み・確認成功・確認失敗の 3 状態を持ち、メール再送とログインへの導線を提供します。

> [!NOTE]
> **補足** 本画面は認証前(メール確認トークンによる本人確認)に表示されるため権限は不要です。確認リンクの有効期限は 24 時間で、期限切れ・使用済みの場合は確認失敗状態を表示し新規登録への復旧導線を出します。

## <span id="2-画面遷移図"></span>2. 画面遷移図

本画面からの画面遷移を、画面 ID・画面名とイベント(操作)で示します。

```mermaid
flowchart LR
  S1["SCR-018 メール確認"]
  S2["SCR-002 アカウント登録"]
  S3["SCR-001 ログイン"]
  S1 -->|メールアドレスを変更する| S2
  S1 -->|確認失敗 / 新規登録からやり直す| S2
  S1 -->|確認成功 / ログインする| S3
```

## <span id="3-画面レイアウト"></span>3. 画面レイアウト

本画面の代表状態(送信済み)を示します。確認成功・確認失敗の各状態は §4 の `表示条件` で定義します。

![SCR-018 画面レイアウト](mocks/SCR-018-1.png)

## <span id="4-画面項目"></span>4. 画面項目

本画面が各状態で表示する入出力項目を定義します。`表示条件` は項目が表示される状態を示します。

| # | 項目 | 種類 | 必須 | 最大長 | 初期値 | 表示条件 |
|----|----|----|----|----|----|----|
| 1 | 状態タイムライン | div | — | — | — | 送信済み状態 |
| 2 | 送信先メールアドレス | div | — | — | — | 送信済み状態 |
| 3 | メール送信済み案内 | alert | — | — | — | 送信済み状態 |
| 4 | メールを再送する | button | — | — | — | 送信済み状態 |
| 5 | メールアドレスを変更する | link | — | — | — | 送信済み状態 |
| 6 | 確認成功案内 | alert | — | — | — | 確認成功状態 |
| 7 | ログインするボタン | button | — | — | — | 確認成功状態 |
| 8 | 確認失敗案内 | alert | — | — | — | 確認失敗状態(リンク期限切れ・使用済み) |
| 9 | 新規登録からやり直すボタン | button | — | — | — | 確認失敗状態(リンク期限切れ・使用済み) |

## <span id="5-バリデーション"></span>5. バリデーション

本画面はメール確認トークンの検証(初期表示)とボタン操作のみで、利用者の入力フォームを持ちません。(本画面に入力検証はありません)

## <span id="6-イベント"></span>6. イベント

本画面のイベント(初期表示・各操作)ごとに、対象の画面項目を定義します。各イベントの処理内容は [7. 画面イベント詳細](#7-画面イベント詳細) で定義します。

<table>
<colgroup>
<col style="width: 18%" />
<col style="width: 22%" />
<col style="width: 60%" />
</colgroup>
<thead>
<tr>
<th>EVT-ID</th>
<th>画面項目</th>
<th>イベント</th>
</tr>
</thead>
<tbody>
<tr>
<td><span id="EVT-132"></span>EVT-132</td>
<td>—</td>
<td>初期表示</td>
</tr>
<tr>
<td><span id="EVT-133"></span>EVT-133</td>
<td>#4</td>
<td>「メールを再送する」を押下</td>
</tr>
<tr>
<td><span id="EVT-134"></span>EVT-134</td>
<td>#5</td>
<td>「メールアドレスを変更する」を押下</td>
</tr>
<tr>
<td><span id="EVT-135"></span>EVT-135</td>
<td>#9</td>
<td>「新規登録からやり直す」を押下</td>
</tr>
<tr>
<td><span id="EVT-136"></span>EVT-136</td>
<td>#7</td>
<td>「ログインする」を押下</td>
</tr>
</tbody>
</table>

## <span id="7-画面イベント詳細"></span>7. 画面イベント詳細

各イベントの処理内容を定義します。

<table>
<colgroup>
<col style="width: 14%" />
<col style="width: 86%" />
</colgroup>
<thead>
<tr>
<th>EVT-ID</th>
<th>処理</th>
</tr>
</thead>
<tbody>
<tr>
<td>EVT-132</td>
<td>初期表示時に URL パラメータ(確認トークンの有無)で分岐する:<pre>
 ┣ トークンなし(SCR-002 完了後の案内表示)
 ┃   状態タイムライン(#1)・送信先メールアドレス(#2)・メール送信済み案内(#3)を表示し、
 ┃   メールを再送する(#4)・メールアドレスを変更する(#5)を活性表示する
 ┗ トークンあり(確認リンクからのアクセス)
     <a href="../../02_backend/03_apis/API-006.md#API-006">メール確認</a> API でトークンを検証し結果で分岐する
        ┣ 成功: 確認成功案内(#6)とログインするボタン(#7)を表示する
        ┗ 失敗(期限切れ・使用済み): 確認失敗案内(#8・EM-01)と新規登録からやり直すボタン(#9)を表示する
</pre></td>
</tr>
<tr>
<td>EVT-133</td>
<td>「メールを再送する」押下時に <a href="../../02_backend/03_apis/API-001.md#API-001">新規登録(確認メール再送)</a> API を発行し、確認メールを再送する:<pre>
1. 成功時: メールを再送する(#4)を非活性化し、レート制限(5 分以内 1 回)のカウントダウンを併記表示する
2. レート制限中: ボタンは非活性のまま(カウントダウン終了で再活性)
3. 失敗時: エラー(EM-02)を表示する
</pre></td>
</tr>
<tr>
<td>EVT-134</td>
<td>「メールアドレスを変更する」押下時に SCR-002 アカウント登録へ遷移し、メールアドレスをやり直す</td>
</tr>
<tr>
<td>EVT-135</td>
<td>「新規登録からやり直す」押下時に確認失敗状態から SCR-002 アカウント登録へ遷移し、登録を最初からやり直す</td>
</tr>
<tr>
<td>EVT-136</td>
<td>「ログインする」押下時に確認成功状態から SCR-001 ログインへ遷移する</td>
</tr>
</tbody>
</table>

## <span id="8-エラーメッセージ"></span>8. エラーメッセージ

本画面が表示するエラー・警告メッセージを定義します。

| エラーコード | エラーメッセージ |
|----|----|
| EM-01 | 確認リンクが期限切れ、または使用済みです。新規登録からやり直してください(有効期限 24 時間) |
| EM-02 | 確認メールの再送に失敗しました。しばらく経ってからお試しください |
</content>
</invoke>
