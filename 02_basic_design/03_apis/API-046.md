<!-- portal-top -->
[設計ポータル](../../README.md) ／ [基本設計](../index.md) ／ [API設計](index.md) ／ **API-046: プロジェクト上限・アラート取得**
<!-- /portal-top -->

# <span id="API-046"></span>API-046: プロジェクト上限・アラート取得

> **このページは プロジェクト上限・アラート取得 API の契約を定義します。** 当該プロジェクトの質問数について利用状況・月次上限件数・アラート設定を返す。

*版数 v2.0 ・ 更新 2026-06-21 ・ 再構成 P4*

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-046` |
| API名 | プロジェクト上限・アラート取得 |
| 対応業務UC | [UC-034](../../01_requirements/04_business_usecases/UC-034.md#UC-034) ・ [UC-035](../../01_requirements/04_business_usecases/UC-035.md#UC-035) ・ [UC-057](../../01_requirements/04_business_usecases/UC-057.md#UC-057) ・ [UC-058](../../01_requirements/04_business_usecases/UC-058.md#UC-058) |
| 対応画面ID | [SCR-026](../01_screens/SCR-026.md#SCR-026) ・ [SCR-027](../01_screens/SCR-027.md#SCR-027) |
| 対応画面イベントID | [EVT-199](../02_screen_events/EVT-199.md#EVT-199) ・ [EVT-202](../02_screen_events/EVT-202.md#EVT-202) |
| エンドポイント | `/projects/{id}/quota-limits` |
| HTTPメソッド | GET |
| 認証 | Cookie |
| 認可 | オーナー / 当該プロジェクトのメンバー(閲覧) |

## 処理概要

当該プロジェクトの質問数について利用状況・月次上限件数・アラート設定を返す。

| 処理 ID | 処理内容                                                  |
|---------|-----------------------------------------------------------|
| P-01    | 認証・認可(対象プロジェクトへのアクセス権)を検証する      |
| P-02    | 質問数の利用状況・月次上限・アラート設定を取得する        |
| P-03    | 上限消化時の参考課金額(`yenEquivalent`)を算出して返却する |

## リクエスト

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{ "id": "プロジェクト ID" }
```

| 項目 | 型     | 説明                        |
|------|--------|-----------------------------|
| `id` | string | 対象プロジェクトの ID。必須 |

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

```json
{
  "projectId": "prj_...",
  "period": "2026-05-01T00:00:00Z",
  "questions": { "used": 750, "limitEnabled": true, "limit": 5000, "percentage": 15, "alertThresholds": [80, 100], "yenEquivalent": 2000, "source": "owner" }
}
```

| 項目 | 型 | 説明 |
|----|----|----|
| `projectId` | string | 対象プロジェクトの ID |
| `period` | string (ISO 8601) | 集計対象期間の起点 |
| `questions` | object | 質問数の利用状況・上限・アラート設定 |
| `　└ used` | integer | 当月の質問数 |
| `　└ limitEnabled` | boolean | 月次上限が有効か(OFF 時は `false`) |
| `　└ limit` | integer | 月次上限件数(OFF 時は `null`) |
| `　└ percentage` | integer | 上限に対する利用率(%、OFF 時は `null`) |
| `　└ alertThresholds` | array | アラート発火の閾値(%)の配列 |
| `　└ yenEquivalent` | integer | 上限消化時の参考課金額(OFF 時は `null`) |
| `　└ source` | enum | 上限設定元。`owner` |

`yenEquivalent` は上限件数をすべて利用した場合の最大課金額の参考値で、`billableCount = max(0, limit - 適用中の質問数無料利用枠)`、`yenEquivalent = billableCount × 質問単価` により算出する。画面では「5,000件 - 1,000件(無料枠) = 4,000件 (¥2,000 / 月)」の形式で表示する。上限 OFF 時は `limitEnabled=false`、`limit=null`、`percentage=null`、`yenEquivalent=null` を返す。SCR-026 は「今月の利用上限」を「OFF」と表示し、「利用量」には割合・件数比・ProgressBar・状態バッジを描画しない。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([API 共通仕様](index.md#conv) の標準エラー体系に従う)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`M_PRJ_QUOTA_LIMITS`](../04_database/TBL-009.md#TBL-009) | `-R--` |
| [`T_USAGE_METER`](../04_database/TBL-020.md#TBL-020) | `-R--` |

## 備考

再構成 P4 で旧 API ID `API-046`(旧グループ別 API 設計)から導出・フラット採番。

---

<!-- portal-bottom -->
[← API設計](index.md) ・ [基本設計](../index.md) ・ [↑ 設計ポータル](../../README.md)
<!-- /portal-bottom -->
