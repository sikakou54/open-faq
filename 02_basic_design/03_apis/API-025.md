<!-- portal-top -->
[設計ポータル](../../README.md) ／ [基本設計](../index.md) ／ [API設計](index.md) ／ **API-025: FAQ 一覧**
<!-- /portal-top -->

# <span id="API-025"></span>API-025: FAQ 一覧

> **このページは FAQ 一覧 API の契約を定義します。** 状態 / プロジェクト / キーワードで FAQ を一覧取得する。

*版数 v2.0 ・ 更新 2026-06-21 ・ 再構成 P4*

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-025` |
| API名 | FAQ 一覧 |
| 対応業務UC | [UC-024](../../01_requirements/04_business_usecases/UC-024.md#UC-024) ・ [UC-027](../../01_requirements/04_business_usecases/UC-027.md#UC-027) ・ [UC-026](../../01_requirements/04_business_usecases/UC-026.md#UC-026) |
| 対応画面ID | [SCR-008](../01_screens/SCR-008.md#SCR-008) |
| 対応画面イベントID | [EVT-062](../02_screen_events/EVT-062.md#EVT-062) ・ [EVT-063](../02_screen_events/EVT-063.md#EVT-063) ・ [EVT-064](../02_screen_events/EVT-064.md#EVT-064) ・ [EVT-065](../02_screen_events/EVT-065.md#EVT-065) |
| エンドポイント | `/faqs` |
| HTTPメソッド | GET |
| 認証 | Cookie |
| 認可 | オーナー / 当該プロジェクトのメンバー |

## 処理概要

状態 / プロジェクト / キーワードで FAQ を一覧取得する。

| 処理 ID | 処理内容                                                      |
|---------|---------------------------------------------------------------|
| P-01    | 認証・認可(オーナー / 当該プロジェクトの利用者)を検証する     |
| P-02    | 状態・プロジェクト・キーワードのフィルタ条件で FAQ を取得する |
| P-03    | ページング(カーソル)で整形して返す                            |

## リクエスト

#### Query Parameters

```json
{
  "status": "draft(任意)",
  "projectId": "(必須)",
  "keyword": "(任意)",
  "cursor": "(共通仕様)",
  "limit": "(共通仕様)"
}
```

| 項目 | 型 | 説明 |
|----|----|----|
| `status` | enum | 状態での絞り込み(任意・`draft` / `published` / `hidden`) |
| `projectId` | string | 対象プロジェクトの ID(必須) |
| `keyword` | string | キーワードでの絞り込み(任意) |
| `cursor` | string | 次ページ取得用カーソル(任意・共通ページング仕様) |
| `limit` | integer | 1 ページの取得件数(任意・共通ページング仕様) |

#### Path Parameters

```json
{}
```

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

FAQ の配列 + `nextCursor`(構造は API-025 のレスポンスに準ずる)。

| 項目 | 型 | 説明 |
|----|----|----|
| `items` | array\<object\> | フィルタ条件に一致した FAQ の一覧(各要素の構造は API-031 全文検索のレスポンスに準ずる) |
| `nextCursor` | string | 次ページ取得用カーソル(末尾は `null`) |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

403 `PROJECT_ACCESS_DENIED` ほか(本 API のエラー節を参照)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`M_FAQS`](../04_database/TBL-006.md#TBL-006) | `-R--` |

## 備考

再構成 P4 で旧 API ID `API-025`(旧グループ別 API 設計)から導出・フラット採番。

---

<!-- portal-bottom -->
[← API設計](index.md) ・ [基本設計](../index.md) ・ [↑ 設計ポータル](../../README.md)
<!-- /portal-bottom -->
