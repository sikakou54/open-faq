<!-- portal-top -->
[設計ポータル](../../README.md) ／ [基本設計](../index.md) ／ [API設計](index.md) ／ **API-059: 外部 Webhook(Resend)**
<!-- /portal-top -->

# <span id="API-059"></span>API-059: 外部 Webhook(Resend)

> **このページは 外部 Webhook(Resend) API の契約を定義します。** メール配信状態 Webhook を受信し配信状態 / 抑制リストを更新する。

*版数 v2.0 ・ 更新 2026-06-21 ・ 再構成 P4*

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-059` |
| API名 | 外部 Webhook(Resend) |
| 対応業務UC | [UC-231](../../01_requirements/04_business_usecases/UC-231.md#UC-231) |
| 対応画面ID | — |
| 対応画面イベントID | — |
| エンドポイント | `/webhooks/resend` |
| HTTPメソッド | POST |
| 認証 | Resend 署名検証(HMAC-SHA256) |
| 認可 | —(署名検証のみ) |

## 処理概要

メール配信状態 Webhook を受信し配信状態 / 抑制リストを更新する。

| 処理 ID | 処理内容 |
|----|----|
| P-01 | Resend 署名(HMAC-SHA256)を検証する(失敗時は 401) |
| P-02 | イベント種別を判定する |
| P-03 | `H_NOTIF_LOGS.delivery_state` を遷移する |
| P-04 | `bounced` / `complained` の場合は `M_EMAIL_SUPPRESS` に追加する(全契約横断) |
| P-05 | 受領応答を返す |

## リクエスト

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

(Resend 仕様準拠):

```json
{
  "type": "email.delivered|email.bounced|email.complained|email.delivery_delayed",
  "data": {
    "message_id": "...",
    "to": "...",
    "subject": "...",
    "timestamp": "..."
  }
}
```

| 項目 | 型 | 説明 |
|----|----|----|
| `type` | enum | 配信イベント種別(`email.delivered` / `email.bounced` / `email.complained` / `email.delivery_delayed`) |
| `data` | object | イベント詳細データ |
| 　└ `message_id` | string | 対象メールのメッセージ ID |
| 　└ `to` | string | 宛先メールアドレス |
| 　└ `subject` | string | メール件名 |
| 　└ `timestamp` | string (ISO 8601) | イベント発生時刻 |

## レスポンス

本 API の戻り値は本文の型定義を参照する。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

| HTTP ステータス | エラーコード        | 内容                           |
|-----------------|---------------------|--------------------------------|
| 401 | `SIGNATURE_INVALID` → [ERR-034](../07_errors/ERR-034.md#ERR-034) | 署名検証失敗 |
| 200 | (冪等) → [ERR-035](../07_errors/ERR-035.md#ERR-035) | 既存処理結果を返却(冪等性違反) |
## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`H_NOTIF_LOGS`](../04_database/TBL-026.md#TBL-026) | `--U-` |
| [`M_EMAIL_SUPPRESS`](../04_database/TBL-007.md#TBL-007) | `C---` |
| [`H_AUDIT_LOGS`](../04_database/TBL-027.md#TBL-027) | `C---` |

## 備考

再構成 P4 で旧 API ID `API-059`(旧グループ別 API 設計)から導出・フラット採番。

---

<!-- portal-bottom -->
[← API設計](index.md) ・ [基本設計](../index.md) ・ [↑ 設計ポータル](../../README.md)
<!-- /portal-bottom -->
