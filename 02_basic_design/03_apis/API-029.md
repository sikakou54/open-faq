<!-- portal-top -->
[設計ポータル](../../README.md) ／ [基本設計](../index.md) ／ [API設計](index.md) ／ **API-029: FAQ インポートテンプレート**
<!-- /portal-top -->

# <span id="API-029"></span>API-029: FAQ インポートテンプレート

> **このページは FAQ インポートテンプレート API の契約を定義します。** ヘッダ行のみの CSV テンプレートを返す。

*版数 v2.0 ・ 更新 2026-06-21 ・ 再構成 P4*

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-029` |
| API名 | FAQ インポートテンプレート |
| 対応業務UC | [UC-028](../../01_requirements/04_business_usecases/UC-028.md#UC-028) |
| 対応画面ID | [SCR-010](../01_screens/SCR-010.md#SCR-010) |
| 対応画面イベントID | [EVT-091](../02_screen_events/EVT-091.md#EVT-091) |
| エンドポイント | `/faqs/import/template` |
| HTTPメソッド | GET |
| 認証 | Cookie |
| 認可 | オーナー / 当該プロジェクトのメンバー(API-022 と同一) |

## 処理概要

ヘッダ行のみの CSV テンプレートを返す。

| 処理 ID | 処理内容                                                    |
|---------|-------------------------------------------------------------|
| P-01    | 認証・認可(オーナー / 該当プロジェクトの編集権限)を検証する |
| P-02    | ヘッダ行のみの CSV テンプレートを生成する                   |
| P-03    | ダウンロード用ヘッダを付与して `text/csv` で返す            |

## リクエスト

#### Query Parameters

```json
{}
```

#### Path Parameters

```json
{}
```

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

ヘッダ行のみの CSV テンプレート(`FAQ ID,質問,回答,カテゴリ`)を `text/csv; charset=utf-8`(UTF-8 BOM 付与可)で返す。`Content-Disposition: attachment; filename="faq_import_template.csv"`。

| 項目 | 型 | 説明 |
|----|----|----|
| (本体) | string | ヘッダ行のみの CSV ファイル(列 `FAQ ID` / `質問` / `回答` / `カテゴリ`)。JSON 本体は返さない |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([API 共通仕様](index.md#conv) の標準エラー体系に従う)。

## 利用テーブル

本 API はテーブルを直接読み書きしない(外部サービス連携 IF)。

## 備考

再構成 P4 で旧 API ID `API-029`(旧グループ別 API 設計)から導出・フラット採番。

---

<!-- portal-bottom -->
[← API設計](index.md) ・ [基本設計](../index.md) ・ [↑ 設計ポータル](../../README.md)
<!-- /portal-bottom -->
