<!-- portal-top -->
[設計ポータル](../../README.md) ／ [基本設計](../index.md) ／ [API設計](index.md) ／ **API-030: FAQ CSV エクスポート**
<!-- /portal-top -->

# <span id="API-030"></span>API-030: FAQ CSV エクスポート

> **このページは FAQ CSV エクスポート API の契約を定義します。** 一覧フィルタ適用結果を CSV で出力する。

*版数 v2.0 ・ 更新 2026-06-21 ・ 再構成 P4*

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-030` |
| API名 | FAQ CSV エクスポート |
| 対応業務UC | [UC-074](../../01_requirements/02_business_usecases/UC-074.md#UC-074) |
| 対応画面ID | [SCR-008](../01_screens/SCR-008.md#SCR-008) |
| 対応画面イベントID | [EVT-074](../02_screen_events/EVT-074.md#EVT-074) |
| エンドポイント | `/faqs/export` |
| HTTPメソッド | GET |
| 認証 | Cookie |
| 認可 | オーナー / 当該プロジェクトのメンバー |

## 処理概要

一覧フィルタ適用結果を CSV で出力する。

| 処理 ID | 処理内容                                                    |
|---------|-------------------------------------------------------------|
| P-01    | 認証・認可(オーナー / 該当プロジェクトの編集権限)を検証する |
| P-02    | 一覧と同じフィルタ条件で対象 FAQ を取得する                 |
| P-03    | CSV(UTF-8)に整形しダウンロードとして返す                    |

## リクエスト

#### Query Parameters

```json
{
  "status": "(任意・一覧フィルタに準ずる)",
  "projectId": "(一覧フィルタに準ずる)",
  "keyword": "(任意・一覧フィルタに準ずる)"
}
```

| 項目        | 型     | 説明                                               |
|-------------|--------|----------------------------------------------------|
| `status`    | enum   | 状態での絞り込み(任意・一覧フィルタに準ずる)       |
| `projectId` | string | 対象プロジェクトの ID(一覧フィルタに準ずる)        |
| `keyword`   | string | キーワードでの絞り込み(任意・一覧フィルタに準ずる) |

#### Path Parameters

```json
{}
```

#### Request Body

```json
{}
```

Query Parameters は一覧フィルタに準ずる。

## レスポンス

**レスポンス(200)**

一覧フィルタ適用結果を **CSV(UTF-8、`text/csv`)** で返す(JSON 形式は提供しない)。

| 項目 | 型 | 説明 |
|----|----|----|
| (本体) | string | 一覧フィルタ適用結果の CSV ファイル(UTF-8、`text/csv`)。JSON 本体は返さない |

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([API 共通仕様](index.md#conv) の標準エラー体系に従う)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`M_FAQS`](../04_database/TBL-006.md#TBL-006) | `-R--` |

## 備考

再構成 P4 で旧 API ID `API-030`(旧グループ別 API 設計)から導出・フラット採番。

---

<!-- portal-bottom -->
[← API設計](index.md) ・ [基本設計](../index.md) ・ [↑ 設計ポータル](../../README.md)
<!-- /portal-bottom -->
