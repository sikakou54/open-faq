<!-- portal-top -->
[設計ポータル](../../README.md) ／ [基本設計](../index.md) ／ [API設計](index.md) ／ **API-044: 請求書一覧**
<!-- /portal-top -->

# <span id="API-044"></span>API-044: 請求書一覧

> **このページは 請求書一覧 API の契約を定義します。** 過去請求書を一覧取得する。

*版数 v2.0 ・ 更新 2026-06-21 ・ 再構成 P4*

## 項目

| 項目 | 内容 |
|---|---|
| API ID | `API-044` |
| API名 | 請求書一覧 |
| 対応業務UC | [UC-037](../../01_requirements/04_business_usecases/UC-037.md#UC-037) |
| 対応画面ID | [SCR-028](../01_screens/SCR-028.md#SCR-028) |
| 対応画面イベントID | [EVT-208](../02_screen_events/EVT-208.md#EVT-208) |
| エンドポイント | `/billing/invoices` |
| HTTPメソッド | GET |
| 認証 | Cookie |
| 認可 | オーナー専有 |

## 処理概要

過去請求書を一覧取得する。

| 処理 ID | 処理内容                                      |
|---------|-----------------------------------------------|
| P-01    | 認証・認可(オーナー専有)を検証する            |
| P-02    | 過去請求書を新しい順に取得する                |
| P-03    | PDF 署名 URL(有効期限 5 分)を付与して返却する |

## リクエスト

#### Query Parameters

```json
{ "limit": "6(等)" }
```

| 項目    | 型      | 説明                                |
|---------|---------|-------------------------------------|
| `limit` | integer | 取得する請求書の件数。任意(例: `6`) |

#### Path Parameters

```json
{}
```

#### Request Body

```json
{}
```

## レスポンス

**レスポンス(200)**

```json
{
  "items": [
    {
      "id": "inv_...",
      "date": "2026-05-01T...",
      "amount": 5000,
      "currency": "JPY",
      "status": "paid|failed|draft",
      "pdfUrl": "https://..."
    }
  ]
}
```

| 項目           | 型                | 説明                                      |
|----------------|-------------------|-------------------------------------------|
| `items`        | array\<object\>   | 請求書の配列(新しい順)                    |
| `　└ id`       | string            | 請求書の ID                               |
| `　└ date`     | string (ISO 8601) | 請求日                                    |
| `　└ amount`   | integer           | 請求金額                                  |
| `　└ currency` | string            | 通貨コード                                |
| `　└ status`   | enum              | 請求書の状態。`paid` / `failed` / `draft` |
| `　└ pdfUrl`   | string            | 請求書 PDF の署名 URL(有効期限 5 分)      |

`pdfUrl` は R2 署名 URL(有効期限 5 分)。

## バリデーション

入力値の検証規則は `## リクエスト` の各パラメータ説明を参照する。

## エラー

本 API 固有のエラーコードはない([API 共通仕様](index.md#conv) の標準エラー体系に従う)。

## 利用テーブル

本 API が参照・更新するテーブルと CRUD 区分。

| テーブル | CRUD |
|---|---|
| [`T_BILL_INVOICES`](../04_database/TBL-019.md#TBL-019) | `-R--` |

## 備考

再構成 P4 で旧 API ID `API-044`(旧グループ別 API 設計)から導出・フラット採番。

---

<!-- portal-bottom -->
[← API設計](index.md) ・ [基本設計](../index.md) ・ [↑ 設計ポータル](../../README.md)
<!-- /portal-bottom -->
