# <span id="SEQ-084"></span>SEQ-084: ランチャーバッジを押下

> **このページは、業務ユースケース UC-040（ランチャーバッジを押下）のシーケンス図を定義します。**

| ID | 業務ユースケースID | イベント(画面ID EVT-NN) | テーブルID |
|----|----|----|----|
| SEQ-084 | [UC-040](../../01_requirements/04_business_usecases/UC-040.md#UC-040) | SCR-030 EVT-02 | [TBL-002](../02_backend/04_database/TBL-002.md#TBL-002) ・ [TBL-004](../02_backend/04_database/TBL-004.md#TBL-004) ・ [TBL-005](../02_backend/04_database/TBL-005.md#TBL-005) ・ [TBL-015](../02_backend/04_database/TBL-015.md#TBL-015) |

## 概要

ウィジェット利用者がランチャーバッジを押下すると、バッジを非表示にしてチャット UI を展開する。サーバーは公開キーとドメインを検証してウィジェットセッションを確立し、表示設定（タイトル・連絡先メール等）を返して質問受付状態にする。

## シーケンス図

```mermaid
sequenceDiagram
  autonumber
  actor U as ウィジェット利用者
  participant Screen as エンドユーザー向け FAQ ウィジェット
  participant Server as サーバー
  participant DB as DB
  U->>Screen: ランチャーバッジを押下
  Screen->>Screen: バッジを非表示・チャット UI を展開
  Screen->>Server: ウィジェット起動要求(公開キー・オリジン)
  Server->>Server: 入力検証
  Server->>DB: 認証情報・データを取得(現行キー・旧キーの猶予期限を含む)
  DB-->>Server: 取得結果
  alt 現行キー一致、または旧キーかつ猶予期限内
    Server-->>Screen: セッション・表示設定
    Screen-->>U: チャット UI を表示(質問受付)
  else 旧キーかつ猶予期限超過 / 公開キー不正 / 許可ドメイン外 / サスペンション中
    Server-->>Screen: エラー
    Screen-->>U: エラー表示・再試行案内(連打防止)
  end
```

## 例外フロー

- 公開キーが不正な場合、ウィジェットは起動せずエラーを表示する。
- 提示キーが現行キーに一致しないが旧キーに一致し、かつ旧キーの猶予期限内であれば起動を許可する(遅延失効)。
- 提示キーが旧キーに一致するが猶予期限を超過している場合、失効として起動を拒否しエラーを表示する。
- リクエスト元オリジンが許可ドメイン外の場合、起動を拒否する。
- プロジェクトがサスペンション(支払い不能)中の場合、起動を拒否しエラーを表示する。

## 備考

- 本図は基本設計レベルの抽象度(ユーザー / 画面 / サーバー、システム起点は外部システム・スケジューラ・バッチを加える)で記述する。DB 操作は DB アクターへのメッセージで表し、テーブル別 CRUD は本図に書かず 関連テーブル 欄で示す。
- 図の出典は業務ユースケース [UC-040](../../01_requirements/04_business_usecases/UC-040.md#UC-040)。画面イベントとの対応は UC-040 を参照。
