# <span id="SEQ-104"></span>SEQ-104: 監査ログ整合性検証(日次)

> **このページは、業務ユースケース UC-070（監査ログ整合性検証(日次)）のシーケンス図を定義します。**

| ID | 業務ユースケースID | イベント(画面ID EVT-NN) | テーブルID |
|----|----|----|----|
| SEQ-104 | [UC-070](../../01_requirements/04_business_usecases/UC-070.md#UC-070) | — | [TBL-027](../02_backend/04_database/TBL-027.md#TBL-027) |

## 概要

スケジューラが日次で整合性検証バッチを起動し、監査ログを全件走査して改ざん検知情報の整合性を検証する。整合性違反を検出した対象はアラート通知し、違反が無ければ正常終了する。

## シーケンス図

```mermaid
sequenceDiagram
  autonumber
  participant SCH as スケジューラ
  participant B as 整合性検証バッチ
  participant DB as DB
  participant OP as 運用担当
  SCH->>B: 日次起動
  B->>DB: 監査ログを全件走査
  DB-->>B: 走査結果
  B->>B: 改ざん検知情報の整合性を検証
  alt 整合性違反あり
    B-->>OP: 違反対象をアラート通知(走査は継続)
  else 違反なし
    B-->>SCH: 正常終了(違反なし)
  end
```

## 例外フロー

- 整合性違反の検出: 改ざん・欠落の疑いを検出した対象を特定し、アラート通知する。検証は中断せず残りの全件走査を継続する。
- 対象なし: 検証対象の監査ログが無い場合は検証を行わず、正常終了する。

## 備考

- 本図は基本設計レベルの抽象度(ユーザー / 画面 / サーバー、システム起点は外部システム・スケジューラ・バッチを加える)で記述する。DB 操作は DB アクターへのメッセージで表し、テーブル別 CRUD は本図に書かず 関連テーブル 欄で示す。
- 図の出典は業務ユースケース [UC-070](../../01_requirements/04_business_usecases/UC-070.md#UC-070)。画面イベントとの対応は UC-070 を参照。
