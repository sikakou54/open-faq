# <span id="SEQ-100"></span>SEQ-100: ログイン失敗ロックアウト・解除

> **このページは、業務ユースケース UC-068（ログイン失敗ロックアウト・解除）のシーケンス図を定義します。**

| ID | シーケンス名 |
|----|----|
| SEQ-100 | ログイン失敗ロックアウト・解除 |

| 関連項目 | 内容 |
|----|----| 
| 業務ユースケース | [UC-068](../../01_requirements/04_business_usecases/UC-068.md#UC-068) |
| イベント | — |
| 関連画面 | — |
| 関連API | [API-002](../02_backend/03_apis/API-002.md#API-002) |
| テーブル | — |
| エラー(ERR) | [ERR-002](../05_errors/ERR-002.md#ERR-002) / [ERR-003](../05_errors/ERR-003.md#ERR-003) / [ERR-004](../05_errors/ERR-004.md#ERR-004) |
| メッセージ(MSG) | [MSG-005](../06_messages/MSG-005.md#MSG-005) |

## 概要

連続ログイン失敗による総当たり攻撃を 5 回 / 15 分のロックで抑止し、ロック中の到達は認証せず拒否する。ロックは時間経過の自動解除または権限者の手動解除で解け、解除後は失敗回数を初期化して試行を再受付する。

## シーケンス図

```mermaid
sequenceDiagram
  autonumber
  actor U as アカウント利用者
  actor M as 権限者(オーナー / メンバー)
  participant Server as サーバー
  participant DB as DB
  participant SCH as スケジューラ
  U->>Server: ログイン要求(認証情報)
  Server->>DB: 認証情報・データを取得(ロック状態の判定)
  DB-->>Server: 取得結果
  alt ロック中
    Server-->>U: 認証せず拒否(ロック中)
  else 未ロック・認証成功
    Server->>DB: 業務データを更新(失敗回数を初期化)
    DB-->>Server: 更新完了
    Server-->>U: ログイン成立
  else 未ロック・認証不一致
    Server->>DB: 業務データを更新(失敗回数を加算)
    DB-->>Server: 更新完了
    alt 5 回連続失敗
      Server->>DB: 業務データを更新(15 分ロックを発動)
      DB-->>Server: 更新完了
      Server-->>U: ロック発動を通知(救済導線)
      Server-->>M: 必要な権限者へ通知
    else 上限未満
      Server-->>U: 認証情報が不正
    end
  end
  alt ロックから 15 分経過(自動解除)
    SCH->>Server: 期限経過の判定を起動
    Server->>DB: 業務データを更新(ロック解除・失敗回数を初期化)
    DB-->>Server: 更新完了
  else 権限者による手動解除
    M->>Server: ロック解除を実行
    Server->>DB: 業務データを更新(ロック解除・失敗回数を初期化)
    DB-->>Server: 更新完了
    Server-->>M: 解除完了を通知
  end
```

## 例外フロー

- ロック中に追加のログイン試行が到達しても認証は行わず、一律にロック中として拒否する。
- 解除直後に再び 5 回連続失敗した場合は、改めて 15 分のロックを発動する。

## 備考

- 本図は基本設計レベルの抽象度(ユーザー / 画面 / サーバー、システム起点は外部システム・スケジューラ・バッチを加える)で記述する。DB 操作は DB アクターへのメッセージで表し、テーブル別 CRUD は本図に書かず 関連テーブル 欄で示す。
- 図の出典は業務ユースケース [UC-068](../../01_requirements/04_business_usecases/UC-068.md#UC-068)。画面イベントとの対応は UC-068 を参照。
