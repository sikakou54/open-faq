# メール設計書(運営者システム)

## 1. 文書概要

### 1.1 目的

本書は **運営者システム(SaaS 提供事業者向け管理コンソール)が送信する全電子メールの件名・本文・テンプレート変数・送信契機を集中定義する正本** である。実装者・QA・運営オペレーターが本書の文言と差分なくメール内容を確認できることを保証する。

### 1.2 対象範囲

- 運営者システムから自動送信される全メール(本書 §4 のテンプレート ID `TPL-OP-*` 全件)
- 各テンプレートの件名 / プレーンテキスト本文 / HTML 本文(プレースホルダ含む)
- 送信契機(通知 ID `NOTIF-OP-*`)/ 配信先 / 重要度 / 集約窓 / 失効・リンク有効期限
- IF #12 経由で **利用者側(メイン契約管理者)に届くメール** も対象に含める(運営者操作の通知主体は運営者システムのため)

**対象外**:

- 運営者 inbox 専用通知の文言詳細(運営者画面内表示は [06_メッセージ一覧.md](06_メッセージ一覧.md) §8.4 を正本とする)
- メインシステムが自発的に送信するメール([../../01_メインシステム/02_基本設計/11_メール設計.md](../../01_メインシステム/02_基本設計/11_メール設計.md))

### 1.3 版数

| 項目 | 値 |
|---|---|
| 版数 | 1.0 |
| 作成日 | 2026-05-19 |
| 更新日 | 2026-05-19 |

### 1.4 関連ドキュメント

- 通知契機・チャネル・配信先 / 重要度: [06_メッセージ一覧.md](06_メッセージ一覧.md) §8
- 通知重要度の正本ルール: [../../共有/共有概念.md](../../共有/共有概念.md) §通知重要度 4 値
- 4-eyes 承認基盤: [08_認証・認可設計.md](08_認証・認可設計.md) §7
- セキュリティ要件(配信信頼性 / DMARC 等): [09_セキュリティ設計.md](09_セキュリティ設計.md) §7 / §11
- メインシステム側受信時表現: [../../01_メインシステム/02_基本設計/11_メール設計.md](../../01_メインシステム/02_基本設計/11_メール設計.md)

---

## 2. 共通基準

### 2.1 送信元 / Reply-To / 差出人表記

| 項目 | 値 |
|---|---|
| Envelope From | `bounces+<deliveryId>@ops.<service-domain>` (VERP) |
| Header From | `open-faq Operations <ops-noreply@<service-domain>>` 固定 |
| Reply-To | `ops-team@<service-domain>`(運営者宛通知のみ。IF #12 経由の利用者宛通知では設定しない)|
| DMARC / DKIM / SPF | 全テンプレートで pass を必須 |

### 2.2 件名規則

- 件名長: 半角換算 70 文字以内
- 重要度プレフィックス:
  - `critical`: `[重大]` または `[緊急]`(`TPL-OP-FRAUD-DETECTED` のみ `[緊急]`)
  - `high`: `[警告]`
  - `normal`: `[通知]`
- 4-eyes 申請関連は `[承認待ち]` / `[承認完了]` プレフィックスを用いる
- 利用者宛通知(IF #12 経由)は重要度プレフィックスを付けず、メイン側メールと混在しても違和感のないフラットな文面とする

### 2.3 本文構造(運営者宛通知)

1. 重要度マーカーと事象タイトル
2. 一段落の状況説明
3. メタデータ表(検知時刻 / 対象範囲 / 関連 ID 等)
4. 推奨対応(Runbook URL 等)
5. 関連画面 deeplink(SCR-* URL)
6. 共通フッタ(返信不可注意、配信元署名)

### 2.4 本文構造(IF #12 経由 利用者宛通知)

1. 宛名(`{recipient_display_name} 様`)
2. 運営者が当該操作を実行した旨の告知
3. 対象と影響範囲(時刻 / 対象データ / 復元・解除手順)
4. 利用者側で取れるアクション(該当画面 deeplink)
5. 共通フッタ(運営者問い合わせ窓口 URL)

### 2.5 文字コード / 言語 / 時刻

- 文字コード: UTF-8
- HTML 版とテキスト版を両方生成し、`multipart/alternative` で送信
- 全文 i18n キー化必須。MVP 対応言語: 日本語(`ja`)
- 本文内の時刻表記は **JST 固定** で `YYYY-MM-DD HH:mm:ss`(運営者宛は秒まで記載、利用者宛は分まで)

### 2.6 サニタイズ

- 監査ログ・Webhook ペイロード・払い戻し payload 等の **生データ埋め込みは禁止**。本文に含めるのは要約のみとし、詳細は SCR-* deeplink から運営者管理画面で確認する
- 利用者宛通知では契約 ID(`accounts.id`)等の内部識別子も埋め込まない

### 2.7 配信信頼性

- 一時失敗時の指数バックオフ再送、恒久失敗時の `notification_logs.delivery_status='bounced'` 記録は メイン側 §2.6 と同じ
- `critical` 通知は配信失敗時に PagerDuty / Slack `#ops-incidents` への自動エスカレーションを併用(NFR-808、§9.1 障害対応)

---

## 3. テンプレート一覧

運営者システム送信の全 17 テンプレートを以下に索引する。詳細は §4 を参照。

### 3.1 運営者宛通知(12 テンプレート)

| # | テンプレートID | 通知ID | 重要度 | 配信先 |
|---|---|---|---|---|
| 1 | TPL-OP-4EYES-REQUEST | NOTIF-OP-4EYES-REQUEST | high | 全運営者 |
| 2 | TPL-OP-4EYES-APPROVED | NOTIF-OP-4EYES-APPROVED | normal | 申請者 |
| 3 | TPL-OP-PII-REMINDER | NOTIF-OP-PII-REMINDER | high | 全運営者 |
| 4 | TPL-OP-PAYLOAD-DIFF | NOTIF-OP-PAYLOAD-DIFF | high | 全運営者 |
| 5 | TPL-OP-DLQ-STUCK | NOTIF-OP-DLQ-STUCK | high | 全運営者 |
| 6 | TPL-OP-HASH-CHAIN-NG | NOTIF-OP-HASH-CHAIN-NG | critical | 全運営者 |
| 7 | TPL-OP-FREQ-ANOMALY | NOTIF-OP-FREQ-ANOMALY | high | 全運営者 |
| 8 | TPL-OP-ANN-TEST | NOTIF-OP-ANN-TEST | テスト | 運営者本人 |
| 9 | TPL-OP-SIG-FAIL | NOTIF-OP-SIG-FAIL | critical | 全運営者 |
| 10 | TPL-OP-WH-FAIL-RATE | NOTIF-OP-WH-FAIL-RATE | high | 全運営者 |
| 11 | TPL-OP-CRON-FAIL | NOTIF-OP-CRON-FAIL | high | 全運営者 |
| 12 | TPL-OP-LOCK-CONFLICT | NOTIF-OP-LOCK-CONFLICT | high | 全運営者 |
| 13 | TPL-OP-FRAUD-DETECTED | NOTIF-OP-FRAUD-DETECTED | critical | 全運営者 |

### 3.2 IF #12 経由 利用者宛通知(5 テンプレート、集約窓 10 分)

| # | テンプレートID | 通知ID | 重要度 | 配信先 |
|---|---|---|---|---|
| 14 | TPL-OP-IF12-RESTORE-DONE | NOTIF-OP-RESTORE-DONE | high | 対象契約全管理者 |
| 15 | TPL-OP-IF12-SUSPEND | NOTIF-OP-SUSPEND | critical | 対象契約全管理者 |
| 16 | TPL-OP-IF12-RATE-CHANGED | NOTIF-OP-RATE-CHANGED | high | 対象契約全管理者 |
| 17 | TPL-OP-IF12-AI-PARAM-CHANGED | NOTIF-OP-AI-PARAM-CHANGED | normal | 対象契約全管理者 |
| 18 | TPL-OP-IF12-WIDGET-FORCE-STOP | NOTIF-OP-WIDGET-FORCE-STOP | critical | 対象契約全管理者 |

---

## 4. テンプレート詳細

各テンプレートは以下のセクション構造で記載する:

- **発火条件**: どの操作・イベントで送信されるか
- **テンプレート変数**: 本文中の `{...}` 変数
- **件名**: 件名(JA)
- **本文(テキスト版)**: プレーンテキスト本文
- **本文(HTML 版)**: HTML 版の構造概要
- **備考**: 注意事項

### 4.1 TPL-OP-4EYES-REQUEST(4-eyes 申請発生)

**発火条件**: 運営者が 4-eyes 対象操作(契約復元 / 契約停止 / レート上書き / AI パラメータ変更 / ウィジェット強制停止 等)を申請した時。承認待ちの全運営者へ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{action_code}` | 操作コード(`RESTORE_CONTRACT` 等)|
| `{action_label}` | 操作の人間可読名称 |
| `{applicant_name}` | 申請者名 |
| `{target_summary}` | 対象の要約(契約 ID は伏せ、表示名のみ)|
| `{payload_preview}` | payload プレビュー(運営者 inbox 表示と同等の差分要約、200 字)|
| `{approval_url}` | SCR-APPROVALS 詳細 URL |
| `{request_at_jst}` | 申請日時(JST、秒まで)|
| `{deadline_at_jst}` | 承認期限(JST、申請 + 48h)|

**件名**: `[承認待ち] {action_code} の承認依頼`

**本文(テキスト版)**:

```
運営者各位

4-eyes 承認待ちの申請が発生しました。

  - 操作: {action_label}({action_code})
  - 申請者: {applicant_name}
  - 対象: {target_summary}
  - 申請日時: {request_at_jst}(JST)
  - 承認期限: {deadline_at_jst}(JST、申請から 48 時間)

payload プレビュー:
{payload_preview}

申請内容を確認のうえ、承認または差し戻しをお願いします。

▶ 承認画面を開く
{approval_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 操作種別を背景色付きバッジで表示 + payload プレビューを引用ブロック + 承認画面プライマリボタン。

---

### 4.2 TPL-OP-4EYES-APPROVED(4-eyes 承認完了 → 申請者)

**発火条件**: 4-eyes 申請が承認され、対象操作が実行された時。申請者本人へ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{applicant_name}` | 申請者名 |
| `{action_code}` | 操作コード |
| `{action_label}` | 操作名称 |
| `{approver_name}` | 承認者名 |
| `{approved_at_jst}` | 承認・実行日時(JST)|
| `{audit_log_url}` | SCR-097 監査ログ詳細(該当エントリ)deeplink |

**件名**: `[承認完了] {action_code} が承認・実行されました`

**本文(テキスト版)**:

```
{applicant_name} 様

申請いただいた以下の操作が承認・実行されました。

  - 操作: {action_label}({action_code})
  - 承認者: {approver_name}
  - 承認・実行日時: {approved_at_jst}(JST)

監査ログから操作詳細を確認できます。

▶ 監査ログを開く
{audit_log_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: シンプルな完了通知 + 監査ログへのセカンダリボタン。

---

### 4.3 TPL-OP-PII-REMINDER(PII 誤検出報告 3 営業日判定リマインド)

**発火条件**: AC-036 PII 誤検出報告が運営者に届いてから 3 営業日経過しても判定が完了していない場合。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{pending_count}` | 未判定件数 |
| `{oldest_report_at_jst}` | 最古の報告日時(JST)|
| `{review_url}` | SCR-OPS-PII-REVIEW deeplink |

**件名**: `[警告] PII 誤検出報告の判定が滞留しています({pending_count} 件)`

**本文(テキスト版)**:

```
運営者各位

PII 誤検出報告の判定が 3 営業日以上滞留しています。SLA 遵守のため速やかに
判定を実施してください。

  - 未判定件数: {pending_count} 件
  - 最古の報告日時: {oldest_report_at_jst}(JST)

▶ 判定画面を開く
{review_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 滞留件数を強調表示 + プライマリボタン。

---

### 4.4 TPL-OP-PAYLOAD-DIFF(Stripe Webhook ペイロード差分検出)

**発火条件**: FR-302 異常系。Stripe Webhook で受信した payload と内部状態の差分を検出した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{stripe_event_id}` | Stripe Event ID |
| `{event_type}` | Stripe イベント種別 |
| `{detected_at_jst}` | 検知日時(JST、秒まで)|
| `{diff_summary}` | 差分要約(フィールド名と差分タイプ、200 字)|
| `{webhook_admin_url}` | SCR-099 Webhook 受信履歴 deeplink |

**件名**: `[警告] Stripe Webhook payload 差分検出({event_type})`

**本文(テキスト版)**:

```
運営者各位

Stripe Webhook の受信時に、内部状態との差分を検出しました。手動確認をお願
いします。

  - Stripe Event ID: {stripe_event_id}
  - イベント種別: {event_type}
  - 検知日時: {detected_at_jst}(JST)

差分要約:
{diff_summary}

詳細は Webhook 受信履歴から確認できます。

▶ Webhook 受信履歴を開く
{webhook_admin_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 差分を背景色付き引用ブロックで表示 + プライマリボタン。

---

### 4.5 TPL-OP-DLQ-STUCK(DLQ 滞留 1h 以上)

**発火条件**: NFR-808 / NFR-809。Dead Letter Queue にメッセージが 1 時間以上滞留した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{queue_name}` | キュー名 |
| `{stuck_message_count}` | 滞留メッセージ件数 |
| `{oldest_message_at_jst}` | 最古メッセージ投入日時(JST)|
| `{queue_admin_url}` | DLQ 管理画面 URL |
| `{runbook_url}` | 対応 Runbook URL |

**件名**: `[警告] DLQ メッセージが滞留しています({queue_name}, {stuck_message_count} 件)`

**本文(テキスト版)**:

```
運営者各位

Dead Letter Queue にメッセージが 1 時間以上滞留しています。

  - キュー名: {queue_name}
  - 滞留件数: {stuck_message_count}
  - 最古メッセージ: {oldest_message_at_jst}(JST)

Runbook に従い対応してください。

▶ DLQ 管理画面を開く
{queue_admin_url}

▶ 対応 Runbook を開く
{runbook_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 滞留件数を視覚的に強調 + 2 プライマリボタン。

---

### 4.6 TPL-OP-HASH-CHAIN-NG(ハッシュチェーン不一致)

**発火条件**: NFR-306。`audit_logs` のハッシュチェーン検証で不一致を検出した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{detected_at_jst}` | 検知日時(JST、秒まで)|
| `{affected_range_from_id}` | 影響範囲開始監査ログ ID |
| `{affected_range_to_id}` | 影響範囲終了監査ログ ID |
| `{audit_admin_url}` | SCR-096 監査ログ整合性チェック画面 |
| `{runbook_url}` | ハッシュチェーン障害対応 Runbook URL |

**件名**: `[重大] 監査ログのハッシュチェーン確認が必要です`

**本文(テキスト版)**:

```
運営者各位

監査ログのハッシュチェーン整合性検証で不一致を検出しました。改ざんまたは
データ破損の可能性があります。直ちに調査を開始してください。

  - 検知日時: {detected_at_jst}(JST)
  - 影響範囲: audit_logs.id {affected_range_from_id} 〜 {affected_range_to_id}

▶ 整合性チェック画面を開く
{audit_admin_url}

▶ 対応 Runbook を開く
{runbook_url}

本通知は critical 重要度のため、配信失敗時には PagerDuty が自動エスカレー
ションします。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 赤系警告 + 影響範囲の明示 + 2 プライマリボタン。

---

### 4.7 TPL-OP-FREQ-ANOMALY(運営者操作頻度異常)

**発火条件**: FR-229(c)。運営者の操作頻度が平常時の閾値を超えた時(運営者本人のアカウント乗っ取り疑い検知)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{operator_name}` | 検知対象運営者名 |
| `{anomaly_window_label}` | 検知ウィンドウ(`過去 5 分` 等)|
| `{action_count}` | 操作回数 |
| `{threshold}` | 閾値 |
| `{detected_at_jst}` | 検知日時(JST)|
| `{audit_log_url}` | 該当運営者の操作ログ deeplink |

**件名**: `[警告] 運営者操作頻度異常を検知({operator_name})`

**本文(テキスト版)**:

```
運営者各位

運営者「{operator_name}」の操作頻度が平常時の閾値を超えました。アカウント
乗っ取り等の可能性がないか確認してください。

  - 検知ウィンドウ: {anomaly_window_label}
  - 操作回数: {action_count} / 閾値 {threshold}
  - 検知日時: {detected_at_jst}(JST)

▶ 操作ログを開く
{audit_log_url}

必要に応じて該当運営者の強制ログアウト・MFA 再セットアップを実施してくだ
さい。

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 検知統計をカード表示 + プライマリボタン。

---

### 4.8 TPL-OP-ANN-TEST(SCR-094 テスト送信)

**発火条件**: FR-149。運営者が SCR-094 サービスお知らせ配信画面で「テスト送信」を実行した時。実行者本人のメールアドレス宛に送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{operator_name}` | テスト送信実行者名 |
| `{announcement_title}` | 配信予定のお知らせタイトル |
| `{announcement_body}` | 配信予定の本文(HTML sanitize 済)|
| `{sent_at_jst}` | テスト送信日時(JST)|

**件名**: `[テスト送信] {announcement_title}`

**本文(テキスト版)**:

```
{operator_name} 様

SCR-094 サービスお知らせ配信画面からのテスト送信です。
実際の配信前に内容をご確認ください。

(以下、配信予定本文)
{announcement_body}

──────────────────────────────────────────────
このメールはテスト送信です。実利用者には届きません。
送信日時: {sent_at_jst}(JST)
open-faq Operations
```

**本文(HTML 版)**: テスト送信であることを明示するヘッダ + 本文プレビュー領域 +「本番送信に進む」セカンダリボタン(SCR-094 deeplink)。

---

### 4.9 TPL-OP-SIG-FAIL(課金 Webhook 署名検証失敗)

**発火条件**: NFR-310。Stripe Webhook の署名検証に失敗した時。攻撃の可能性があるため即時通知。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{detected_at_jst}` | 検知日時(JST、秒まで)|
| `{stripe_event_id_or_unknown}` | 受信ペイロードから読み取れた Event ID(取得不能時は `不明`)|
| `{source_ip}` | 送信元 IP |
| `{webhook_admin_url}` | SCR-099 Webhook 受信履歴 deeplink |

**件名**: `[重大] 課金 Webhook の署名検証失敗を検知`

**本文(テキスト版)**:

```
運営者各位

Stripe Webhook 受信時に署名検証で失敗が発生しました。攻撃または設定不備の
可能性があります。

  - 検知日時: {detected_at_jst}(JST)
  - Stripe Event ID: {stripe_event_id_or_unknown}
  - 送信元 IP: {source_ip}

直ちに Webhook 受信履歴を確認し、攻撃と判断される場合は IP allowlist を更
新してください。

▶ Webhook 受信履歴を開く
{webhook_admin_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 赤系警告ヘッダ + 検知メタデータ表 + プライマリボタン。

---

### 4.10 TPL-OP-WH-FAIL-RATE(課金 Webhook 受信失敗率 5% 超過)

**発火条件**: NFR-808。Stripe Webhook の 1 時間あたり失敗率が 5% を超えた時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{failure_rate_pct}` | 失敗率(`6.2`)|
| `{window_label}` | 集計窓(`過去 1 時間`)|
| `{detected_at_jst}` | 検知日時(JST)|
| `{webhook_admin_url}` | Webhook 受信履歴 |
| `{runbook_url}` | 対応 Runbook URL |

**件名**: `[警告] 課金 Webhook 受信失敗率が {failure_rate_pct}% に達しました`

**本文(テキスト版)**:

```
運営者各位

課金 Webhook 受信の失敗率が SLO 閾値を超えました。

  - 失敗率: {failure_rate_pct}%(閾値 5%)
  - 集計窓: {window_label}
  - 検知日時: {detected_at_jst}(JST)

▶ Webhook 受信履歴を開く
{webhook_admin_url}

▶ 対応 Runbook を開く
{runbook_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 失敗率カード + 2 プライマリボタン。

---

### 4.11 TPL-OP-CRON-FAIL(月次請求確定 cron 失敗 3 回連続)

**発火条件**: FR-303。月次請求確定バッチが 3 回連続失敗した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{cron_name}` | cron ジョブ名 |
| `{billing_period_label}` | 対象期間 |
| `{last_error_summary}` | 最終エラー要約(200 字)|
| `{last_run_at_jst}` | 最終実行日時(JST)|
| `{job_admin_url}` | ジョブ管理画面 |
| `{runbook_url}` | 月次請求 cron 障害対応 Runbook |

**件名**: `[警告] 月次請求確定 cron が 3 回連続失敗({billing_period_label})`

**本文(テキスト版)**:

```
運営者各位

月次請求確定バッチが 3 回連続で失敗しました。SLA 遵守のため至急対応してく
ださい。

  - ジョブ名: {cron_name}
  - 対象期間: {billing_period_label}
  - 最終実行: {last_run_at_jst}(JST)

最終エラー要約:
{last_error_summary}

▶ ジョブ管理画面を開く
{job_admin_url}

▶ 対応 Runbook を開く
{runbook_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: エラー要約引用ブロック + 2 プライマリボタン。

---

### 4.12 TPL-OP-LOCK-CONFLICT(復元時 423 Locked)

**発火条件**: FR-204 異常系。論理削除データの復元処理時、deletion-queue との競合により HTTP 423 Locked が返却された時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{target_resource}` | 対象リソース名(契約表示名)|
| `{operator_name}` | 復元操作者名 |
| `{detected_at_jst}` | 検知日時(JST)|
| `{retry_after_jst}` | 推奨再試行時刻(JST、deletion-queue 完了予定 + 5 分)|
| `{restore_admin_url}` | SCR-091 復元管理画面 |

**件名**: `[警告] 復元処理が deletion-queue 競合でロックされました`

**本文(テキスト版)**:

```
運営者各位

論理削除データの復元処理で deletion-queue との競合が発生し、ロック状態に
なりました。

  - 対象: {target_resource}
  - 操作者: {operator_name}
  - 検知日時: {detected_at_jst}(JST)
  - 推奨再試行時刻: {retry_after_jst}(JST)

deletion-queue 完了後に再度復元を試行してください。

▶ 復元管理画面を開く
{restore_admin_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 競合状態の可視化 + プライマリボタン。

---

### 4.13 TPL-OP-FRAUD-DETECTED(不正利用検知通知 = IF #9 受信)

**発火条件**: FR-195。外部不正利用検知システムから IF #9 経由で検知通知を受信した時。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{owner_account_label}` | 対象オーナーアカウント表示名(内部 ID は伏せる)|
| `{detection_pattern}` | 検知パターン名 |
| `{detected_at_jst}` | 検知日時(JST、秒まで)|
| `{risk_score}` | リスクスコア(0-100)|
| `{recommended_action}` | 推奨対応(`即時サスペンション` / `監視継続` 等)|
| `{rate_limit_admin_url}` | SCR-093 契約別レート制限管理 |

**件名**: `[緊急] 不正利用検知 — {owner_account_label}`

**本文(テキスト版)**:

```
運営者各位

外部検知システムから不正利用の疑いが報告されました。

  - 対象: {owner_account_label}
  - 検知パターン: {detection_pattern}
  - リスクスコア: {risk_score} / 100
  - 検知日時: {detected_at_jst}(JST)
  - 推奨対応: {recommended_action}

至急 SCR-093 で対応策(レート制限・サスペンション等)を実施してください。

▶ レート制限管理画面を開く
{rate_limit_admin_url}

──────────────────────────────────────────────
このメールに直接返信しないでください。
open-faq Operations
```

**本文(HTML 版)**: 赤系緊急ヘッダ + リスクスコアをゲージ表示 + プライマリボタン。

---

### 4.14 TPL-OP-IF12-RESTORE-DONE(論理削除データの復元)→ 利用者宛

**発火条件**: 運営者が SCR-091 から論理削除データの復元を実施した時。IF #12 経由で対象契約の管理者(オーナー + 全プロジェクト管理者)へ送信(集約窓 10 分)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{restored_at_jst}` | 復元日時(JST、分まで)|
| `{restored_resource_summary}` | 復元対象の要約(プロジェクト名 / 件数等。内部 ID は含めない)|
| `{restore_reason}` | 復元理由(運営者入力、HTML sanitize 済)|
| `{operator_signature}` | 運営者署名(`open-faq 運営事務局`)|
| `{ticket_id}` | サポートチケット ID(問い合わせ起点の復元の場合のみ)|
| `{contact_url}` | サポート問い合わせ URL |

**件名**: `論理削除データを復元しました({restored_at_jst})`

**本文(テキスト版)**:

```
{recipient_display_name} 様

ご利用の open-faq で運営者による以下のデータ復元を実施しました。

  - 復元日時: {restored_at_jst}(JST)
  - 復元対象: {restored_resource_summary}
  - 復元理由: {restore_reason}
  - 関連チケット: {ticket_id}

復元したデータは管理画面から通常通りご利用いただけます。
ご不明な点はサポートまでご連絡ください。

▶ サポートに問い合わせる
{contact_url}

──────────────────────────────────────────────
本通知は open-faq 運営事務局からの自動配信です。返信は受け付けておりません。
{operator_signature}
```

**本文(HTML 版)**: 復元内容をパネル表示 + サポート問い合わせセカンダリボタン。

---

### 4.15 TPL-OP-IF12-SUSPEND(契約無効化・復旧)→ 利用者宛

**発火条件**: 運営者が SCR-092 から契約をサスペンション / 復旧した時。IF #12 経由で対象契約管理者へ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{event_type}` | `suspend` / `release` |
| `{event_at_jst}` | 実施日時(JST、分まで)|
| `{reason_label}` | 理由ラベル(運営者選択肢)|
| `{billing_screen_url}` | SCR-015 課金部 deeplink(`suspend` 時のみ)|
| `{contact_url}` | サポート問い合わせ URL |
| `{operator_signature}` | 運営者署名 |

**件名**:

- suspend: `[重要] アカウントが停止されました`
- release: `アカウントの利用を再開しました`

**本文(テキスト版・suspend)**:

```
{recipient_display_name} 様

ご利用の open-faq のアカウントを {event_at_jst}(JST) 時点で停止しました。

  - 停止理由: {reason_label}

停止解除のお手続きは管理画面の課金画面または下記サポート窓口からご確認く
ださい。

▶ 課金画面を開く
{billing_screen_url}

▶ サポートに問い合わせる
{contact_url}

──────────────────────────────────────────────
本通知は open-faq 運営事務局からの自動配信です。返信は受け付けておりません。
{operator_signature}
```

**本文(テキスト版・release)**:

```
{recipient_display_name} 様

ご利用の open-faq のアカウントを {event_at_jst}(JST) 時点で利用再開しました。
引き続きどうぞよろしくお願いいたします。

ご不明な点はサポートまでご連絡ください。

▶ サポートに問い合わせる
{contact_url}

──────────────────────────────────────────────
本通知は open-faq 運営事務局からの自動配信です。返信は受け付けておりません。
{operator_signature}
```

**本文(HTML 版)**: `suspend` は赤系警告、`release` は緑系。

---

### 4.16 TPL-OP-IF12-RATE-CHANGED(契約別レート制限・しきい値上書き変更)→ 利用者宛

**発火条件**: 運営者が SCR-093 からレート制限・しきい値を上書きした時。IF #12 経由で対象契約管理者へ送信(集約窓 10 分)。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{changed_at_jst}` | 変更日時(JST、分まで)|
| `{change_summary}` | 変更項目要約(新旧値の対比、200 字)|
| `{reason_label}` | 変更理由ラベル |
| `{rate_limit_screen_url}` | SCR-015 課金部内のレート制限表示 deeplink |
| `{contact_url}` | サポート問い合わせ URL |
| `{operator_signature}` | 運営者署名 |

**件名**: `契約のレート制限・しきい値を更新しました`

**本文(テキスト版)**:

```
{recipient_display_name} 様

ご利用の open-faq の契約レート制限・しきい値を更新しました。

  - 変更日時: {changed_at_jst}(JST)
  - 変更内容:
{change_summary}
  - 変更理由: {reason_label}

詳細は管理画面の課金画面でご確認いただけます。

▶ 課金画面を開く
{rate_limit_screen_url}

▶ サポートに問い合わせる
{contact_url}

──────────────────────────────────────────────
本通知は open-faq 運営事務局からの自動配信です。返信は受け付けておりません。
{operator_signature}
```

**本文(HTML 版)**: 変更内容を新旧値の表で表示 + 課金画面プライマリボタン。

---

### 4.17 TPL-OP-IF12-AI-PARAM-CHANGED(AI 推論パラメータ上書き変更)→ 利用者宛

**発火条件**: 運営者が SCR-AI-PARAM-OVERRIDE から AI 推論パラメータを上書きした時。IF #12 経由で対象契約管理者へ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{changed_at_jst}` | 変更日時(JST、分まで)|
| `{parameter_diff_summary}` | 変更項目と新旧値の対比(200 字)|
| `{reason_label}` | 変更理由ラベル |
| `{contact_url}` | サポート問い合わせ URL |
| `{operator_signature}` | 運営者署名 |

**件名**: `AI パラメータが変更されました`

**本文(テキスト版)**:

```
{recipient_display_name} 様

ご利用の open-faq の AI 推論パラメータを {changed_at_jst}(JST) 時点で更新
しました。

変更内容:
{parameter_diff_summary}

変更理由: {reason_label}

回答品質や応答に変化を感じた場合はサポートまでご連絡ください。

▶ サポートに問い合わせる
{contact_url}

──────────────────────────────────────────────
本通知は open-faq 運営事務局からの自動配信です。返信は受け付けておりません。
{operator_signature}
```

**本文(HTML 版)**: 変更項目を新旧値の表で表示。

---

### 4.18 TPL-OP-IF12-WIDGET-FORCE-STOP(ウィジェット強制停止)→ 利用者宛

**発火条件**: 運営者が SCR-093 等からウィジェット強制停止を実施した時。IF #12 経由で対象契約管理者へ送信。

**テンプレート変数**:

| 変数 | 説明 |
|---|---|
| `{recipient_display_name}` | 受信者表示名 |
| `{stopped_at_jst}` | 停止日時(JST、分まで)|
| `{target_project_summary}` | 対象プロジェクト要約 |
| `{reason_label}` | 停止理由ラベル |
| `{contact_url}` | サポート問い合わせ URL |
| `{operator_signature}` | 運営者署名 |

**件名**: `[重要] FAQ ウィジェットを強制停止しました`

**本文(テキスト版)**:

```
{recipient_display_name} 様

ご利用の open-faq の FAQ ウィジェットを {stopped_at_jst}(JST) 時点で強制
停止しました。

  - 対象: {target_project_summary}
  - 停止理由: {reason_label}

停止解除のお手続きはサポートまでご連絡ください。停止中もウィジェット以外
の管理画面機能は通常通りご利用いただけます。

▶ サポートに問い合わせる
{contact_url}

──────────────────────────────────────────────
本通知は open-faq 運営事務局からの自動配信です。返信は受け付けておりません。
{operator_signature}
```

**本文(HTML 版)**: 赤系警告ヘッダ + 対象プロジェクト明示 + サポートプライマリボタン。

---

## 5. 配信運用

### 5.1 配信先解決ロジック

| 通知 ID | 配信先解決 |
|---|---|
| 運営者宛全テンプレート(`TPL-OP-*` の §3.1 全件) | `operator_accounts` テーブルから `disabled_at IS NULL` の全運営者(`TPL-OP-4EYES-APPROVED` は申請者本人のみ、`TPL-OP-ANN-TEST` はテスト送信実行者のみ)|
| TPL-OP-IF12-* | 対象 `accounts.owner_account_id` 配下のオーナー + 同一オーナースコープで `account_project_grants.role='admin'` を 1 件でも保持するメンバー |

### 5.2 集約窓(IF #12 経由 利用者宛通知)

- FR-211 に従い、10 分以内の連続イベントは 1 通にまとめる(運営者が短時間に複数操作した場合、利用者への通知が乱発するのを防ぐ)
- 集約時の件名は `[まとめ] N 件の運営者操作通知`、本文は時系列で操作ログを列挙

### 5.3 重要度別の強制送信ルール

- `critical`: 受信オプトアウト不可、必ずメール送信。`TPL-OP-HASH-CHAIN-NG` / `TPL-OP-SIG-FAIL` / `TPL-OP-FRAUD-DETECTED` / `TPL-OP-IF12-SUSPEND` / `TPL-OP-IF12-WIDGET-FORCE-STOP`
- `high`: 強制送信(運営者宛は inbox 通知も併用)
- `normal`: 強制送信(運営者は MVP では個別オプトアウト不可)

### 5.4 配信ログ

- 全送信は `operator_notification_logs` / `notification_logs`(IF #12 経由分)に記録
- 列: メイン側 §5.4 と同一構造 + `tenant_owner_account_id`(IF #12 経由分のみ)
- 保持期間: 運営者宛 = 1 年(`operator_high_priv` retention class)、IF #12 経由 = 90 日

### 5.5 テスト送信

- 開発・ステージング環境では `MAIL_SAFE_SINK_OPS` 環境変数で全宛先を `ops-mail-sink@<internal-domain>` に強制リダイレクト
- 件名にプレフィックス `[STAGING-OPS]` / `[DEV-OPS]` を自動付与

---

## 6. テンプレート開発・運用フロー

### 6.1 新規テンプレート追加手順

1. 通知契機を [06_メッセージ一覧.md](06_メッセージ一覧.md) §8.1 / §8.2 に追加
2. 本書 §3 索引 + §4 詳細にテンプレート行を追加
3. テンプレート変数を確定し i18n キーファイル(`locales/ja/email_templates_ops.yml`)に追加
4. ステージング `MAIL_SAFE_SINK_OPS` で目視確認
5. `check-spec-sync.sh` を実行し SC-001〜SC-011 全パスを確認

### 6.2 既存テンプレート変更手順

- 軽微な文言修正: 本書 §4 のみ更新 + 変更履歴に記載
- 件名変更 / CTA 変更: 本書 §4 + 06 メッセージ一覧 §8 + ステージング再送テスト
- 配信先・重要度変更: 本書 §5.1 / §5.3 + 06 メッセージ一覧 + 共有概念.md の整合確認

---

## 7. 変更履歴

| 日付 | 版数 | 変更内容 | 変更者 |
|---|---|---|---|
| 2026-05-19 | 1.0 | 初版作成。運営者システム送信の全 18 テンプレートを件名 + 本文(テキスト版・HTML 版要素)を含めて正本化。06 メッセージ一覧 §8.3 から件名・本文の主管責任を本書に移管。06 で未定義だった `TPL-OP-4EYES-APPROVED` / `TPL-OP-PII-REMINDER` / `TPL-OP-DLQ-STUCK` / `TPL-OP-FREQ-ANOMALY` / `TPL-OP-ANN-TEST` / `TPL-OP-SIG-FAIL` / `TPL-OP-WH-FAIL-RATE` / `TPL-OP-CRON-FAIL` / `TPL-OP-LOCK-CONFLICT` / `TPL-OP-IF12-RATE-CHANGED` / `TPL-OP-IF12-WIDGET-FORCE-STOP` を新規定義 | claude |
